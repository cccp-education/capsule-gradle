= TODO — capsule-gradle (Détroit)
:last_update: 2026-08-09 (Session 086 — CAP-ARCH-3 Koog pipeline ✅)

'''
== ✅ EPIC CAP-MODEL — TERMINE (session 069)

. [x] Unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US)
. [x] `SlideType` enum migré vers `capsule.feed`
. [x] `SlideSegment` étendu (type+manimScene)
. [x] `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (-64 lignes)
. [x] 6 consommateurs migrés
. [x] `capsule.CapsuleModels` purgé
. [x] 361 unit tests VERT (+124)
. [x] Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios)

'''
== ✅ Sync slider 0.0.16 — TERMINE (session 070)

. [x] Bump catalogue `slider = "0.0.16"` (was 0.0.15)
. [x] `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal)

'''
== ✅ CR-2 Thread safety + CR-3 TTS error handling + CR-4 XSS + CR-6 CLI + CR-7 YAML — TERMINE (session 071)

. [x] CR-2 — Thread safety + timeout `captureSlideParallel` (executor.shutdownNow finally, it.get(5, MIN))
. [x] CR-3 — Error handling TTS (retry 1× + accumulation `failedSlides` + logger.error)
. [x] CR-4 — HTML escape XSS `injectSubtitleTrack` (HtmlEscape object)
. [x] CR-6 — Robust CLI parsing (cliInt/cliDouble/cliBoolean helpers)
. [x] CR-7 — Validation YAML (déjà implémenté + test couverture)
. [x] Fix cucumber consolidated (DuplicateStepDefinitionException + feature quotes + temp file)

'''
== ✅ CR-5 DRY CapsuleConfigMerger — TERMINE (session 072)

. [x] `merge()` unifié — `yaml: T?` nullable dispatch
. [x] 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 unifiées
. [x] 4 helpers génériques `mergeStr/mergeInt/mergeDouble/mergeBoolean`
. [x] Sémantique préservée (`toBoolean()` lenient, `isNotBlank()` heuristic)
. [x] +13 unit tests NoYaml (48 total ConfigMergerTest)
. [x] +1 scénario cucumber `@config @no-yaml` + 1 step Given
. [x] 322→302 lignes (-19 net), 396 unit VERT (+12)

'''
== ✅ CR-8 Logging structuré — TERMINE (session 073)

. [x] `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`)
. [x] `formatConfigLog(config): List<String>` → 4 lignes structurées (TTS/Capture/Subtitles/Manim)
. [x] Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=)
. [x] `CapsulePlugin.kt` câblage (l.40-48 monolithic → 3 lignes forEach)
. [x] +9 unit tests `CapsuleConfigLoggerTest`
. [x] +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs
. [x] 405 unit VERT (+9), 0 failures, 0 errors

'''
== ✅ CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues — TERMINE (session 077)

. [x] BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés au build
. [x] Domaine `capsule.multilang` créé (7ème) — `VoiceMapping.kt` (62 lignes, 11 Piper + 11 espeak)
. [x] `MultiLanguageResolver.kt` (51 lignes) — `ResolvedLanguage` data class + resolver object
. [x] `Language` enum supprimé — `LanguageVoiceMapping.kt` détruit (40 lignes)
. [x] `TtsEngine.language(): SupportedLanguage?` — `PiperTtsEngine`/`EspeakTtsEngine` migrés
. [x] `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` — `Language.fromCode` → `MultiLanguageResolver.resolve`
. [x] 22 unit `MultiLanguageResolverTest` (resolve 10 codes, unknown→null, RTL ar/ur, piperModel/espeakVoice 11 codes)
. [x] 2 tests `LanguageVoiceMappingTest` adaptés + 4 tests `PiperTtsEngine`/`EspeakTtsEngine` migrés
. [x] 436 unit VERT (+15 net), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner — TERMINE (session 078)

. [x] `CapsuleVideoPlan.kt` — `CapsuleVideoEntry` (deckFile, scriptFile, language: SupportedLanguage, outputVideo) + invariants fail-fast (deck exists, script exists, language in LanguageCatalog)
. [x] `CapsuleVideoPlan` — invariant non-empty, accès `size()`/`isEmpty()`/`languages()`
. [x] `CapsuleVideoPlanner.kt` — object pur `plan(deckDir, scriptDir, outputDir, targetLanguages)`, skip langues sans deck+script, fail-fast plan vide
. [x] Nommage aligné slider — `<deckName>_<lang>-deck.html` + `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`
. [x] 10 unit `CapsuleVideoPlanTest` + 8 unit `CapsuleVideoPlannerTest` (avec `@TempDir`)
. [x] 454 unit VERT (+18 net), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.3 GenerateCapsuleVideoAllLanguagesTask — TERMINE (session 079)

. [x] Domaine `capsule.multilang` : `CapsuleVideoAllLanguagesRunner` (object pur, `run(plan, probeDuration, render)` + `shouldRender(entry, probeDuration)`)
. [x] Économie d'encre — skip si WebM existe ET probe > 0, re-render si probe ≤ 0 (artefact invalide)
. [x] `GenerateCapsuleVideoAllLanguagesTask : CapsuleVideoTask` — pipeline mono-langue réutilisé (CR-1), TTS par langue via `MultiLanguageResolver`
. [x] Refactor base `CapsuleVideoTask` — `execute()` → `open`, `resolveTtsEngine()`/`injectAudio()` → `internal` (zéro changement de comportement)
. [x] Enregistrement `generateCapsuleVideoAllLanguages` (groupe generate, CapsuleManager)
. [x] 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN) — correction : `LanguageCatalog` n'a pas "de" (10 langues)
. [x] 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`)
. [x] 460 unit VERT (+6 net), functional PASS, `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.4 Câblage translateAndGenerateCapsuleVideos — TERMINE (session 080)

. [x] Tâche composite `translateAndGenerateCapsuleVideos` — dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`
. [x] Constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + description (domaine `capsule.feed`)
. [x] `CapsuleFeedTaskRegistrar.registerTranslateAndGenerateCapsuleVideos` (group capsule)
. [x] Câblage 3ème appel registrar dans `CapsuleManager.registerExtractSpeakerNotesTask()`
. [x] 3 functional tests RED→GREEN (registration groupe, description, dependsOn chain `--dry-run` avec stub translateDeck)
. [x] 460 unit VERT (inchangé) + 25 functional (+3), `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.5 Cucumber BDD E2E 10 langues — TERMINE (session 081)

. [x] `multi_language_capsule.feature` — 6 scénarios `@multilang @i18n`
. [x] `MultiLanguageCapsuleSteps` — step defs (plan pur `CapsuleVideoPlanner` + E2E GradleRunner NoOp)
. [x] Plan 10 langues (LanguageCatalog), nommage `<deckName>_<lang>.webm`, skip langue sans paire, fail-fast plan vide
. [x] E2E NoOp 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered` + RTL ar/ur
. [x] `cucumberTest -PrunCucumber` : 6/6 multilang GREEN (suite 154 tests, 1 FAIL préexistant burn-in @integration — hors périmètre)
. [x] 460 unit VERT (inchangé) + 25 functional VERT + kover VERT, `check` BUILD SUCCESSFUL, zéro secret

'''
== ✅ CAP-29.6 Documentation + dogfooding + clôture EPIC — TERMINE (session 082)

. [x] README master EN + README plugin mis à jour (domaine `capsule.multilang`, table des tâches, convention nommage)
. [x] Dogfooding réel consommateur composite `includeBuild` : `extractSpeakerNotes` réel → `generateCapsuleVideoAllLanguages` réel
. [x] 3 WebM générés (fr/en/ar dont 1 RTL) en 3m07 (Piper fr/en + espeak, chromium Playwright)
. [x] Défaut nommage découvert : rendu `<deckName>_<lang>-deck.webm` au lieu du plan `<deckName>_<lang>.webm` (brise économie d'encre)
. [x] Fix TDD `CapsuleVideoOutputRenamer` (4 unit) + wiring post-capture `renderEntry`
. [x] Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` (3 langues) + économie d'encre prouvée (0/3 rendered, 3 skipped)
. [x] 464 unit VERT (+4 net) + 25 functional VERT + 6 cucumber multilang GREEN, zéro secret
. [x] EPIC CAP-29 TERMINÉ 7/7 US — domaine `capsule.multilang` clos (7 fichiers prod)

'''
== 🟡 EPIC CAP-ARCH — Capsule LLM-driven piloté par codebase (koog + langchain4j + RAG + graphify) — EN COURS (CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084, CAP-ARCH-2 ✅ S-085)

. [x] CAP-ARCH-0 — Cadrage EPIC refondu (audit code réel : zéro LLM, koog 0.8.0 mort, i18n-contracts déjà consommé) ✅ S-083
. [x] CAP-ARCH-1 — Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`, `CapsuleLlmService` register/resolve/adapter, bump koog 1.0.0, purge dépendance morte, tâche `capsuleAiSmokeTest`, 477 unit + 28 functional + 3 cucumber `@ai`) ✅ S-084
. [x] CAP-ARCH-2 — Contexte augmenté CompositeContext (domaine `capsule.context`, `CapsuleContextBuilder`, `CapsuleContext` invariant fail-fast, tâche `collectCapsuleAugmentedContext`, 491 unit + 31 functional + 4 cucumber `@context @rag`) ✅ S-085
. [x] CAP-ARCH-3 — Koog pipeline génération contenu (domaine `capsule.pipeline`, `CapsulePipelineGraph`, `generateCapsuleContent`, 540 unit + 34 functional) ✅ S-086
. [ ] CAP-ARCH-4 — Wiring exécution existante (sortie LLM → backend sans modification, décision compileOnly slider)
. [ ] CAP-ARCH-5 — i18n aligné sur le socle (translateDeck codebase-LLM, non-régression CAP-29)
. [ ] CAP-ARCH-6 — Cucumber BDD boundary cross-borough (@i18n @cross-borough @architecture @ai)

'''
== 🟡 Backlog restant — À planifier

. [x] **CR-9** (robust HTML parsing) — à coder ✅ S-074
. [x] **CR-10** (CI optimization cucumberTest onlyIf) — à coder ✅ S-075
. [x] **CAP-29** (pipeline traduction vidéo multi-langue) — TERMINÉ 7/7 US (CAP-29.0 ✅ S-076, CAP-29.1 ✅ S-077, CAP-29.2 ✅ S-078, CAP-29.3 ✅ S-079, CAP-29.4 ✅ S-080, CAP-29.5 ✅ S-081, CAP-29.6 ✅ S-082)
. [x] **CAP-ARCH-0** (cadrage refondu LLM-driven via codebase) — ✅ S-083
. [x] **CAP-ARCH-1** (socle LLM bridge codebase, domaine `capsule.ai`) — ✅ S-084
. [x] **CAP-ARCH-2** (contexte augmenté CompositeContext, domaine `capsule.context`) — ✅ S-085
. [x] **CAP-ARCH-3** (koog pipeline génération contenu, domaine `capsule.pipeline`) — ✅ S-086
. [ ] **CAP-ARCH-4→6** (wiring, i18n, cucumber boundary) — EN COURS (prochaine session 087)

'''
== ✅ README i18n — TERMINE (session 041)

. [x] 20 fichiers README créés (10 langues × consumer + plugin)
. [x] `README.consommateurs/` — guide utilisateur (10 langues)
. [x] `README.plugin/` — guide développeur/contributeur (10 langues)
. [x] Badges shields.io (Maven Central 0.0.1-SNAPSHOT, Plugin Portal, CI, License)
. [x] Marqueurs : 195 unit + 22 Cucumber PASS, version 0.0.1-SNAPSHOT (non publié)
. [x] Switcher i18n 10 langues (EN/FR/ES/PT/RU/AR/UR/ZH/HI/BN)
. [x] AR/UR RTL (Right-To-Left)

'''
== ✅ CAP-26 Style burn-in configurable — TERMINE

. [x] **DSL `subtitleBurnInStyle`** — data class `SubtitleBurnInStyle(fontSize, fontColor, outlineColor, position)` + validation + `toForceStyle()`
. [x] **CapsuleExtension** — 4 nouvelles Property (subtitleBurnInFontSize, subtitleBurnInFontColor, subtitleBurnInOutlineColor, subtitleBurnInPosition)
. [x] **CapsuleConfig** — 4 nouveaux champs dans `CaptureConfig`
. [x] **CapsuleConfigMerger** — merge 4 sources (ENV, props, YAML, CLI) pour les 4 champs style
. [x] **CapsuleScaffoldTask** — scaffold style dans `capsule-context.yml`
. [x] **SubtitleBurnInServiceImpl** — paramètre `style: SubtitleBurnInStyle`, `toForceStyle()` dans commande FFmpeg
. [x] **CapsuleVideoTask** — `resolveSubtitleBurnInService()` construit le style depuis l'extension
. [x] **Tests unitaires** — `SubtitleBurnInStyleTest` (16 tests : defaults, validation, toForceStyle, alignments)
. [x] **Cucumber** — 2 scénarios @subtitles @burnin @style (defaults + custom style)
. [x] **Compile + unit tests PASS**

'''
== ✅ CR-1 Refactor CapsuleVideoTask.execute() — TERMINE (session 040)

. [x] Extraire `synthesizeTtsForScript()` (14 lignes) + 3 tests unitaires
. [x] Extraire `renderManimSlides()` (40 lignes) + 2 tests unitaires
. [x] Extraire `replaceManimSlidesInDeck()` (45 lignes) + 2 tests unitaires
. [x] Extraire `captureDeckSequential()` (30 lignes) + 1 test unitaire
. [x] Extraire `captureDeckParallel()` (20 lignes) + 1 test unitaire
. [x] execute() → orchestration pure (76 lignes, vs 218 avant)
. [x] Compile + unit tests PASS

== ✅ CR-9 Robust HTML parsing — TERMINE (session 074)

. [x] `HtmlSectionParser.findTopLevelSectionOpenTags(html): List<MatchResult>` (38 lignes)
. [x] Depth-tracking existant réutilisé (skip nested + self-closing)
. [x] `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`
. [x] DRY — réutilise `HtmlSectionParser` (DRY-2 S-031)
. [x] Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections
. [x] +8 unit tests `HtmlSectionParserTest.findTopLevelSectionOpenTags` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline)
. [x] +1 unit test `CapsuleVideoTaskTest.sequential fallback skips nested sections in vertical stack` (data-audio count = 2, pas 4)
. [x] +1 scénario cucumber `@audio @sequential` + 2 step defs (Given nested, Then exactly N)
. [x] 414 unit tests VERT (+9), 0 FAIL, 0 SKIP

'''
== ✅ CR-10 CI optimization cucumberTest onlyIf — TERMINE (session 075)

. [x] Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes : shouldRun/shouldSkip/skipReason)
. [x] 7 unit tests `CucumberTestGuardTest` (runCucumber active, CI active, both, neither, shouldSkip negation, skipReason mention, skipReason blank)
. [x] Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée — script ne peut pas importer classpath `main`)
. [x] BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps` (5 step defs)
. [x] `./gradlew check` VERT — cucumberTest SKIPPED sans flag (garde active)
. [x] `./gradlew cucumberTest -PrunCucumber` s'exécute (garde désactivée)
. [x] 421 unit tests VERT (+7), 0 FAIL, 0 SKIP
. [x] Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature` (à investiguer session future)

'''
== 🟡 Prochaine session — Backlog capsule restant (CAP-CR3, CAP-ARCH)

'''
== Backlog priorisé (ordre d'attaque)

. [x] 🔴 CR-1 — Refactor CapsuleVideoTask.execute() (218→76 lignes orchestration) ✅ S-040
. [x] 🔴 CR-2 — Thread safety + timeout captureSlideParallel ✅ S-071
. [x] 🔴 CR-3 — Error handling TTS — dégradation signalée + retry ✅ S-071
. [x] 🟠 CR-4 — Sécurité HTML escape injectSubtitleTrack ✅ S-071
. [x] 🟠 CR-5 — DRY CapsuleConfigMerger (~76 lignes dupliquées) ✅ S-072
. [x] 🟠 CR-6 — Robust CLI parsing (`as? Int` → `toIntOrNull()`) ✅ S-071
. [x] 🟠 CR-7 — Validation YAML avec try/catch gracieux ✅ S-071
. [x] 🟡 CR-8 — Logging structuré CapsulePlugin ✅ S-073
. [x] 🟡 CR-9 — Robust HTML parsing (HtmlSectionParser au lieu de regex raw) ✅ S-074
. [x] 🟡 CR-10 — CI optimization cucumberTest onlyIf ✅ S-075
. [ ] 🟢 CAP-27 — VTT burn-in
. [ ] 🟢 CAP-28 — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%)
. [ ] 🔴 CAP-CR3-1 — CI `full` avec vrais binaires + probe de durée vidéo
. [ ] 🔴 CAP-CR3-2 — Mode strict anti-NoOp (`capsule.strictMode`)
. [ ] 🟠 CAP-CR3-3 — Décision `ScreenshotCaptureImpl` vs `PlaywrightCaptureImpl`
. [ ] 🟡 CAP-CR3-4 — Convention Gradle `resolveScriptDir/resolveDeckDir`
. [x] 🟢 CAP-29 — Pipeline Traduction Vidéo Multi-Langue (7 US ~20 pts, TERMINÉ 7/7 US : CAP-29.0 ✅ S-076, CAP-29.1 ✅ S-077, CAP-29.2 ✅ S-078, CAP-29.3 ✅ S-079, CAP-29.4 ✅ S-080, CAP-29.5 ✅ S-081, CAP-29.6 ✅ S-082)
. [x] 🟢 CAP-ARCH — Collaboration cross-borough slider+codebase (7 US ~21 pts, refondu S-083 LLM-driven via codebase — CAP-ARCH-0 ✅ S-083, CAP-ARCH-1 ✅ S-084, CAP-ARCH-2 ✅ S-085, CAP-ARCH-3 ✅ S-086, prochaine CAP-ARCH-4 S-087)
