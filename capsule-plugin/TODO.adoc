= TODO — capsule-gradle (Détroit)
:last_update: 2026-08-07 (Session 074 — CR-9 robust HTML parsing terminé)

'''
== ✅ EPIC CAP-MODEL — TERMINE (session 069)

. [x] Unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US)
. [x] `SlideType` enum migré vers `capsule.feed`
. [x] `SlideSegment` étendu (type+manimScene)
. [x] `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (-64 lignes)
. [x] 6 consommateurs migrés
. [x] `capsule.CapsuleModels` purgé
. [x] 361 unit tests VERT (+124)
. [x] Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios)

'''
== ✅ Sync slider 0.0.16 — TERMINE (session 070)

. [x] Bump catalogue `slider = "0.0.16"` (was 0.0.15)
. [x] `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal)

'''
== ✅ CR-2 Thread safety + CR-3 TTS error handling + CR-4 XSS + CR-6 CLI + CR-7 YAML — TERMINE (session 071)

. [x] CR-2 — Thread safety + timeout `captureSlideParallel` (executor.shutdownNow finally, it.get(5, MIN))
. [x] CR-3 — Error handling TTS (retry 1× + accumulation `failedSlides` + logger.error)
. [x] CR-4 — HTML escape XSS `injectSubtitleTrack` (HtmlEscape object)
. [x] CR-6 — Robust CLI parsing (cliInt/cliDouble/cliBoolean helpers)
. [x] CR-7 — Validation YAML (déjà implémenté + test couverture)
. [x] Fix cucumber consolidated (DuplicateStepDefinitionException + feature quotes + temp file)

'''
== ✅ CR-5 DRY CapsuleConfigMerger — TERMINE (session 072)

. [x] `merge()` unifié — `yaml: T?` nullable dispatch
. [x] 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 unifiées
. [x] 4 helpers génériques `mergeStr/mergeInt/mergeDouble/mergeBoolean`
. [x] Sémantique préservée (`toBoolean()` lenient, `isNotBlank()` heuristic)
. [x] +13 unit tests NoYaml (48 total ConfigMergerTest)
. [x] +1 scénario cucumber `@config @no-yaml` + 1 step Given
. [x] 322→302 lignes (-19 net), 396 unit VERT (+12)

'''
== ✅ CR-8 Logging structuré — TERMINE (session 073)

. [x] `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`)
. [x] `formatConfigLog(config): List<String>` → 4 lignes structurées (TTS/Capture/Subtitles/Manim)
. [x] Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=)
. [x] `CapsulePlugin.kt` câblage (l.40-48 monolithic → 3 lignes forEach)
. [x] +9 unit tests `CapsuleConfigLoggerTest`
. [x] +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs
. [x] 405 unit VERT (+9), 0 failures, 0 errors

'''
== 🟡 EPIC CAP-ARCH — Collaboration cross-borough (slider + codebase) — CADRÉ (session 073)

. [ ] CAP-ARCH-0 — Cadrage EPIC (audit appels LLM dispersés, inventaire contrats N0)
. [ ] CAP-ARCH-1 — Migration i18n vers `i18n-contracts` (`CapsuleI18nBridge`)
. [ ] CAP-ARCH-2 — Délégation génération contenu → codebase (RAG + vibecoding)
. [ ] CAP-ARCH-3 — Pipeline i18n aligné sur slider (`translateDeck` → capsule)
. [ ] CAP-ARCH-4 — Suppression appels LLM dispersés (capsule = consommateur terminal)
. [ ] CAP-ARCH-5 — Cucumber BDD boundary cross-borough (slider → capsule × 10 langues)

'''
== 🟡 Backlog restant — À planifier

. [ ] **CR-9** (robust HTML parsing) — à coder
. [ ] **CR-10** (CI optimization cucumberTest onlyIf) — à coder
. [ ] **CAP-29** (pipeline traduction vidéo multi-langue) — à cadrer (débloqué)
. [ ] **CAP-ARCH** (collaboration cross-borough slider+codebase) — à planifier après CAP-29 et CR-10

'''
== ✅ README i18n — TERMINE (session 041)

. [x] 20 fichiers README créés (10 langues × consumer + plugin)
. [x] `README.consommateurs/` — guide utilisateur (10 langues)
. [x] `README.plugin/` — guide développeur/contributeur (10 langues)
. [x] Badges shields.io (Maven Central 0.0.1-SNAPSHOT, Plugin Portal, CI, License)
. [x] Marqueurs : 195 unit + 22 Cucumber PASS, version 0.0.1-SNAPSHOT (non publié)
. [x] Switcher i18n 10 langues (EN/FR/ES/PT/RU/AR/UR/ZH/HI/BN)
. [x] AR/UR RTL (Right-To-Left)

'''
== ✅ CAP-26 Style burn-in configurable — TERMINE

. [x] **DSL `subtitleBurnInStyle`** — data class `SubtitleBurnInStyle(fontSize, fontColor, outlineColor, position)` + validation + `toForceStyle()`
. [x] **CapsuleExtension** — 4 nouvelles Property (subtitleBurnInFontSize, subtitleBurnInFontColor, subtitleBurnInOutlineColor, subtitleBurnInPosition)
. [x] **CapsuleConfig** — 4 nouveaux champs dans `CaptureConfig`
. [x] **CapsuleConfigMerger** — merge 4 sources (ENV, props, YAML, CLI) pour les 4 champs style
. [x] **CapsuleScaffoldTask** — scaffold style dans `capsule-context.yml`
. [x] **SubtitleBurnInServiceImpl** — paramètre `style: SubtitleBurnInStyle`, `toForceStyle()` dans commande FFmpeg
. [x] **CapsuleVideoTask** — `resolveSubtitleBurnInService()` construit le style depuis l'extension
. [x] **Tests unitaires** — `SubtitleBurnInStyleTest` (16 tests : defaults, validation, toForceStyle, alignments)
. [x] **Cucumber** — 2 scénarios @subtitles @burnin @style (defaults + custom style)
. [x] **Compile + unit tests PASS**

'''
== ✅ CR-1 Refactor CapsuleVideoTask.execute() — TERMINE (session 040)

. [x] Extraire `synthesizeTtsForScript()` (14 lignes) + 3 tests unitaires
. [x] Extraire `renderManimSlides()` (40 lignes) + 2 tests unitaires
. [x] Extraire `replaceManimSlidesInDeck()` (45 lignes) + 2 tests unitaires
. [x] Extraire `captureDeckSequential()` (30 lignes) + 1 test unitaire
. [x] Extraire `captureDeckParallel()` (20 lignes) + 1 test unitaire
. [x] execute() → orchestration pure (76 lignes, vs 218 avant)
. [x] Compile + unit tests PASS

== ✅ CR-9 Robust HTML parsing — TERMINE (session 074)

. [x] `HtmlSectionParser.findTopLevelSectionOpenTags(html): List<MatchResult>` (38 lignes)
. [x] Depth-tracking existant réutilisé (skip nested + self-closing)
. [x] `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`
. [x] DRY — réutilise `HtmlSectionParser` (DRY-2 S-031)
. [x] Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections
. [x] +8 unit tests `HtmlSectionParserTest.findTopLevelSectionOpenTags` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline)
. [x] +1 unit test `CapsuleVideoTaskTest.sequential fallback skips nested sections in vertical stack` (data-audio count = 2, pas 4)
. [x] +1 scénario cucumber `@audio @sequential` + 2 step defs (Given nested, Then exactly N)
. [x] 414 unit tests VERT (+9), 0 FAIL, 0 SKIP

'''
== 🟡 Prochaine session — CR-10 CI optimization cucumberTest onlyIf

'''
== Backlog priorisé (ordre d'attaque)

. [x] 🔴 CR-1 — Refactor CapsuleVideoTask.execute() (218→76 lignes orchestration) ✅ S-040
. [x] 🔴 CR-2 — Thread safety + timeout captureSlideParallel ✅ S-071
. [x] 🔴 CR-3 — Error handling TTS — dégradation signalée + retry ✅ S-071
. [x] 🟠 CR-4 — Sécurité HTML escape injectSubtitleTrack ✅ S-071
. [x] 🟠 CR-5 — DRY CapsuleConfigMerger (~76 lignes dupliquées) ✅ S-072
. [x] 🟠 CR-6 — Robust CLI parsing (`as? Int` → `toIntOrNull()`) ✅ S-071
. [x] 🟠 CR-7 — Validation YAML avec try/catch gracieux ✅ S-071
. [x] 🟡 CR-8 — Logging structuré CapsulePlugin ✅ S-073
. [x] 🟡 CR-9 — Robust HTML parsing (HtmlSectionParser au lieu de regex raw) ✅ S-074
. [ ] 🟡 CR-10 — CI optimization cucumberTest onlyIf
. [ ] 🟢 CAP-27 — VTT burn-in
. [ ] 🟢 CAP-28 — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%)
. [ ] 🟢 CAP-29 — Pipeline Traduction Vidéo Multi-Langue (6 US ~20 pts, débloqué)
. [ ] 🟠 CAP-ARCH — Collaboration cross-borough slider+codebase (6 US ~19 pts, cadré S-073)
