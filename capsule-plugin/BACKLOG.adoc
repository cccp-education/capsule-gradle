= BACKLOG — capsule-gradle (Détroit)
:toc:
:last_update: 2026-06-13 (Session 029 — Publication Maven Central)

'''
== 📦 Publication Maven Central ✅

`education.cccp:capsule-plugin:0.0.1` publié via nmcp (Session 029).
195 unit + 22 Cucumber = 217 tests PASS.

'''

== 🔴 P0 — Code Review Session 028

. **DRY: wavToMp3 dupliqué ×2** — Identifier dans PiperTtsEngine et EspeakTtsEngine. Extraire dans `AudioUtil`.
. **DRY: ffprobeDuration dupliqué ×3** — CapsuleVideoTask, ManimEngineImpl, ManimVideoMixerImpl. MediaProbeUtil existe déjà, l'utiliser.
. **DRY: extractTopLevelSections dupliqué ×2** — CapsuleVideoTask + ManimSlideReplacer.
. **Resource leak Playwright** — `capture.close()` pas dans try/finally (branche séquentielle).
. **Magic number `4`** — `Executors.newFixedThreadPool(4)` non configurable.

== 🟠 P1

. **println → logger** — Remplacer `println()` dans PlaywrightCaptureImpl (l.75), CapsuleManager.parseScript (l.177-218).
. **Manim slide path replacement** — Remplacer slide HTML Manim par MP4 muxée.
. **Manim slide duration** — Utiliser `ManimEngine.probeDuration()`.
. **Cucumber @integration Manim** — Scénario BDD end-to-end ManimEngine noop.

== 🟡 P2

. **Code mort: isPlaceholder()** — Supprimer ou réutiliser.
. **Parallélisme configurable** — Remplacer magic number `4` par property DSL.
. **ManimEngine configurable** — Scripts Manim en dehors de `src/manim/`.
. **Manim parallel rendering** — Déjà implémenté (ManimParallelRenderer), valider.

== 🟢 P3

. **lateinit var → lazy delegation** — Remplacer `lateinit var capsuleExtension`.
. **Fichier temporaire concat** — Garantir cleanup dans try/finally.
. **ManimConfig.validate()** — Simplifier avec `when` explicite.

'''

== Prochain EPIC

1. **P0 DRY wavToMp3** — Extraire `AudioUtil`. ~1 pt. < 30 min.
2. **P0 ffprobeDuration** — Unifier sur `MediaProbeUtil`. ~1 pt. < 30 min.
3. **P0 Resource leak** — try/finally capture.close(). ~0.5 pt. < 10 min.
