= BACKLOG — capsule-gradle (Détroit)
:toc:
:last_update: 2026-06-15 (Session 031 — P0 DRY + Resource Leak + Magic Number)

'''
== 📦 Publication Maven Central ✅

`education.cccp:capsule-plugin:0.0.1` publié via nmcp (Session 029).
195 unit + 22 Cucumber = 217 tests PASS.

'''
== 🔴 P0 — Code Review Session 028 ✅ TERMINE (Session 031)

. ✅ **DRY: wavToMp3 dupliqué ×2** — Extraire dans `AudioConversionUtil`. PiperTtsEngine et EspeakTtsEngine déléguent.
. ✅ **DRY: ffprobeDuration dupliqué ×3** — Unifié sur `MediaProbeUtil`. CapsuleVideoTask, ManimEngineImpl, ManimVideoMixerImpl déléguent.
. ✅ **DRY: extractTopLevelSections dupliqué ×2** — Extraire dans `HtmlSectionParser`. CapsuleVideoTask et ManimSlideReplacer déléguent.
. ✅ **Resource leak Playwright** — `capture.close()` dans finally (branche séquentielle).
. ✅ **Magic number `4`** — `CaptureConfig.parallelCaptureThreads` + `ManimConfig.parallelRenderThreads` DSL 4 sources.

'''
== 🟠 P1

. **println → logger** — Remplacer `println()` dans PlaywrightCaptureImpl (l.75), CapsuleManager.parseScript (l.177-218).

'''
== 🟡 P2

. **Code mort: isPlaceholder()** — Supprimer ou réutiliser.

'''
== 🟢 P3

. **lateinit var → lazy delegation** — Remplacer `lateinit var capsuleExtension`.
. **Fichier temporaire concat** — Garantir cleanup dans try/finally.
. **ManimConfig.validate()** — Simplifier avec `when` explicite.

'''
== Prochain EPIC — Session 032

1. **P1 println → logger** — PlaywrightCaptureImpl l.75 + CapsuleManager.parseScript. ~1 pt.
2. **P2 isPlaceholder()** — Supprimer ou réutiliser. ~0.5 pt.
3. **P3 lateinit → lazy** — Sécuriser extensions de tâches. ~1 pt.
4. **P3 concat-cleanup** — try/finally fichier temporaire. ~0.5 pt.
5. **P3 validate-simplify** — ManimConfig.validate() → when explicite. ~0.5 pt.
