= BACKLOG — capsule-gradle (Détroit)

== P0 — CRITIQUE

. **CAP-13.3** — CapsuleConfigMerger : merge 4 sources (ENV < props < YAML < CLI). Pattern plantuml-gradle.

== P1

. **CAP-13.4** — CapsuleExtension revisitée : `configPath: Property<String>` + wiring afterEvaluate
. **CAP-13.5** — capsule-context.yml scaffold task
. **CAP-13.6** — Plugin wiring afterEvaluate → ConfigMerger
. **Cucumber Manim** — Scénario Gherkin pipeline [manim:SceneName] avec mock/skip conditionnel
. **Intégration ManimEngine** — Quand SlideType.MANIM, appeler ManimEngine.render() au lieu de PlaywrightCapture
. **CapsuleManager resolveManimEngine()** — Factory résolvant ManimEngine depuis CapsuleConfig
