# 📊 RAPPORT DE CODE REVIEW COMPLET - `cccp-edu/capsule-gradle`

---

## 📋 EXECUTIVE SUMMARY

| Métrique | Évaluation |
|----------|-----------|
| **Qualité Globale** | 🟡 **MOYEN** (6/10) |
| **Risque de Merge** | 🔴 **ÉLEVÉ** |
| **Testabilité** | 🟠 **FAIBLE** |
| **Maintenabilité** | 🟠 **FAIBLE** |
| **Sécurité** | 🟠 **PRÉOCCUPANTE** |

**Verdict**: ❌ **NE PAS MERGER** sans corrections majeures

---

## 🔍 ANALYSE DÉTAILLÉE PAR FICHIER

### 1️⃣ **CapsuleVideoTask.kt** - 🔴 CRITIQUE

**Stats**:
- 758 lignes
- Complexité cyclomatique estimée: ~45
- Responsabilités: 8+

**Problèmes Critiques**:

#### 🐛 **Problème #1: Ligne 747 TRONQUÉE**
```kotlin
747| logger.lifecycle("  Audio mix: {} slides concatenated (audio={}s, video={}s)", totalSlides, String.format("%.1f", audioDur), String.format("%.1f", MediaProbeUtil.probeDuration(vid[...]
```
**→ CODE INCOMPLET !** Le fichier s'arrête au milieu d'un appel.

#### 🐛 **Problème #2: Fonction execute() MONSTRUEUSE (lignes 283-501)**

```
Niveaux d'imbrication: 6
Lignes de code: 218
Boucles imbriquées: 3
Appels externes: 15+
```

**Chemin d'exécution**:
```
execute()
├─ pour chaque script
│  ├─ parseScript()
│  ├─ pour chaque slide TTS
│  │  └─ engine.synthesize()  ← 1er point de défaillance (silencieusement ignoré !)
│  ├─ si Manim slides
│  │  ├─ parallelRenderer.renderAll()
│  │  ├─ pour chaque Manim slide
│  │  │  ├─ manim.probeDuration()
│  │  │  └─ manimMixer.mix()
│  │  └─ manimReplacer.replaceSlideAt()
│  ├─ generateSubtitles()
│  ├─ si parallelCaptureEnabled
│  │  └─ captureSlideParallel()  ← Appel threads sans timeout !
│  └─ sinon
│     ├─ resolvePlaywrightCapture()
│     ├─ deckCapture.capture()
│     ├─ mixAudioWithVideo()
│     └─ burnInSubtitlesIfEnabled()
```

**Recommandation**: Décomposer en 5-6 méthodes privées

#### 🔴 **Problème #3: Gestion d'erreurs silencieuse (ligne 331)**

```kotlin
for (seg in parsed.slides) {
    val ttsFile = audioDir.resolve("slide-$idx.mp3")
    if (!ttsFile.exists()) {
        try {
            engine.synthesize(seg.speakerNote, ttsFile)
            logger.lifecycle("  TTS → {} ({} chars)", ttsFile.name, seg.speakerNote.length)
        } catch (e: TtsException) {
            logger.warn("  TTS SKIP slide {}: {}", seg.index, e.message)
            // ❌ CONTINUE sans créer fichier audio !
            // Résultat: Capsule vidéo silencieuse pour ce slide
            // Aucune indication à l'utilisateur que la vidéo est dégradée
        }
    }
}
```

**Impact**: 
- ❌ Vidéos incomplètes générées sans avertissement critique
- ❌ Pas de validation finale
- ❌ Pas de retry

#### 🔴 **Problème #4: Thread Pool non fermé (ligne 609)**

```kotlin
val executor = Executors.newFixedThreadPool(capsuleExtension.parallelCaptureThreads.get())
val futures = mutableListOf<Future<File?>>()

// ... soumettre tasks ...

val webmFiles = futures.mapNotNull { it.get() }  // ← BLOQUE À L'INFINI si une task échoue !
executor.shutdown()  // ← jamais atteint en cas d'exception

// ✅ DOIT ÊTRE
try {
    val webmFiles = futures.map { 
        try {
            it.get(5, TimeUnit.MINUTES)  // ← timeout critique !
        } catch (e: TimeoutException) {
            logger.error("Slide capture timeout")
            null
        }
    }
} finally {
    executor.shutdownNow()  // Force l'arrêt même en erreur
}
```

#### 🟠 **Problème #5: Injection HTML sans échappement (ligne 537)**

```kotlin
val trackElement = """<track kind="captions" src="${subtitleFile.name}" srclang="${capsuleExtension.ttsLanguage.get()}"..."""
```

**Scénario XSS**:
```
ttsLanguage.get() = """fr"><script>alert('XSS')</script><div srclang="es"""
↓
<track kind="captions" src="subs.vtt" srclang="fr"><script>alert('XSS')</script>...
```

**Fix**:
```kotlin
import org.apache.commons.text.StringEscapeUtils  // ou HtmlEscapers de Guava
val language = StringEscapeUtils.escapeHtml4(capsuleExtension.ttsLanguage.get())
val trackElement = """<track kind="captions" src="${subtitleFile.name}" srclang="$language"..."""
```

#### 🟠 **Problème #6: Regex non sécurisée (ligne 678)**

```kotlin
val slideRegex = Regex("""<section\b[^>]*>""")
val sections = slideRegex.findAll(originalHtml).toList()
```

**Risque**: Si HTML malformé (tags non fermés, attributs exotiques), comportement imprévisible.

---

### 2️⃣ **CapsuleConfigMerger.kt** - 🟠 MAJEUR

**Stats**:
- 292 lignes
- Duplication de code: **~60%**

**Problème #1: Duplication massive**

```kotlin
// Fonction 1 (lignes 178-186)
private fun mergeInputConfig(
    env: InputConfig, props: InputConfig, yaml: InputConfig, cli: Map<String, Any?>
): InputConfig {
    return InputConfig(
        outputDir = cli["input.outputDir"]?.toString() 
            ?: yaml.outputDir.ifNotBlankOrElse(props.outputDir),
        // ... 3 autres champs
    )
}

// Fonction 2 (lignes 239-247) - PRESQUE IDENTIQUE !
private fun mergeInputConfigNoYaml(
    env: InputConfig, props: InputConfig, cli: Map<String, Any?>
): InputConfig {
    return InputConfig(
        outputDir = cli["input.outputDir"]?.toString() 
            ?: props.outputDir.ifNotBlankOrElse(env.outputDir),
        // ... 3 autres champs (ordre différent !)
    )
}
```

**Duplication identifiée**:
- `mergeInputConfig` + `mergeInputConfigNoYaml` = 16 lignes × 2 = 32 lignes
- `mergeTtsConfig` + `mergeTtsConfigNoYaml` = 10 lignes × 2 = 20 lignes
- `mergeCaptureConfig` + `mergeCaptureConfigNoYaml` = 12 lignes × 2 = 24 lignes
- **TOTAL**: ~76 lignes de code dupliqué

**Fix recommandé**:
```kotlin
// Stratégie générique
private fun <T> mergeConfig(
    cli: Map<String, Any?>,
    yamlValue: String,
    propsValue: String,
    envValue: String,
    key: String
): String = cli[key]?.toString() 
    ?: yamlValue.ifNotBlankOrElse(propsValue.ifNotBlankOrElse(envValue))
```

**Problème #2: Conversions fragiles (ligne 195)**

```kotlin
espeakSpeed = cli["tts.espeakSpeed"] as? Int ?: yaml.espeakSpeed,
// ❌ Si cli["tts.espeakSpeed"] = "150" (String), retourne null → yaml.espeakSpeed
// Mais "150".toIntOrNull() = 150 (valide !)
// INCONSISTANCE : CLI accepte String, mais cast attend Int
```

**Problème #3: Priorité mal documentée**

```kotlin
/**
 * Merges configuration from four sources with priority:
 * ENV vars < gradle.properties < YAML file < CLI -P params
 *
 * Pattern aligné sur plantuml-gradle ConfigMerger.
 * Each higher-priority source overrides the same key from lower-priority sources.
 */
```

Mais le code réel :
- Ligne 30-36: Si yamlLoaded = false → logique différente !
- Ligne 40-44: Si yamlLoaded = true → logique différente !
- ⚠️ Deux chemins d'exécution NON testés ensemble

---

### 3️⃣ **CapsulePlugin.kt** - 🟡 MOYEN

**Stats**:
- 112 lignes
- Coupling: 3 (direct calls)

**Problème #1: Logging non scannable (lignes 36-44)**

```kotlin
logger.lifecycle(
    "Capsule config resolved: engine={}, voice={}, language={}, vw={}x{}, parallel={}, subtitle={}/{}/burnIn={} manim=[path={}, quality={}, scriptsDir={}, outputDir={}]",
    mergedConfig.tts.engine, mergedConfig.tts.voice, mergedConfig.tts.language,
    mergedConfig.capture.viewportWidth, mergedConfig.capture.viewportHeight,
    mergedConfig.capture.parallelCaptureEnabled,
    mergedConfig.capture.subtitleEnabled, mergedConfig.capture.subtitleFormat, mergedConfig.capture.subtitleBurnIn,
    mergedConfig.manim.executablePath, mergedConfig.manim.quality,
    mergedConfig.manim.scriptsDir, mergedConfig.manim.outputDir
)
```

**Résultat en log**:
```
Capsule config resolved: engine=piper, voice=fr_FR-siwis-medium, language=fr, vw=1408x792, 
parallel=false, subtitle=true/srt/burnIn=true manim=[path=/usr/bin/manim, quality=l, 
scriptsDir=src/manim, outputDir=build/capsule/manim]
```

❌ Impossible à parser avec les yeux !

**Fix**:
```kotlin
logger.lifecycle("✓ Capsule config resolved:")
logger.lifecycle("  TTS: engine={} voice={} language={}", 
    mergedConfig.tts.engine, mergedConfig.tts.voice, mergedConfig.tts.language)
logger.lifecycle("  Capture: {}x{} parallel={}", 
    mergedConfig.capture.viewportWidth, mergedConfig.capture.viewportHeight, 
    mergedConfig.capture.parallelCaptureEnabled)
// ... etc
```

**Problème #2: Configuration non validée**

```kotlin
val configPath = capsuleExt.configPath.get()
val configFile = File(project.projectDir, configPath)
val yamlLoaded = configFile.exists() && configFile.length() > 0
// ✓ Bon : check fichier
// ✓ Bon : check size > 0

// MAIS ensuite :
val yamlConfig = CapsuleConfigLoader.load(configFile)
// ❌ Si YAML malformé (syntax error), quelle exception ?
// ❌ Pas de try/catch ! Task échoue sans message clair
```

---

### 4️⃣ **CapsuleManager.kt** - 🟢 BON

**Stats**:
- 258 lignes
- Complexity: 12
- Responsibility: 1 (orchestration)

✅ **Points positifs**:
- Patterns d'usine clairs (resolveManimEngine, resolveManimParallelRenderer)
- Dégradation gracieuse (NoOp fallbacks)
- Parseur de script lisible (lignes 160-224)

⚠️ **Améliorations mineures**:
- Ligne 192: `colonIdx` → variable muette, calculée deux fois
- Ligne 234-240: résolution de répertoire complexe, pas de logs

---

### 5️⃣ **Build & Tests** - 🟠 INCOMPLET

**capsule-plugin/build.gradle.kts** (lignes 1-132):

```kotlin
val cucumberTest by tasks.registering(Test::class) {
    description = "Runs Cucumber BDD tests"
    group = "verification"
    // ...
    outputs.upToDateWhen { false }  // ← Jamais en cache = toujours re-run (coûteux)
}

tasks.check {
    dependsOn(cucumberTest)
}
```

⚠️ **Problème** : Tester `cucumberTest` à chaque `./gradlew check` prend ~15 min (Playwright + FFmpeg) !

**Recommandation**:
```kotlin
val cukeTest by tasks.registering(Test::class) {
    onlyIf { project.hasProperty("runCucumber") || CI }
    // ← Ne lance que manuellement ou en CI
}
```

---

## 🎯 RÉSUMÉ DES PROBLÈMES

| Sévérité | Nombre | Exemples |
|----------|--------|----------|
| 🔴 BLOQUANT | **3** | Ligne 747 tronquée, execute() monstrueuse, pas de timeout threads |
| 🟠 MAJEUR | **6** | XSS HTML, duplication config, erreurs silencieuses TTS |
| 🟡 MOYEN | **4** | Logging, validation config, regex non sécurisée |
| 🟢 MINEUR | **2** | Logging bruyant, variables muettes |

**TOTAL**: 15 problèmes identifiés

---

## ✅ POINTS POSITIFS

| Aspect | Observation |
|--------|-------------|
| **Architecture** | Interfaces bien définies (TtsEngine, ManimEngine) ✓ |
| **Dégradation** | NoOp implementations évitent les crashs ✓ |
| **Config** | 4 sources (ENV/Props/YAML/CLI) orchestrées correctement ✓ |
| **Parsing** | Script parser robuste et testé ✓ |
| **Logs descriptifs** | Bons chemins d'exécution tracés ✓ |

---

## 🚫 VERDICT FINAL

### **RECOMMANDATION: ❌ NE PAS MERGER**

**Critères de bloquage**:
1. ⛔ Fichier source incomplet (ligne 747 tronquée)
2. ⛔ Complexité cyclomatique > 40 (testabilité = 0)
3. ⛔ Gestion d'erreur silencieuse (résultats dégradés non signalés)
4. ⛔ Vulnérabilité XSS identifiée

**Actions requises AVANT merge**:
1. ✅ Terminer le fichier (ligne 747)
2. ✅ Refactorer CapsuleVideoTask.execute() en 5-6 méthodes
3. ✅ Ajouter timeouts aux Futures
4. ✅ Sécuriser injection HTML
5. ✅ Réduire duplication CapsuleConfigMerger
6. ✅ Tests d'intégration pour scénarios d'erreur

**Effort estimé**: 2-3 jours de refactoring sérieux

---