= Session 135 — RENAME-4 : neutralisation vocabulaire métier formation (FPA + AFNOR/REAC + SPD/SPG)
:date: 2026-09-08
:source: Session 135 (RENAME-4, baby-step 4e de l'EPIC RENAME)
:type: refactor+tests
:borough: Détroit — capsule-gradle (N2, capsules vidéo reveal.js + TTS)
:status: TERMINÉE
:cadre: EPIC RENAME — voir `.agents/CADRAGE_EPIC_RENAME.adoc` (S-215 globale)

== Contexte hérité

Session 215 (globale, 2026-09-07) — Cadrage EPIC RENAME : neutralisation du
nommage métier privé (FPA→content, CDA→coding) dans les 7 boroughs publics.
Rationale pilote : FPA est un fond de commerce (talaria.school), il n'a pas
vocation à vivre dans le nommage public Apache 2.0. Baby-steps actés :
RENAME-1 codebase ✅ (S-212, bump 0.0.12), RENAME-2 document, RENAME-3
bakery, RENAME-4 capsule (cette session), RENAME-5 plantuml, RENAME-6
graphify/codex gouvernance.

== Mission Session 135 — RENAME-4 (1 pt)

Neutraliser les fixtures de test mentionnant « FPA » — zéro changement
sémantique, pur renommage (gate cadrage : check VERT baseline exacte +
`grep -ri fpa src/` = zéro match hors faux positifs).

=== Audit réel (écart cadrage)

Le cadrage annonçait 4 fichiers ; le grep réel en trouve **9**
(8 Kotlin + 1 feature Gherkin, 11 occurrences) :

- `src/test/kotlin/capsule/context/GlossaryLoaderTest.kt` (fixture `= Glossaire FPA`)
- `src/test/kotlin/capsule/context/CapsuleScenarioLoaderTest.kt` (fixture
  `= Bienvenue dans la Formation FPA` × 2 : titre + attribut `:title:`)
- `src/test/kotlin/capsule/scenarios/CapsuleDocContextSteps.kt` (fixture
  `AFNOR grading rules for FPA formation.`)
- `src/test/kotlin/capsule/scenarios/CapsuleScenarioContextSteps.kt` (fixture
  `= Bienvenue dans la Formation FPA`)
- `src/test/kotlin/capsule/scenarios/CapsuleGlossaryContextSteps.kt`
  (fixtures × 2 : `= Formation FPA — Glossaire AFNOR/REAC` +
  `= Formation FPA — Document without glossary`)
- `src/test/features/capsule_doc_context.feature` (2 assertions Gherkin
  appariées `AFNOR grading rules for FPA` — scénarios 2 et 3)
- `src/functionalTest/kotlin/capsule/context/DocContextFunctionalTest.kt`
  (fixture appariée à la feature)
- `src/functionalTest/kotlin/capsule/context/ScenarioContextFunctionalTest.kt`
  (fixture + assertion appariée `Session: Bienvenue dans la Formation FPA`)
- `src/functionalTest/kotlin/capsule/context/GlossaryContextFunctionalTest.kt`
  (fixture `= Formation FPA — Glossaire AFNOR/REAC`)

=== Décisions

- D1 : terme de substitution « Content » (aligné RENAME-1 codebase
  S-212 : `Domain.CONTENT`) — fixtures `Formation Content`,
  `Glossaire Content`, `AFNOR grading rules for content formation.`
- D2 : références publiques AFNOR **conservées** (même règle que
  qualiopi/rncp/vae chez codebase — références publiques françaises,
  pas IP du pilote).
- D3 : assertions Gherkin/functional **appariées** aux fixtures (le step
  doc context écrit la fixture que la feature affirme — renommage
  cohérent des deux côtés de la paire).
- D4 : zéro code production touché — `VideoDestinationResolver.kt` match
  grep = faux positif `ofPattern` (`ofPa`), explicitement accepté par le
  cadrage (même famille que `ggufPa`/`ofPa`).

== Déroulement (baby-step, 4 vagues)

. *Unit* : GlossaryLoaderTest (1) + CapsuleScenarioLoaderTest (2).
. *Steps Cucumber* : CapsuleDocContextSteps (1) +
  CapsuleScenarioContextSteps (1) + CapsuleGlossaryContextSteps (2).
. *Feature Gherkin* : capsule_doc_context.feature (2 assertions).
. *Functional* : DocContextFunctionalTest (1) +
  ScenarioContextFunctionalTest (2 : fixture + assertion) +
  GlossaryContextFunctionalTest (1).

== Résultats

- ✅ Gate grep cadrage : `grep -ri fpa src/` = **1 match** (faux positif
  `ofPattern` accepté) — zéro occurrence métier.
- ✅ Suites ciblées : `test` (2 classes) VERT, `cucumberTestDocContext` +
  `cucumberTestScenarioContext` + `cucumberTestGlossaryContext` VERT,
  `functionalTest` VERT.
- ✅ Gate check complet : `./gradlew check` BUILD SUCCESSFUL 13m16s.
- ✅ Baseline exacte vérifiée par stash A/B : **1166 unit, 0 skipped,
  0 FAIL** avant ET après (la baseline gouvernance « 1172 » était
  approximative ; 1166 est la valeur réelle). 75 functional VERT.
- ✅ Zéro fichier production modifié (9 fichiers test only).

== Extension pilote (même session) — AFNOR/REAC/SPD/SPG

Le pilote a arbitré : AFNOR/REAC/SPG/SPD sont **aussi** des termes propres
à la formation (métier) — ils doivent être renommés. Extension exécutée
immédiatement, **16 fichiers au total** (5 prod + 11 test) :

=== Code production (5 fichiers)

- `CapsuleScenarioLoader.kt` : validation `type != "SPD"` → `type != "content"`
  (ligne 58, enveloppe K-2) + KDoc (`SPD-compliant` → content type) —
  **breaking change sémantique assumé** : l'enveloppe K-2 `metadata.json`
  produite par le pipeline A2 (planner, dormant depuis S-070) devra
  produire `type:"content"`.
- `CapsuleScenario.kt` : KDoc `type == "SPD"` → `type == "content"`.
- `ContextConfig.kt` : KDoc exemple globs `docs/afnor/**` →
  `docs/referential/**` + glossaire "AFNOR/REAC referential" →
  "training referential" + tags CAP-SPD→CAP-SCENARIO.
- `GlossaryEntry.kt` : KDoc "AFNOR/REAC vocabulary" → "training vocabulary".
- 5 fichiers comments CAP-SPD-2/3 → CAP-SCENARIO-2/3 (CapsuleContext,
  CapsuleContextBuilder, CollectCapsuleAugmentedContextTask,
  ContextProvenance, CapsuleManager, CapsuleModels, CapsulePlugin).

=== Décision sémantique type de contenu

|===
| Ancien (métier) | Nouveau (générique) | Rôle
| "SPD" | "content" | type de scénario pédagogique accepté (enveloppe K-2)
| "SPG" | "syllabus" | type rejeté dans le test de graceful skip
|===

Le label rendu `==== Pedagogical Scenario (scenario)` (S-095) était déjà
générique — inchangé.

=== Tests (11 fichiers)

- `DocContextLoaderTest` : fixtures `afnor-guide`/`reac-module` →
  `referential-guide`/`curriculum-module` (assertions appariées).
- `CapsuleScenarioLoaderTest` : `type:"SPD"` → `"content"`,
  `type:"SPG"` → `"syllabus"`, fixtures `spg.adoc`/`spd.adoc` →
  `syllabus.adoc`/`scenario.adoc`, titre KDoc `(spd)` → `(scenario)`.
- `CapsuleConfigMergerTest` : 14 globs `docs/afnor`/`docs/reac` →
  `docs/referential`/`docs/curriculum`.
- Steps + features : `AFNOR grading rules` → `Referential grading rules`
  (fixtures + assertions Gherkin appariées), `Glossaire AFNOR/REAC` →
  `Glossaire référentiel`, `type:"SPD"` → `"content"` (step + functional).
- 2 tags CAP-SPD (CapsuleContextBuilderTest, DefaultCapsulePromptBuilderTest).

=== Gate extension

- `grep -riE "\bafnor\b|\breac\b|\bspg\b|\bspd\b|\bfpa\b" src/` = **zéro match**.
- Suites ciblées VERT : `test` (context + merger + prompt builder),
  3 cucumber dédiés, `functionalTest` (3m19s).
- `./gradlew check` BUILD SUCCESSFUL **13m54s** — **1166 unit 0 FAIL +
  75 functional 0 FAIL** (baseline exacte préservée).
- Le changement sémantique `type:"content"` est couvert :
  CapsuleScenarioLoaderTest (unit) + CapsuleScenarioContextSteps +
  ScenarioContextFunctionalTest (acceptance).

=== Correction incident sed

Un `sed` a corrompu 2 lignes (`:module:` attrapé par le motif
`spd.adoc`). Corrigé immédiatement : `= Scenario\n:module: core`.
Compilation vérifiée après correction (`compileTestKotlin` VERT).

== Résultats consolidés S-135

- 16 fichiers modifiés (5 prod + 11 test), zéro régression.
- Gate cadrage étendue VERT : grep 5 termes = 0, check VERT.
- Breaking change prod documenté (BACKLOG Points d'attention) :
  enveloppe K-2 `type:"content"` — à propager cross-borough (planner)
  quand le pipeline A2 sera réactivé.
- Gouvernance synchronisée (BACKLOG, TODO, INDEX, SESSIONS_HISTORY,
  archive, PROMPT_REPRISE).

== Gouvernance synchronisée

- BACKLOG : RENAME-4 ajouté (✅ TERMINÉ S-135).
- INDEX : dernière session 135, prochaine = maintenance OU RENAME-5
  plantuml (cross-borough).
- SESSIONS_HISTORY : entrée S-135.
- TODO.adoc : item RENAME-4 coché.
- PROMPT_REPRISE : mission S-136.
- Archive : COMPLETED_TASKS_ARCHIVE_2026-08.adoc (entrée S-135).