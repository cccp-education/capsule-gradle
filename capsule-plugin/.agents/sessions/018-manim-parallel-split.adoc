= Session 018 — ManimEngine + Parallel Capture + Split CapsuleManager.kt

_Date_ : 2026-06-02
_Type_ : dev
_Score_ : 9/10

== Objectifs

3 livrables depuis PROMPT_REPRISE session 017 :
. ManimEngine.kt — Interface + impl + NoOp, TDD
. Parallel capture — captureSlideParallel() + concatWebmFiles(), TDD
. Split CapsuleManager.kt — 1 fichier = 1 top-level class

== Livrés

=== 1. ManimEngine (6/6 tests TDD RED→GREEN)

Nouveau fichier `ManimEngine.kt` (102 lignes) :
- `interface ManimEngine` : render(sceneName, scriptPath, outputDir), isAvailable(), name(), probeDuration(videoFile)
- `ManimEngineImpl` : ProcessBuilder `manim -q{l|m|h} <script.py> <SceneName>`, sortie `{quality}p60/<Scene>.mp4`, probeDuration via ffprobe
- `NoOpManimEngine` : placeholder `# MANIM PLACEHOLDER`, probeDuration=0.0
- `ManimException` : RuntimeException

Pattern fidèle à TtsManager.kt (interface → impl → noop → exception).

=== 2. Parallel Capture (4-6 tests TDD)

Ajouté à `CapsuleVideoTask.kt` :
- `captureSlideParallel()` (internal) : pool fixe 4 threads, `captureFactory` injectable pour mocks, close() dans finally
- `concatWebmFiles()` (companion @JvmStatic) : FFmpeg concat demuxer, échappe apostrophes, cleanup safe (ne supprime les sources qu'en cas de succès)

Alternative à la capture séquentielle — `execute()` inchangé, branchement futur via DSL.

=== 3. Split CapsuleManager.kt (refactoring safe)

| Avant | Après |
| 1 fichier, 957 lignes, 9 classes | 8 fichiers, convention 1 classe = 1 fichier |

| CapsuleManager.kt | 195 lignes | orchestrateur + companion (parseScript, readScriptFiles, resolveScriptDir, resolveDeckDir) |
| CapsuleVideoTask.kt | 423 lignes | + captureSlideParallel + concatWebmFiles |
| CapsuleDistribTask.kt | 123 lignes | |
| CapsuleBuildTask.kt | 130 lignes | |
| CapsuleCompositeContextTask.kt | 93 lignes | |
| CapsuleScriptTask.kt | 46 lignes | |
| CapsuleParseContextTask.kt | 57 lignes | |
| ManimEngine.kt | 102 lignes | nouveau |

== Scoreboard

| Métrique | Avant (session 017) | Après (session 018) |
| Tests unitaires | 37 | 56 (+19) |
| Tests fonctionnels | 8 | 8 |
| Tests Cucumber | 9 | 9 |
| **Total** | **54** | **73** |
| Fichiers source | 9 | 13 (+4 nouveaux fichiers extraits) |
| CapsuleManager.kt | 957 lignes | 195 lignes (-80%) |

== Méthodologie

TDD baby-step (RED→GREEN→REFACTOR) sur ManimEngine + Parallel Capture.
Refactoring baby-step sur le split (1 extraction à la fois, compileKotlin à chaque étape).

== Différés

- **createSingleSlideHtml()** — Fragile (dépend du DOM reveal.js), à évaluer séparément
- **Amélioration Cucumber [manim:SceneName]** — Skip conditionnel ou mock, P3

== État git

Non commité (règle : aucun commit sans permission).
Nouveaux fichiers : CapsuleBuildTask.kt, CapsuleCompositeContextTask.kt, CapsuleDistribTask.kt, CapsuleParseContextTask.kt, CapsuleScriptTask.kt, CapsuleVideoTask.kt, ManimEngine.kt, ManimEngineTest.kt
Modifiés : CapsuleManager.kt (957→195), CapsulePluginTest.kt (+ParallelCaptureTest)

== Remote fork lion92

Toujours en remote, 2 commits clés non mergés. Aucun nouveau cherry-pick cette session.