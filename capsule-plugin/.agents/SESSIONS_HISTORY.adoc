= SESSIONS_HISTORY — capsule-gradle

_Date creation_ : 2026-05-14

== Sessions

|===
| # | Date | Type | Sujet | Score
| 093 | 2026-08-11 | feat+ddd+tdd+bdd | **CAP-DOCCONTEXT injection corpus documentaire (EPIC TERMINÉ 5/5 US)** — `ContextConfig` data class (`docsGlobs: List<String> = emptyList()`) 8ème section `CapsuleConfig`. `CapsuleConfigMerger.mergeContextConfig` + nouveau helper `mergeStrList` (List<String> comma-separated, 4 sources ENV < props < YAML < CLI) + `splitCommaList` utilitaire. `loadFromEnvironment` (`CAPSULE_CONTEXT_DOCS_GLOBS`) + `buildConfigFromProperties` (`capsule.context.docsGlobs`). `CapsuleExtension.docsGlobs: ListProperty<String>`. `pushConfigIntoExtension` push context. `DocContextLoader` (object pur, domaine `capsule.context`) — résout fichiers pré-matchés, concatène trié par nom `--- filename ---\n<content>`, tronque via `ContextChannel.Docs.truncateToTokens(budget.docsTokens)` (10% = 800 tokens/8000, pas 40% qui est EAGER — correction S-093). `CollectCapsuleAugmentedContextTask` étendu : `docsFiles` (`@InputFiles @PathSensitive(RELATIVE)`) + `docsGlobs` (`@Input`) + `resolveDocsSection` (globs > legacy CLI string rétrocompat). `CapsuleManager` lazy `project.provider { }` (extension afterEvaluate > CLI) + `resolveGlobFiles` (`fileTree().matching { include(glob) }`). 4 scénarios cucumber `@context @docs` (`capsule_doc_context.feature`) + runner dédié `CapsuleDocContextCucumberRunner` (pattern S-082) + steps préfixés "doc context" (bug S-088). `CucumberTaskSpec("cucumberTestDocContext")` câblé `build.gradle.kts`. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 694 unit VERT (+20 : 11 ContextConfig merger + 9 DocContextLoader) → 47 functional VERT (+3 DocContextFunctionalTest : docsContent CLI rétrocompat, docsGlobs résolus, empty no-op) → 4 cucumber scénarios GREEN. Zéro régression, zéro secret. | 9/10
| 092 | 2026-08-10 | cadrage+gouvernance | **Cadrage 11 EPICs neufs (contexte augmenté + post-production)** — Audit post-CAP-ARCH/CAP-CR3-2. 11 EPICs neufs (47 US, ~88 pts) sur 2 axes : contexte augmenté (5 EPICs : CAP-DOCCONTEXT injection corpus documentaire `context.docsGlobs` 4 sources + `DocContextLoader` ~12 pts P0, CAP-SPD consommation métadonnées SPD `SpdContextLoader` parse metadata.json K-2 + canal `spd` dans `CapsulePipelineGraph` ~10 pts P0, CAP-GLOSSARY injection terminologique `context.glossaryFile` + canal `glossary` ~6 pts P1, CAP-PROVENANCE audit context anti-hallucination `ProvenanceTracker` → `context-provenance.json` ~5 pts P1, CAP-TRANSCRIPT reverse pipeline capsule → article AsciiDoc domaine `capsule.transcript` cross-borough document/bakery ~10 pts P2) + post-production (5 EPICs + CAP-CR3-3 : CAP-CR3-3 branchement ScreenshotCapture `capture.strategy: playwright|screenshot` 4 sources + `ScreenshotCaptureImpl` étendu multi-slide + `resolveCapture` factory + StrictModeGuard ~13 pts P0, CAP-MP4 export MP4/H.264 `output.format: webm|mp4|both` + `VideoFormatConverter` ~4 pts P1, CAP-AUDIO post-production audio BGM + loudnorm EBU R128 + sidechain ducking domaine `capsule.audio` ~10 pts P1, CAP-CHAPITRE intro/outro + chapter markers Matroska domaines `capsule.cards`/`capsule.chapters` ~10 pts P2, CAP-PODCAST extraction audio-only `PodcastConcatenator` FFmpeg concat MP3 ~4 pts P2, CAP-PREVIEW mode dry-run `capture.previewOnly` PNGs sans mux ~4 pts P2). Décision architecturale CAP-CR3-3 : brancher ScreenshotCapture (non expulser) — avantages fonctionnels (CI-friendly CAP-CR3-1, 10x plus rapide decks statiques, empreinte CI légère, cohérent chemin Manim, artifact preview/debug). Séquence MVP0 : DOCCONTEXT > SPD > CR3-3 > MP4 > AUDIO > GLOSSARY > PROVENANCE > CHAPITRE > TRANSCRIPT > PODCAST > PREVIEW. MàJ BACKLOG (11 sections EPIC + synthèse + séquence), INDEX (roadmap 12 lignes + correction faux positif CAP-29.6 `⬜ TODO` → `✅ TERMINÉ S-082`), PROMPT_REPRISE (session 092 + contexte reprise 093 CAP-DOCCONTEXT). Zéro code modifié, `compileKotlin` VERT. | 9/10
| 091 | 2026-08-10 | feat+ddd+tdd+bdd | **CAP-CR3-2 Mode strict anti-NoOp (EPIC CAP-CR3-2 TERMINÉ 4/4 US)** — `capsule.strictMode` DSL (Boolean, défaut `false`, 4 sources ENV < props < YAML < CLI). `StrictModeGuard` (object pur, domaine `capsule.config`) — `requireAvailable(strict, engineName, isAvailable, path)` lève `IllegalStateException` avec message actionable si `strict && !isAvailable` (nomme engine + path + suggestion `strictMode=false` ou `install <tool>`). 6 `resolve*` branchés : `CapsuleBuildTask.resolveTtsEngine` (piper/espeak absent → guard avant NoOp), `CapsuleVideoTask.resolveTtsEngine` (piper/espeak), `CapsuleVideoTask.resolvePlaywrightCapture` (Playwright), `CapsuleManager.resolveManimEngine` (manim, param `strict`), `CapsuleManager.resolveManimVideoMixer` (ffmpeg, param `strict`), `CapsuleManager.resolveSubtitleBurnInService` (ffmpeg, param `strict`). 3 appelants CapsuleVideoTask passent `strict = capsuleExtension.strictMode.get()`. `noop` explicite (`ttsEngine = "noop"`, `ffmpegPath = "noop"`) reste légitime pour tests, non concerné. `StrictModeConfig` data class (1 champ `enabled: Boolean = false`). `CapsuleConfig` étendu `strictMode: StrictModeConfig`. `CapsuleConfigMerger.mergeStrictModeConfig` (utilise `mergeBoolean`) + `loadFromEnvironment` (`CAPSULE_STRICT_MODE_ENABLED`) + `buildConfigFromProperties` (`capsule.strictMode.enabled`). `CapsuleExtension.strictMode: Property<Boolean>` (convention `false`). `CapsulePlugin.pushConfigIntoExtension` push strictMode. Baby-step TDD/BDD 3 niveaux : RED → GREEN → REFACTOR (unit → functional → cucumber). 674 unit VERT (+19 : 7 `StrictModeGuardTest` + 3 `StrictModeConfigTest` + 9 strictMode merger) → 44 functional VERT (+4 `StrictModeFunctionalTest` : default NoOp preserved, strict + piper absent → FAIL, strict + noop explicite → OK, CLI flag strict → FAIL) → 8 cucumber dédiés GREEN (`capsule_strict_mode.feature` 4 scénarios `@strict @config`, runner `CapsuleStrictModeCucumberRunner` pattern S-082, steps préfixées "strict" pour éviter doublons glue, `writeBuildFile()` privé regénère build.gradle complet au lieu d'appendText double bloc). `CucumberTaskSpec("cucumberTestStrictMode")` câblé `build.gradle.kts`. `check` BUILD SUCCESSFUL 6m54s. Zéro régression, zéro secret. | 9/10
| 090 | 2026-08-10 | feat+bdd+cucumber | **CAP-ARCH-6 Cucumber BDD boundary cross-borough (EPIC CAP-ARCH TERMINÉ 8/8 US)** — `capsule_arch_boundary.feature` (4 scénarios `@cross-borough @architecture @i18n @ai`) single-language français (pas 10 langues — CAP-29.5 `multi_language_capsule.feature` garde la responsabilité i18n, éviter test redondant, Single Responsibility). Scénarios : registration task `generateCapsuleContentAndVideos` groupe `capsule`, content pipeline LLM (`generateCapsuleContent` mock Ollama 2 réponses alternées propose/generate → `demo-speaker-notes.adoc` + `demo-script.txt`), video pipeline composite (NoOp engines → `demo_fr.webm`), économie d'encre skip WebM pré-existant valide (ffmpeg 1s, `ffprobe` > 0). Runner dédié `CapsuleArchBoundaryCucumberRunner` (pattern S-082 `@SelectClasspathResource` + filter `@cross-borough and not @wip and not @integration`). `CapsuleArchBoundarySteps` self-contained GradleRunner (fake `translateDeck`, pré-génération artefacts fr `demo_fr-deck.html` + `demo_fr-script.txt` dans dirs attendus). `CucumberTaskSpec("cucumberTestArchBoundary")` câblé `build.gradle.kts`. Baby-step TDD/BDD 3 niveaux : RED (8 tests failed steps undefined) → GREEN (4 scénarios PASSED ; itérations : doublons glue `DuplicateStepDefinitionException` avec `MultiLanguageCapsuleSteps`/`CapsuleAiSteps` → steps préfixées "capsule arch boundary", skip `MINIMAL_WEBM` header EBML `ffprobe` 0.0 → ffmpeg WebM valide 1s, feature `demo_fr-speaker-notes.adoc` corrigé en `demo-speaker-notes.adoc` car content pipeline mono-langue sans `_lang`) → REFACTOR (`check` BUILD SUCCESSFUL 6m12s). 655 unit VERT (inchangé) + 40 functional VERT (inchangé) + 4 cucumber dédiés GREEN. Zéro régression, zéro secret. EPIC CAP-ARCH TERMINÉ 8/8 US. | 9/10
| 089 | 2026-08-10 | feat+ddd+tdd+i18n | **CAP-ARCH-5 i18n aligné socle (CapsuleMessages)** — Domaine `capsule.i18n` créé (8ème) : `CapsuleMessages` object (pattern `SliderMessages`/`PlantumlMessages` — `utf8Control` ResourceBundle.Control UTF-8, `forLanguage`/`get`/`format`/`resolveLanguage(project)` cascade `-Pcapsule.language` > DSL `capsule { language }` > default `"en"`). 11 `Messages*.properties` (base + en + 9 langues fr/zh/hi/es/ar/bn/pt/ru/ur) — 23 keys (6 task groups + 17 task descriptions). Wiring `CapsuleManager` (13 tasks) + `CapsuleFeedTaskRegistrar` (4 tasks) → `CapsuleMessages.get("key", lang)`. Fix 3 descriptions françaises hardcoded (deployCapsule "Recadre les capsules...", collectCapsuleContext "Exporte le contexte...", transformCapsuleContext "Parse le fichier..." — violation règle English-only). `CapsuleExtension.language` ajouté (default `"en"`). `MultiLanguageResolver` KDoc corrigé (remove `DocumentI18nBridge` ref supprimé S-030, cite `CapsuleMessages` + `SliderMessages`/`PlantumlMessages`). Décisions : pas de `CapsuleI18nBridge` (anti-pattern S-030), pas de `MessageBundle`/`LocaleResolver` (contrats N0 morts zéro consommateur), `Messages_en.properties` explicite requis (fallback default locale système `fr_FR`). Bug découvert : `Locale.ENGLISH` sans fichier `_en` explicite chargeait `Messages_fr.properties`. Baby-step TDD : 655 unit VERT (+87 net : 65 `CapsuleMessagesTest` 23 keys × 10 langs + fallback + missing + 22 `MessagesFrTranslationTest` assertNotEquals). 40 functional VERT (inchangé). `compileKotlin` + `test` + `functionalTest` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 088 | 2026-08-10 | feat+bdd+wiring | **CAP-ARCH-4 Wiring backend + CAP-ARCH-7 US-4 Cucumber distrib** — CAP-ARCH-4 : tâche composite `generateCapsuleContentAndVideos` (groupe `capsule`) chaîne `generateCapsuleContent` → `translateAndGenerateCapsuleVideos` (pattern `DefaultTask` + `dependsOn` par nom string, backend existant intact). `CapsuleFeedTaskDomain.kt` constante `GENERATE_CAPSULE_CONTENT_AND_VIDEOS` + `CONTENT_AND_VIDEOS_DESCRIPTION`. `CapsuleFeedTaskRegistrar.kt` `registerGenerateCapsuleContentAndVideos(project)`. `CapsuleManager.kt` câblage. 3 functional tests (`GenerateCapsuleContentAndVideosFunctionalTest` : registration, description, dependsOn chain `--dry-run` avec dummy deck car `generateCapsuleContent` résout `deckFile` par convention). 3 cucumber `@architecture @wiring` (`capsule_content_and_videos.feature` + `CapsuleContentAndVideosSteps` constants pures + `CapsuleContentAndVideosCucumberRunner` dédié). CAP-ARCH-7 US-4 : feature `capsule_distrib.feature` (5 scénarios `@distrib @versioning`) + `CapsuleDistribSteps` (self-contained GradleRunner pattern `MultiLanguageCapsuleSteps`, steps uniques "distrib" pour éviter doublons `CapsuleStepDefinitions`) + `CapsuleDistribCucumberRunner`. Scénarios : registration tasks --group distribute, copy timestamp, copy incremental v1, second run v2, skip idempotent économie d'encre. 2 `CucumberTaskSpec` câblés (`cucumberTestContentAndVideos` + `cucumberTestDistrib`). 568 unit VERT (inchangé) + 40 functional VERT (+3) + 8 cucumber dédiés GREEN (3 `@architecture @wiring` + 5 `@distrib @versioning`). `check` BUILD SUCCESSFUL. Zéro régression. | 9/10
| 087 | 2026-08-10 | review+fix+tdd+feat | **Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (US-1→3)** — Code review post-S-083→086 : 4 points (duplication pipeline koog = dette extraction MEMPHIS future, versionning codebase-contracts 0.1.0 = régularisation, hack buildscript force annotations = acceptable pattern slider, **1 bug réel** = FAIL `@integration` burn-in path mismatch `build/capsule/` vs `outputDir=capsules` connu S-069/S-082 jamais traité). Fix bug TDD : `CapsuleOutputDirResolver` (object pur, package `capsule.support`, résout `outputDir` depuis `build.gradle` du projet Cucumber) + 9 unit tests RED→GREEN + 3 steps Cucumber migrés (`aSubtitleFileIsGeneratedInTheCapsuleOutputDirectory`, `theVideoFileHasAValidWebmEbmlHeader`, `aVideoFileIsGenerated` vers `capsuleOutputDir()` dynamique) + runner `cucumberTestIntegration` câblé (`CucumberTaskSpec` dédié, runner `CucumberIntegrationTestRunner` orphelin exécuté). **BUILD SUCCESSFUL 3m39s** — scénario E2E burn-in `@burnin @integration` VERT (était FAIL depuis S-069). CAP-ARCH-7 (nouvelle feature issue review) : US-1 `OutputConfig` + `VersioningStrategy` enum (TIMESTAMP|INCREMENTAL) étend `CapsuleConfig` section `output` (3 champs `videoDestinationDir` défaut `office/videos`/`versioning`/`versionPrefix`), `CapsuleConfigMerger` 4 sources + `mergeOutputConfig` + `mergeVersioning` + `loadFromEnvironment`/`buildConfigFromProperties` étendus, 10 unit tests. US-2 `VideoDestinationResolver` (object pur main, `resolveVersionLabel` timestamp ISO-8601 compact ou incremental depuis `collectExistingVersions`, `resolveDestinationDir`, `resolveDestinationFile`, `collectExistingVersions`), 10 unit tests. US-3 `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`, groupe `distribute`, copie WebM de `build/<outputDir>/` vers `<videoDestinationDir>/<version>/`, CLI `-Poutput.*` overrides config mergée, économie d'encre skip si destination existe et non-vide, log `CAPSULE DISTRIB → …`) + wiring `CapsuleManager.registerDistributeCapsuleVideoTask()`, 3 functional tests (registration, copy versionné, skip idempotent). 568 unit VERT (+28 net) + 37 functional VERT (+3). `./gradlew test` BUILD SUCCESSFUL (2m7s). Zéro régression, zéro secret. | 9/10
| 086 | 2026-08-09 | feat+ddd+tdd | **CAP-ARCH-3 Koog pipeline génération contenu (domaine `capsule.pipeline`)** — `CapsulePipelineGraph` (koog 1.0.0 consommé direct via BOM, pattern `DeckPipelineGraph` slider SLD-8.3) : `strategy<CapsuleState, CapsuleState>` 3 nœuds propose-context → validate-context → generate-speaker-notes + edges conditionnels (propose FAILED → stop, validate INVALID → FAILED), `execute()` séquentiel (pattern `codebase.KoogAugmentedContextGraph`), `asMermaidDiagram()`. Domaine pur 9 fichiers : `CapsuleState` (stage machine `CapsuleStage` IDLE→PROPOSE_CONTEXT→CONTEXT_VALIDATED→CONTENT_GENERATED/FAILED + invariants croisés fail-fast `init`), `CapsulePromptBuilder`/`DefaultCapsulePromptBuilder` (ports pédagogiques : propose = plan JSON strict deck+contexte, generate = speaker notes enrichies respectant contrat `capsule.feed` `[NOTE.speaker]`), `CapsuleLlm`/`ChatModelCapsuleLlm` (port suspend + adapter langchain4j ChatModel), `ContentPlanValidator`/`ContentPlanValidationResult` (8 checks ordonnés : champs requis, types, langue ISO), `TtsScriptDeriver` (dérive `*-script.txt` contrat feed). 49 unit RED→GREEN. Tâche `generateCapsuleContent` (groupe `generate`) : deck `-Pdeck.file` ou premier `.adoc` de `slides/misc`, `@OutputFile` speakerNotes + ttsScript (économie d'encre UP-TO-DATE), `@ServiceReference LlmBuildService`, mock `-Pollama.baseUrl`. Wiring `CapsuleManager`. Durcissement API. 3 functional VERT. README master EN mis à jour. `./gradlew check` BUILD SUCCESSFUL (3m39s). Zéro régression. | 9/10
| 085 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`)** — `CapsuleContextBuilder` (object pur : `defaultBudget()` `ChannelBudget` 8000 40/30/20/10/0, `toChannels(composite)` 5 canaux EAGER→RAG→GRAPHIFY→DOCS→RESOURCE, `channelsWithBudget(channels, budget)` troncature + append `\n`, `merge(channels)` sections `==== <sectionHeader> (<name>)` canaux vides ignorés, `build(composite, budget)` orchestration) assemble le `CompositeContext` (codebase-contracts 0.1.0, bump catalogue 0.0.2→0.1.0 + `implementation(libs.codebase.contracts)` — non transitif en compile, absent BOM 0.0.16). `CapsuleContext` (data class 2 champs, invariant fail-fast `rendered.isBlank() == channels.isEmpty()`, `isEmpty`/`nonEmptyCount`/`tokenEstimate`). Tâche `collectCapsuleAugmentedContext` (groupe `collect`) : eagerFiles `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc` `@InputFiles @PathSensitive(PathSensitivity.RELATIVE)` (fixtures chemin absolu functional), `-Pcontext.{ragContent,graphifyContent,docsContent,tokenBudget=8000}`, output `build/capsule/augmented-context.txt` + log `CAPSULE CONTEXT → …`, no-op si contexte vide. Wiring `CapsuleManager.registerCollectAugmentedContextTask()`. Cucumber `capsule_context.feature` `@context @rag` : runner dédié `CapsuleContextCucumberRunner` (contourne bug S-082 — `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner écrase la system property cucumber.features) + `cucumberConventions.additionalTasks` `CucumberTaskSpec("cucumberTestContext", ..., runnerClass)` (`import build.CucumberTaskSpec`). Baby-step TDD/BDD 3 niveaux : 491 unit VERT (+14 `CapsuleContextBuilderTest`) → 31 functional VERT (+3 `CapsuleAugmentedContextFunctionalTest` : registration, artefact EAGER+RAG+Graphify écrit, second run UP-TO-DATE économie d'encre) → cucumber 4/4 GREEN (~31s). `./gradlew check` BUILD SUCCESSFUL (3m33s : test + functionalTest + kover + validatePlugins ; cucumberTest SKIPPED par défaut — CR-10). Zéro régression, zéro secret. | 9/10
| 084 | 2026-08-09 | feat+ddd+tdd+bdd | **CAP-ARCH-1 Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`)** — codebase plugin `implementation` 0.0.5 + koog-agents 1.0.0 (bump 0.8.0 mort, `exclude(org.jetbrains:annotations)` + buildscript `force annotations:26.0.2-1` pattern slider) + langchain4j / langchain4j-ollama (BOM 0.0.16). Domaine `capsule.ai` : `CapsuleLlmService` (object pur — `registerLlmBuildService(project)` = `gradle.sharedServices.registerIfAbsent("capsuleLlmService", LlmBuildService)` + `spec.parameters.model.convention(project.aiProvider)` ; `resolveModel(provider, serviceProvider): ChatModel` via `LlmProviderChatModelAdapter` ; `aiProvider(project)` `-Pai.provider` default `ollama`). Tâche `capsuleAiSmokeTest` (groupe `generate`, `usesService(llmServiceProvider)`, prompt minimal + log `CAPSULE AI → …` + fail si réponse non conforme, mock `-Pollama.baseUrl` sinon pool rotation) câblée dans `CapsuleManager` (+12 lignes). Baby-step TDD/BDD 3 niveaux : 477 unit VERT (+10 net : `CapsuleLlmServiceTest` + `LlmProviderChatModelAdapterTest`) → 28 functional VERT (+3 `CapsuleAiSmokeTestFunctionalTest` : registration, mock HTTP, unreachable fail) → cucumber `capsule_ai.feature` `@ai` 3/3 GREEN (task listée, smoke mock LLM log + réponse, fail-fast unreachable ; mock HttpServer local, zéro LLM réel). `./gradlew check` VERT. 1 FAIL `@integration` burn-in préexistant (subtitle path `build/capsule/` vs `outputDir=capsules`) hors CAP-ARCH-1, connu S-069/S-072. | 9/10
| 083 | 2026-08-09 | cadrage | **CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu (capsule LLM-driven via codebase)** — Audit fondé sur le code réel, remplace le cadrage S-073 (périmé). Constats : (1) zéro `Ollama`/`ChatModel`/`ChatLanguageModel`/`langchain4j`/`LLM` dans `src/main` → capsule n'a AUCUN appel LLM à purger ; (2) `koog-agents` `0.8.0` en `implementation` mais 0 usage dans le code → dépendance morte, à réactiver en `1.0.0` pour CAP-ARCH-3 (pattern slider : exclusion koog submodules + forcer `org.jetbrains:annotations:26.0.2-1`) ; (3) `compileOnly slider` mais 0 import de classe slider (seul `dependsOn("translateDeck")` string-based dans `CapsuleFeedTaskRegistrar`) → à re-confirmer en CAP-ARCH-4 ; (4) `i18n-contracts` 0.0.2 déjà consommé (CAP-29.1 MultiLanguageResolver/TtsManager/VoiceMapping) → l'US « migration i18n » S-073 est largement faite. Inventaire contrats N0 : `codebase-contracts` 0.1.0 (CompositeContext EAGER+RAG+Graphify+Docs, ContextChannel, ChannelBudget — la « réalité augmentée complète »), pipeline-contracts 0.0.2, agent-contracts 0.1.0. Pattern de référence inventorié : slider `AssistantManager` SLD-8 (`registerLlmBuildService()` → `LlmBuildService` codebase.koog.llm.service → `LlmProvider` fun interface suspend → `LlmProviderChatModelAdapter` → `ChatModel` langchain4j). Vision refondue : capsule LLM-driven piloté par codebase (koog orchestration graphe + langchain4j exécution + RAG pgvector + Graphify contexte augmenté), backend d'exécution existant recyclé intact (extractSpeakerNotes, TTS Piper/espeak, capture Playwright, subtitles, burn-in, multi-lang CAP-29). EPIC redécoupé : 7 US (CAP-ARCH-0 ✅, CAP-ARCH-1 socle LLM bridge `capsule.ai` → CAP-ARCH-2 contexte augmenté `capsule.context` → CAP-ARCH-3 graphe koog `CapsulePipelineGraph` `capsule.pipeline` → CAP-ARCH-4 wiring backend → CAP-ARCH-5 i18n aligné socle → CAP-ARCH-6 cucumber cross-borough, ~21 pts). Décisions : codebase plugin en `implementation` (comme slider), i18n reste via `slider.translateDeck` (déjà LLM codebase SLD-8), génération de contenu enrichi = LA nouveauté. Zéro code modifié, `compileKotlin` VERT. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY. | 9/10
| 082 | 2026-08-08 | doc+dogfooding+fix+tdd | **CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 TERMINÉ 7/7 US** — README master EN + README plugin mis à jour (domaine `capsule.multilang`, diagramme pipeline, table des tâches `extractSpeakerNotes`/`translateAndExtractSpeakerNotes`/`generateCapsuleVideoAllLanguages`/`translateAndGenerateCapsuleVideos`, convention nommage `<deckName>_<lang>-deck.html|script.txt|webm`, totaux tests 464 unit + 25 functional + 6 cucumber multilang). Dogfooding réel consommateur composite `includeBuild` (`/tmp/opencode/dogfood-multilang`) : `extractSpeakerNotes` réel (3 scripts fr/en/ar) → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium Playwright) → 3 WebM générés en 3m07 (ar 2MB / en 4.7MB / fr 4.3MB). Défaut nommage découvert : le rendu produisait `<deckName>_<lang>-deck.webm` (dérivé du header script `=== CAPSULE SCRIPT : <deckName>_<lang>-deck ===`, flux réel slider SLD-11) au lieu de la convention du plan `<deckName>_<lang>.webm` (`entry.outputVideo`), ce qui brisait aussi l'économie d'encre (le probe `shouldRender` ne matchait jamais l'output existant → re-render systématique). Fix TDD baby-step `CapsuleVideoOutputRenamer` (domaine `capsule.multilang`, object pur, 4 unit RED→GREEN) aligne le WebM rendu sur `entry.outputVideo` (contrat plan), wiring post-capture dans `GenerateCapsuleVideoAllLanguagesTask.renderEntry`. Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` logué pour les 3 langues + économie d'encre prouvée (`--rerun-tasks` → `0/3 languages rendered, 3 skipped: existing valid WebM`). 464 unit VERT (+4 net : +4 `CapsuleVideoOutputRenamerTest`) + 25 functional VERT (`GenerateCapsuleVideoAllLanguagesFunctionalTest` GREEN) + 6 cucumber multilang GREEN (S-081 baseline, non régressés — 1 FAIL `@integration` burn-in préexistant subtitle path `build/capsule/` vs `outputDir=capsules`, hors CAP-29, connu S-069/S-072). EPIC CAP-29 TERMINÉ 7/7 US, domaine `capsule.multilang` clos (7 fichiers prod : MultiLanguageResolver, VoiceMapping, ResolvedLanguage, CapsuleVideoEntry, CapsuleVideoPlan, CapsuleVideoPlanner, CapsuleVideoAllLanguagesRunner, GenerateCapsuleVideoAllLanguagesTask, CapsuleVideoOutputRenamer). Prochain EPIC : CAP-ARCH (cross-borough slider+codebase, débloqué). | 9/10
| 081 | 2026-08-08 | bdd+cucumber | **CAP-29.5 Cucumber BDD E2E 10 langues** — `multi_language_capsule.feature` (6 scénarios `@multilang @i18n`) + `MultiLanguageCapsuleSteps` : helpers plan pur `CapsuleVideoPlanner` (10 langues LanguageCatalog, nommage `<deckName>_<lang>.webm`, skip langue sans paire, fail-fast plan vide) + 2 scénarios E2E GradleRunner NoOp (ttsEngine=noop, captureEnabled=false, deck source + 10 scripts par langue, assert log `CAPSULE MULTILANG → n/n languages rendered` + WebM `build/capsules/*.webm`, RTL ar/ur). `cucumberTest -PrunCucumber` : 6/6 multilang GREEN (suite 154 tests, 1 FAIL préexistant `@integration` burn-in — subtitle path `build/capsule/` vs `outputDir=capsules`, connu S-069/S-072, hors périmètre). 460 unit VERT (inchangé) + 25 functional VERT + kover VERT. `check` BUILD SUCCESSFUL. | 9/10
| 080 | 2026-08-08 | feat+wiring+tdd | **CAP-29.4 Câblage translateAndGenerateCapsuleVideos** — Tâche composite purement déclarative `translateAndGenerateCapsuleVideos` : dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`. `CapsuleFeedTaskNames.TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS` + description ajoutés (domaine `capsule.feed`), `CapsuleFeedTaskRegistrar.registerTranslateAndGenerateCapsuleVideos` (group capsule, 2 dependsOn), câblage 3ème appel registrar dans `CapsuleManager.registerExtractSpeakerNotesTask()`. Pipeline complet : deck source → translateDeck (10 langues) → extractSpeakerNotes (10 scripts) → generateCapsuleVideoAllLanguages (10 WebM). 3 functional tests RED→GREEN (registration groupe, description mentionne les 2 sous-tâches, dependsOn chain via `--dry-run` avec stub `translateDeck` dans projet isolé). 460 unit VERT (inchangé) + 25 functional (+3), cucumber SKIPPED. `check` BUILD SUCCESSFUL. | 9/10
| 079 | 2026-08-08 | feat+ddd+tdd+functional | **CAP-29.3 GenerateCapsuleVideoAllLanguagesTask** — Domaine `capsule.multilang` : `CapsuleVideoAllLanguagesRunner` (object pur, `run(plan, probeDuration, render): List<CapsuleVideoEntry>` itère le plan + `shouldRender(entry, probeDuration)` = skip si WebM existe ET probe > 0, callback render injecté — frontière domaine/adaptateur nette). Tâche `GenerateCapsuleVideoAllLanguagesTask : CapsuleVideoTask` (réutilise `synthesizeTtsForScript` + `captureDeckSequential/Parallel` + `injectAudio` + `computeSlideDurations` CR-1 ; résolution TTS par langue via `MultiLanguageResolver` : set `ttsLanguage`/`ttsVoice`/`espeakVoice`, reset `ttsEngine=null`). Refactor base `CapsuleVideoTask` zéro changement de comportement : `execute()`→`open`, `resolveTtsEngine()`/`injectAudio()`→`internal`. Enregistrement `generateCapsuleVideoAllLanguages` (groupe generate, CapsuleManager). 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN, _correction : LanguageCatalog n'a pas "de", 10 langues_) + 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` (3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`). 460 unit VERT (+6 net), functional PASS, cucumber SKIPPED. | 9/10
| 078 | 2026-08-08 | feat+ddd+tdd | **CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner** — Domaine `capsule.multilang` enrichi. `CapsuleVideoEntry` (data class immutable 4 champs deckFile/scriptFile/language/outputVideo, invariants fail-fast : deck exists, script exists, language in LanguageCatalog). `CapsuleVideoPlan` (data class immutable, invariant non-empty, size/isEmpty/languages). `CapsuleVideoPlanner` (object pur : `plan(deckDir, scriptDir, outputDir, targetLanguages)`, skip langues sans deck+script, fail-fast plan vide). Nommage aligné slider : `<deckName>_<lang>-deck.html` + `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm`. 454 unit VERT (+18 net : +10 `CapsuleVideoPlanTest` + 8 `CapsuleVideoPlannerTest`). `check` BUILD SUCCESSFUL. | 9/10
| 077 | 2026-08-07 | feat+ddd+tdd | **CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues** — Domaine `capsule.multilang` créé (7ème). `MultiLanguageResolver` (object pur) consomme `LanguageCatalog` (BOM `i18n-contracts` 0.0.2) + `VoiceMapping` étendu à 10 langues (fr/en/es/de/zh/hi/ar/bn/pt/ru/ur, 11 Piper + 11 espeak). `Language` enum supprimé (4 entrées). `ResolvedLanguage` data class (SupportedLanguage + piperModel + espeakVoice). `TtsEngine.language(): SupportedLanguage?`. `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` migrés (`Language.fromCode` → `MultiLanguageResolver.resolve`). BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés. 436 unit VERT (+15 net : +22 `MultiLanguageResolverTest`, +2 `LanguageVoiceMappingTest` adaptés, -9 anciens tests Language). `check` BUILD SUCCESSFUL. | 9/10
| 076 | 2026-08-07 | cadrage | **CAP-29.0 Cadrage EPIC pipeline multi-langue** — Audit code existant (`Language` enum 4 entrées, `VoiceMapping` 4 Piper+4 espeak, `generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes` SLD-11.4, `slider.translateDeck` 10 langues via `LanguageCatalog`). Décision clé : consommer `LanguageCatalog` (10 langues, contrats N0) au lieu d'étendre enum local — évite dette technique détruite dans CAP-ARCH-1. Domaine cible `capsule.multilang` (7ème). 7 US (CAP-29.0→6), ~20 pts. Backlog/INDEX/PROMPT_REPRISE/AGENTS racine mis à jour. Zéro code modifié. | 9/10
| 075 | 2026-08-07 | refactor+tdd+bdd | **CR-10 CI optimization cucumberTest onlyIf** — Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes) + 7 unit tests `CucumberTestGuardTest`. Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée — script ne peut pas importer classpath `main`). BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps` (5 step defs). `./gradlew check` VERT (cucumberTest SKIPPED sans flag). 421 unit VERT (+7). Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature` (à investiguer). EPIC CR TERMINÉ (10/10). | 9/10
| 074 | 2026-08-07 | refactor+tdd+bdd | **CR-9 Robust HTML parsing** — `HtmlSectionParser.findTopLevelSectionOpenTags` (38 lignes, depth-tracking existant réutilisé). `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne `HtmlSectionParser.findTopLevelSectionOpenTags`. DRY (DRY-2 S-031). Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées → `data-audio` injecté sur sous-sections. +8 unit `HtmlSectionParserTest` (flat, nested, self-closing, attributes, empty, malformed, positions, multiline) + 1 unit `CapsuleVideoTaskTest` (data-audio count = 2, pas 4) + 1 scénario cucumber `@audio @sequential` + 2 step defs. 414 unit VERT (+9). | 9/10
| 073 | 2026-08-07 | refactor+tdd+bdd+cadrage | **CR-8 Logging structuré + EPIC CAP-ARCH cadré** — `CapsulePlugin.kt:40-48` 1 ligne monolithic 13 placeholders → 4 lignes `logger.lifecycle` par section (TTS/Capture/Subtitles/Manim). `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`), `formatConfigLog(config): List<String>`. Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=) → Cucumber @config existants verts. +9 unit tests `CapsuleConfigLoggerTest`, +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs. 405 unit VERT (+9). EPIC CAP-ARCH cadré au BACKLOG (cross-borough slider+codebase pour contenu et i18n, 6 US ~19 pts, inspiration document/bakery/plantuml). | 9/10
| 072 | 2026-08-07 | refactor+tdd+bdd | **CR-5 DRY CapsuleConfigMerger** — 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` (~76 lignes dupliquées) → 5 méthodes unifiées + 4 helpers génériques (`mergeStr/mergeInt/mergeDouble/mergeBoolean`). `merge()` dispatch sur `yaml: T?` nullable. 322→302 lignes (-19 net). Sémantique préservée : `toBoolean()` lenient, heuristic `isNotBlank()` strings, Bool/Int/Double YAML>Props>ENV. +13 unit tests NoYaml (48 total ConfigMergerTest), +1 scénario cucumber `@config @no-yaml` + 1 step Given. 396 unit VERT (+12). 1 échec cucumber `Burn-in E2E @integration` préexistant (ffmpeg exit 183, non lié CR-5). | 9/10
| 071 | 2026-08-06 | feat+tdd+bdd+fix | **CR-2 + Fix Cucumber + CR-3/4/6/7** — Thread safety + timeout `captureSlideParallel` (unit 4 + functional 3 + cucumber 2). Fix 5 échecs cucumber consolidated (DuplicateStepDefinitionException doublons title/speakerNote renommés `the read segment` + feature quotes + temp file exact name). CR-3 error handling TTS (retry 1× + accumulation `failedSlides` + logger.error final). CR-4 HTML escape XSS `injectSubtitleTrack` (HtmlEscape object). CR-6 robust CLI parsing (cliInt/cliDouble/cliBoolean helpers tolèrent String). CR-7 validation YAML (déjà implémenté + test couverture). 384 unit VERT (+23), 64 cucumber 0 FAIL, 3 functional VERT. | 9/10
| 070 | 2026-08-06 | ops+cross-borough | **Sync slider 0.0.15 → 0.0.16** (cross-borough, suite slider 0.0.16 S-066 + BOM 0.0.17 S-019). Bump catalogue `capsule-plugin/gradle/libs.versions.toml` `slider = "0.0.15"` → `"0.0.16"`. `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal). Zéro code production modifié. Session pure sync consommateur. | 9/10
| 069 | 2026-08-05 | feat+ddd+tdd+bdd | **EPIC CAP-MODEL TERMINÉ** — unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US : cadrage + 6 exécution). `SlideType` enum migré vers `capsule.feed`, `SlideSegment` étendu (type+manimScene), `CapsuleScriptReader` remplace `CapsuleManager.parseScript` (64 lignes supprimées), 6 consommateurs migrés, `capsule.CapsuleModels` purgé. 361 unit tests VERT (+124). Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios). Zéro doublon modèle. | 9/10
| 068 | 2026-06-29 | migration | **CNV-6 Migration conventions plugins** — `build.gradle.kts` migré vers 4 conventions plugins (gradle-plugin + publishing + functional-test + cucumber 0.0.1). ~70 lignes supprimées (functionalTest 15 + cucumberTest 25 + test config 10 + toolchain/signing 20). `compileKotlin` ✅. Régularisation migration déjà faite (conventions S005 dogfood legacy) mais non committée. | 9/10
| 067 | 2026-06-24 | refactor README | Architecture README Plugin + Consommateur — déplacement README.plugin → capsule-plugin/, README.consommateurs → racine, liens i18n recréés (10 langues) | 8/10
| 041 | 2026-06-24 | documentation | README i18n — 20 fichiers README (10 langues × consumer + plugin) dans `README.consommateurs/` et `README.plugin/`. Badges shields.io (Maven Central 0.0.1-SNAPSHOT orange, Plugin Portal, CI, License). Marqueurs : 195 unit + 22 Cucumber PASS. Switcher i18n 10 langues. AR/UR RTL. Non publié. | 8/10
| 040 | 2026-06-16 | refactor | CR-1 Refactor CapsuleVideoTask.execute() — 5 méthodes extraites (synthesizeTtsForScript, renderManimSlides, replaceManimSlidesInDeck, captureDeckSequential, captureDeckParallel), execute() 218→76 lignes orchestration, 9 nouveaux tests unitaires, compile OK, unit tests PASS | 9/10
| 039 | 2026-06-16 | governance+dev | Analyse CODE_REVIEW.adoc — 15 problèmes → 10 EPICs CR (CR-1→CR-10) + 3 EPICs CAP (CAP-26→CAP-28). Enrichissement BACKLOG, création TODO.adoc, MàJ INDEX. CAP-26 Style burn-in configurable TERMINE — SubtitleBurnInStyle data class + 4 champs DSL/Config/Merger/Scaffold + FFmpeg force_style. 16 tests unitaires + 2 Cucumber @subtitles @burnin @style. Compile OK, unit tests PASS. | 9/10
| 038 | 2026-06-16 | dev | CAP-25 Pipeline E2E burn-in — Cucumber @subtitles @burnin @integration (deck → TTS → capture → mix → burn-in → WebM final), 1 scénario + 2 step defs (Given real ffmpeg, Then burn-in completed), compile OK, unit tests PASS | 9/10
| 037 | 2026-06-16 | dev | CAP-24 Burn-in sous-titres — SubtitleBurnInService interface + Impl (FFmpeg subtitles filter) + NoOp, DSL subtitleBurnIn 4 sources, wiring CapsuleVideoTask post-mixAudioWithVideo, 10 tests unitaires, 3 Cucumber @subtitles @burnin | 9/10
| 036 | 2026-06-16 | dev | CAP-23 Sous-titres SRT/VTT — SubtitleFormat enum + SubtitleEntry + SubtitleGenerator, SrtGenerator + VttGenerator + SubtitleTimingCalculator, DSL subtitleEnabled/subtitleFormat 4 sources, wiring CapsuleVideoTask, 6 Cucumber @subtitles, 43/43 PASSED | 9/10
| 035 | 2026-06-16 | dev | CAP-22 TTS multi-langue — Language enum + VoiceMapping (FR/EN/ES/DE), PiperTtsEngine + EspeakTtsEngine language-aware, 6 Cucumber @tts, 37/37 PASSED, Kover 83.3% line | 9/10
| 034 | 2026-06-16 | diagnostic+bugfix | Bug-fix PlaywrightCaptureImpl.close() avant check vidéo (6 failures Cucumber → 0). Kover: 83.3% line, 51.8% branch. Décision CAP-22 TTS multi-langue. 31/44 Cucumber PASSED. | 9/10
| 033 | 2026-06-15 | bugfix+ci | Bug-fix DSL>merge (CapsuleConventions snapshot + pushConfigIntoExtension guard) + NoOp WebM EBML header (MINIMAL_WEBM ByteArray) + CI Template .github/workflows/test.yml. 211 unit PASS. | 8/10
| 032 | 2026-06-15 | refactor | P1 println→logger (PlaywrightCaptureImpl + CapsuleConfigLoader) + P2 isPlaceholder dead code + P3 lateinit→lazy (6 tâches) + concat-cleanup try/finally + validate-simplify when explicite. 211 unit PASS. | 9/10
| 031 | 2026-06-15 | refactor | P0 DRY (AudioConversionUtil + HtmlSectionParser + MediaProbeUtil) + resource leak finally + magic number DSL (parallelCaptureThreads + parallelRenderThreads) | 9/10
| 030 | 2026-06-13 | maintenance | Mise à jour BACKLOG (publication + priorités P0→P3) | 8/10
| 029 | 2026-06-13 | publish | Publication Maven Central 0.0.1 — nmcp, 195 unit + 22 Cucumber = 217 PASS | 9/10
| 028 | 2026-06-02 | dev | CAP-20 Manim Parallel Render — ManimParallelRenderer interface + NoOp (sequential) + Impl (thread pool Callable) + factory resolveManimParallelRenderer + CapsuleVideoTask refactor renderAll→mux + Config DSL parallelRender (ManimConfig + CapsuleExtension + CapsuleConfigMerger 4 sources + CapsuleScaffoldTask) + 8 config tests + 3 ManimConfigTest + Cucumber @manim @parallel. 195 unit GREEN. | 9/10
| 027 | 2026-06-02 | dev | Cucumber @config @manim — 5 scénarios BDD (quality YAML override, CLI -P override, defaults, outputDir YAML, scaffold manim fields) + 8 step defs + CapsulePlugin lifecycle log manim config + CapsuleScaffoldTask outputDir field. 173 unit GREEN, 5/5 Cucumber PASSED | 9/10
| 026 | 2026-06-02 | dev | CAP-19 ManimEngine configurable — ManimConfig enrichment (outputDir, VALID_QUALITIES, validate) + ManimEngineImpl(config) + CapsuleExtension manimOutputDir + CapsuleConfigMerger 4 sources outputDir + CapsuleVideoTask ManimConfig résolu + 19 nouveaux tests (13+3+3) → 173 unit tests GREEN | 9/10
| 025 | 2026-06-02 | dev | Cucumber @integration @manim E2E complet — CucumberIntegrationTestRunner (filtre not @wip and (@integration or @manim)), capsule.feature scénario E2E pipeline (NoOp render→mux→replace→deck validé), 4 new steps Manim E2E (noop config, .py script, placeholder MP4, muxed MP4, replaced deck). 154 unit tests GREEN. Cucumber @integration @manim PASSED. | 8/10
| 024 | 2026-06-02 | dev | ManimSlideReplacer + computeSlideDurationsWithManim + probeDuration wiring (154 unit GREEN, 21 nouveaux : ManimSlideReplacerTest 14 + ComputeSlideDurationsWithManimTest 4 + ManimSlideReplacementWiringTest 3 + 1 Cucumber @manim slide replacement) | 9/10
| 023 | 2026-06-02 | dev | ManimVideoMixer wiring + mux MP4+TTS (133 unit GREEN, 16 nouveaux : ManimVideoMixerTest 11 + ManimVideoMixerWiringTest 5 + 2 Cucumber @manim) | 8/10
| 022 | 2026-06-02 | dev | Tag @integration Cucumber + ManimEngine wiring + resolveManimEngine factory (117 unit tests GREEN, ManimEngineWiringTest 3/3, CucumberManimEngineTest 4/4) | 8/10
| 021 | 2026-06-02 | dev | Diagnostic Cucumber : 3 failures = environnementaux (espeak/ffmpeg/chromium absents), @config GREEN | 5/10
| 020 | 2026-06-02 | dev | CapsuleConfigMerger + scaffoldCapsuleContext + afterEvaluate wiring (110 unit + 8 func + 9 cucumber GREEN, 3 @config FAILED) | 7/10
| 019 | 2026-06-02 | dev | createSingleSlideHtml + parallelCaptureEnabled DSL + CapsuleConfig + CapsuleConfigLoader (78 unit + 8 func + 9 cucumber = 95 tests) | 8/10
| 018 | 2026-06-02 | dev | ManimEngine.kt (interface+impl+NoOp, 6 tests TDD) + captureSlideParallel + concatWebmFiles (4-6 tests TDD) + split CapsuleManager.kt 957→195 lignes (8 fichiers). Full check GREEN : 56 unit + 8 func + 9 cucumber = 73 tests. | 9/10
| 017 | 2026-06-02 | dev | Intégration sélective fork lion92 (5/5+2) : Parallélisme TTS + ScreenshotCapture + Jackson + Output build/ + Plugin ID fix (37 unit + 8 func + 9 cucumber PASS) | 9/10
| 016 | 2026-06-02 | dev | Intégration sélective fork lion92 2/5 : CapsuleModels (SlideType, espeak, manim config) + DRY (companion resolveScriptDir/DeckDir, AUDIO_INJECT_SCRIPT, parseScript [manim:Scene]). 6 unit tests TDD. | 7/10
| 015 | 2026-06-02 | audit | Revert merge fork lion92 + Cleanup — retour main à 0d69a94, suppression branche audit/lion92-fork. Leçon : zero merge cross-fork. | 8/10
| 014 | 2026-06-01 | audit | Audit fork lion92 — parallélisme TTS, ScreenshotCapture, Manim, DRY refacto. Commit 17adafb sur audit/lion92-fork. | 10/10
| 013 | 2026-06-01 | rename | Renommage borough "Brooklyn Tech" → "Détroit" (24 fichiers live, gouv + docs + code Kotlin) | 10/10
| 012 | 2026-05-19 | documentation | EPIC K K-5 — INDEX.adoc synchronise taxonomie 4 verbes + metadata.json + convention over config. EPIC K ferme (K-0→K-5). | ✅
| 011 | 2026-05-19 | migration | EPIC K-1 — Renommage taxonomie (7 tâches : generateCapsuleScript, generateCapsule, generateCapsuleVideo, deployCapsule, collectCapsuleContext, transformCapsuleContext, collectCapsuleRetrieve) — 12 fichiers modifiés, Compile OK | 10/10
| 010 | 2026-05-18 | migration | CCCP — namespace rename cccp.education.capsule → capsule (8 fichiers + build) | 10/10
| 009 | 2026-05-17 | feat+fix | CAP-9 Pipeline E2E + TTS espeak + Audio mix FFmpeg + Timing dynamique (WebM 322s, 31+10 tests PASS) | 10/10
| 008 | 2026-05-17 | audit | Diagnostic pipeline (4 bugs : reveal.js paths, example-deck, format string, audio injection) | 8/10
| 007 | 2026-05-16 | dev | CapsuleParseContextTask (Jackson parse JSON) + collectCapsuleRetrieve engine N3 + CapsuleDistrib isValidWebM fix + Jackson dans engine build.gradle.kts (42/42 PASS) | 10/10
| 006 | 2026-05-16 | dev | CAP-6 Integrations N3 (capsuleCompositeContext, export JSON engine) + CAP-7 capsuleDistrib (FFmpeg crop 9:16, fallback copy, multi-deck, 36/36 PASS) | 10/10
| 005 | 2026-05-16 | dev | CAP-4 CapsuleVideo orchestration (sequential fallback, multi-deck, noop bridge, @Tag integration chromium) + CAP-5 Cucumber BDD (5 scenarios, cucumberTest task, 28/28 PASS) | 10/10
| 004 | 2026-05-15 | dev | CAP-3 — Playwright Java (PlaywrightCapture interface/Impl/NoOp, generateCapsuleVideo task, injectAudio HTML, Kover 0.9.1, 19/19 PASS) | 10/10
| 003 | 2026-05-15 | dev | CAP-2 — TTS Piper (TtsEngine interface, PiperTtsEngine CLI, NoOpTtsEngine fallback, 11/11 PASS) | 10/10
| 002 | 2026-05-15 | dev | CAP-1 — Plugin Gradle capsule (compileOnly slider, 2 taches, CapsuleExtension DSL, 5/5 PASS) | 10/10
| 001 | 2026-05-15 | build | CAP-0.3 — Structure Gradle root + plugin indépendant, compileKotlin OK, 2/2 PASS | ✅ TERMINE
| 000 | 2026-05-14 | bootstrap | CAP-0.1 + CAP-0.2 — Gouvernance agent + cadrage architecture | ✅ TERMINE
|===
