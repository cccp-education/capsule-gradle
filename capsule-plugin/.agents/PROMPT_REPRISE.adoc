= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminée_ : 106 — CAP-AUDIO US-4 Cucumber BDD + clôture EPIC CAP-AUDIO (5/5 US)
_Prochaine session_ : 107 — CAP-GLOSSARY (P1) ou CAP-PROVENANCE (P1) — à décider

== Mission session 106 (TERMINÉE)

. **CAP-AUDIO US-4 Cucumber BDD** — Feature `capsule_audio_post.feature`
  `@audio @post` (4 scénarios : default NoOp skip économie d'encre, BGM
  enabled NoOp → degraded, ducking only NoOp → degraded, ducking + BGM
  NoOp → degraded) + `CapsuleAudioPostSteps` (13 step defs préfixés
  "audio post", pattern `CapsuleCaptureStrategySteps` — étape interne
  `generateCapsuleVideo`, pas tâche standalone) + `CapsuleAudioPostCucumberRunner`
  (pattern S-082 `@SelectClasspathResource` + filter `@audio and @post and
  not @wip and not @integration`) + `CucumberTaskSpec("cucumberTestAudioPost")`
  12ème câblé `build.gradle.kts` (11ème cucumber dédié).
. 4/4 scénarios PASSED en 7m21s (NoOp processor, pas de vrai FFmpeg —
  valide wiring `applyAudioPostIfEnabled` + économie d'encre + factory
  dispatch, pas le rendu audio couvert par US-2 unit tests).
. `./gradlew check` BUILD SUCCESSFUL in 24m12s. 819 unit VERT (baseline
  S-105 préservée) + 60 functional VERT (baseline S-105 préservée) +
  4 cucumber audio post GREEN. Zéro régression, zéro secret.
. **EPIC CAP-AUDIO TERMINÉ 5/5 US** (US-0 cadrage S-102, US-1 config+merger
  S-103, US-2 processor+factory S-104, US-3 wiring S-105, US-4 cucumber S-106).
  Domaine `capsule.audio` (10ème) clos (6 fichiers prod : `AudioPostConfig`,
  `AudioPostProcessor` interface, `AudioPostProcessorImpl`, `NoOpAudioPostProcessor`,
  `AudioPostCommand`, `AudioPostException`).

== Contexte reprise session 107

- EPIC CAP-AUDIO ✅ TERMINÉ 5/5 US (domaine `capsule.audio` clos).
- 819 unit VERT + 60 functional VERT + 12 `CucumberTaskSpec` (11 cucumber
  dédiés + `cucumberTestIntegration`).
- Modifications S-102→S-106 non committées (Règle 1 — demander permission
  de commit).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Domaines DDD capsule : `capsule.feed` + `capsule.ci` + `capsule.multilang`
  + `capsule.ai` + `capsule.context` + `capsule.pipeline` + `capsule.support`
  + `capsule.i18n` + `capsule.validation` + `capsule.audio` (clos).
- `cucumberTestAudioPost` câblé (12ème `CucumberTaskSpec`) — exécute les
  scénarios `@audio @post` via `CapsuleAudioPostCucumberRunner`.

== EPICs restants (BACKLOG)

. **CAP-GLOSSARY** (P1) — Injection terminologique — `context.glossaryFile`
  4 sources, canal `glossary`, prompt "Use official terminology". 4 US ~6 pts.
. **CAP-PROVENANCE** (P1) — Audit context — `ProvenanceTracker` accumulate
  canaux → `context-provenance.json`, anti-hallucination. 3 US ~5 pts.
. **CAP-CHAPITRE** (P2) — Intro/outro + chapter markers — `CardRenderer` PNG,
  `ChapterMarkers` Matroska tags, domaines `capsule.cards`/`capsule.chapters`.
  5 US ~10 pts.
. **CAP-TRANSCRIPT** (P2) — Reverse pipeline capsule → article AsciiDoc —
  `TranscriptBuilder` + `TranscriptLlmEnhancer`, domaine `capsule.transcript`,
  cross-borough document/bakery. 5 US ~10 pts.
. **CAP-PODCAST** (P2) — Extraction audio-only — `PodcastConcatenator`
  FFmpeg concat MP3 slides, tâche `generateCapsulePodcast`. 3 US ~4 pts.
. **CAP-PREVIEW** (P2) — Mode dry-run — `capture.previewOnly` 4 sources,
  screenshot path s'arrête après PNG. 3 US ~4 pts.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 819 unit tests VERT (S-106 baseline) — vérifier avant de coder
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082 :
  `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner écrase
  la system property cucumber.features) — pattern `CapsuleContextCucumberRunner`
* English only : code, Gherkin, commentaires
* Économie d'encre = skip WebM existant + probe > 0 (ink economy)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `cucumberTestIntegration` câblé (`CucumberTaskSpec` dédié) — exécute les
  scénarios `@integration` via `CucumberIntegrationTestRunner`.
  Lancer avec : `./gradlew cucumberTestIntegration -PrunCucumber`
* `cucumberTestAudioPost` câblé (12ème `CucumberTaskSpec`) — exécute les
  scénarios `@audio @post` via `CapsuleAudioPostCucumberRunner`.
  Lancer avec : `./gradlew cucumberTestAudioPost -PrunCucumber`