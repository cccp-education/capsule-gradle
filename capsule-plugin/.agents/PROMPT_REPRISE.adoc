= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 084 — CAP-ARCH-1 Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`)
_Prochaine session_ : 085 — CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`)

== Mission session 084 (TERMINÉE)

. CAP-ARCH-1 — Socle LLM bridge. Codebase plugin `implementation` (0.0.5) +
  koog-agents 1.0.0 (bump 0.8.0 mort, exclude annotations + buildscript force
  annotations:26.0.2-1 pattern slider) + langchain4j / langchain4j-ollama (BOM).
. Domaine `capsule.ai` : `CapsuleLlmService` (object pur —
  `registerLlmBuildService` + `resolveModel` + `aiProvider` `-Pai.provider`
  default ollama) + `LlmProviderChatModelAdapter` (LlmProvider → ChatModel).
. Tâche `capsuleAiSmokeTest` (groupe `generate`, `usesService`, mock
  `-Pollama.baseUrl`) câblée dans `CapsuleManager`.
. Baby-step TDD/BDD 3 niveaux : 477 unit VERT (+10), 28 functional VERT (+3),
  cucumber `capsule_ai.feature` `@ai` 3/3 GREEN (mock HTTP). `check` VERT.
. 1 FAIL préexistant `@integration` burn-in (subtitle path) — hors CAP-ARCH.

== Contexte reprise session 085

- EPIC CAP-ARCH : 0 cadrage ✅ (S-083), 1 socle LLM bridge ✅ (S-084) —
  **reste CAP-ARCH-2→6** (4+2+3+3 pts). Prochain : CAP-ARCH-2 (~4 pts).
- 477 unit VERT + 28 functional VERT + 3 cucumber `@ai` GREEN.
- **CAP-ARCH-2** — domaine `capsule.context` : `CapsuleContextBuilder` (object
  pur) assemble `CompositeContext` (codebase-contracts 0.1.0) — canaux
  EAGER/RAG/Graphify/Docs + `ChannelBudget`. Unit (toChannels/channelsWithBudget/
  merge) → functional → cucumber (`@context @rag`).
- Backend existant intact : extractSpeakerNotes, TTS, capture, multi-lang CAP-29.
- Modifications S-083→084 non committées (Règle 1 — demander permission de commit).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider 0.0.16
  (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Domaines DDD capsule : `capsule.feed` + `capsule.ci` + `capsule.multilang` +
  `capsule.ai`.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 477 unit tests VERT (S-084 baseline) — vérifier avant de coder
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip WebM existant + probe > 0 (ink economy)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
