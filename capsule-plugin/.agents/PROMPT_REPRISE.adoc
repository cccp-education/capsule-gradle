= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 117 — **CAP-TRANSCRIPT US-3 TranscriptLlmEnhancer
port + ChatModelTranscriptEnhancer adapter** (baby-step TDD strict
RED → GREEN → REFACTOR). Domaine `capsule.transcript` (11ème) enrichi
avec `TranscriptLlmEnhancer.kt` (interface port sync `fun
enhance(template: String, plan: TranscriptPlan): String`, pattern
`CapsuleLlm` — le domaine reste Gradle-free/coroutine-free,
unit-testable avec un plain fake) + `ChatModelTranscriptEnhancer.kt`
(adapter `ChatModel` langchain4j pattern `ChatModelCapsuleLlm` —
`model.chat(UserMessage.from(prompt)).aiMessage().text()`, prompt
construit via `TranscriptPromptBuilder.buildEnhancePrompt`). Fallback
degraded pattern `AudioPostProcessor.process` : LLM blank/whitespace
→ template original (économie d'encre). Short-circuit si template
blank (no LLM call). 6 unit tests créés (`TranscriptLlmEnhancerTest`
+ FakeChatModel minimal override `doChat`). 917 unit VERT (+6 net :
baseline 911 → 917), 64 functional VERT, 13 cucumber dédiés GREEN.
`check` BUILD SUCCESSFUL 23m33s, Kover VERT, validatePlugins VERT,
zéro régression, zéro secret, zéro bump BOM. EPIC CAP-TRANSCRIPT 4/6
US (US-0 ✅ S-114, US-1 ✅ S-115, US-2 ✅ S-116, US-3 ✅ S-117,
US-4→5 TODO).
_Prochaine session_ : 118 — CAP-TRANSCRIPT US-4 tâche
`generateCapsuleTranscript` + wiring. Groupe `generate`, `@OutputFile
transcriptOutput`, `@InputFile speakerNotesFile` (dependsOn
`generateCapsuleContent`), strategy TEMPLATE → `TranscriptBuilder`
directly, strategy LLM → `TranscriptLlmEnhancer.enhance`. Baby-step
TDD ~3 functional.

== Contexte reprise session 118 (post CAP-TRANSCRIPT US-3 S-117)

- EPIC CAP-TRANSCRIPT (P2) — cadré US-0 S-114, US-1 ✅ S-115, US-2 ✅
  S-116, US-3 ✅ S-117. 6 US ~12 pts. Domaine `capsule.transcript`
  (11ème) enrichi US-3 avec `TranscriptLlmEnhancer` (port) +
  `ChatModelTranscriptEnhancer` (adapter). US-4→5 TODO. US-4 cible :
  tâche `generateCapsuleTranscript` groupe `generate` `@OutputFile
  transcriptOutput` (`build/capsule/<deckName>-transcript.adoc`),
  `@InputFile speakerNotesFile` (dependsOn `generateCapsuleContent`),
  `@Input language`/`strategy`, `@Optional @ServiceReference
  LlmBuildService` (si strategy=LLM). Parse speaker notes via
  `AsciidocSpeakerNoteParser` → `CapsuleScript` → `TranscriptPlan`.
  Strategy TEMPLATE → `TranscriptBuilder.build` directly. Strategy
  LLM → `TranscriptBuilder.build` then
  `TranscriptLlmEnhancer.enhance`. Économie d'encre skip si output
  existe ET non-blank. `CapsuleMessages` clé
  `task.generateCapsuleTranscript.description` (24ème key, 11
  `Messages_*.properties`). Wiring
  `CapsuleManager.registerGenerateTranscriptTask`.
- Pattern de référence US-4 : `GenerateCapsuleContentTask`
  (`GenerateCapsuleContentTask.kt:45`) — `@OutputFile
  speakerNotesOutput` = `build/capsule/<deckName>-speaker-notes.adoc`
  (source du transcript). `generateCapsuleTranscript` dependsOn
  `generateCapsuleContent`.
- `capsule.transcript.TranscriptLlmEnhancer`
  (`TranscriptLlmEnhancer.kt`) — interface port `enhance(template,
  plan): String`. Pattern `CapsuleLlm`. Consommé par
  `generateCapsuleTranscript` (US-4) quand strategy=LLM.
- `capsule.transcript.ChatModelTranscriptEnhancer`
  (`ChatModelTranscriptEnhancer.kt`) — adapter `ChatModel`
  langchain4j. Short-circuit si template blank. Fallback degraded si
  LLM blank → template original.
- `capsule.transcript.TranscriptBuilder` (`TranscriptBuilder.kt`) —
  object pur `build(plan): String` template AsciiDoc déterministe.
  Constantes `INTRO`/`OUTRO` publiques. MANIM segments skipped.
- `capsule.transcript.TranscriptPromptBuilder`
  (`TranscriptPromptBuilder.kt`) — object pur
  `buildEnhancePrompt(plan): String` embed `TranscriptBuilder.build`
  + rules. Consommé par `ChatModelTranscriptEnhancer`.
- `TranscriptStrategy` enum (US-1) — `TEMPLATE` (défaut, assemble
  AsciiDoc depuis segments, zéro LLM, déterministe) + `LLM` (enrichit
  via `TranscriptLlmEnhancer`, requiert `LlmBuildService`).
- 4 sources DSL `transcriptEnabled`/`transcriptStrategy` : ENV
  `CAPSULE_TRANSCRIPT_ENABLED`/`_STRATEGY` < props
  `capsule.transcript.enabled`/`.strategy` < YAML < CLI.
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly), codebase-plugin 0.0.5 (implementation),
  koog-agents 1.0.0, BOM 0.0.16, codebase-contracts 0.1.0,
  i18n-contracts 0.0.2.
- 917 unit VERT (S-117 baseline), 64 functional VERT, 13 cucumber dédiés
  GREEN + 2 scénarios VTT. Borough Détroit en maintenance. Tous les EPICs
  P0/P1 terminés. Reste P2 : CAP-TRANSCRIPT (US-4→5), CAP-CHAPITRE,
  CAP-PODCAST, CAP-PREVIEW, CAP-CR3-4, CAP-28.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 917 unit VERT (S-117 baseline), 64 functional VERT, 13 cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) +
  steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues, 23 keys (24ème à ajouter US-4)
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers
  `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` +
  `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat`/
  `mergeTranscriptStrategy` (enums)
* 3 loaders domaine `capsule.context` : `DocContextLoader` (corpus
  documentaire, CAP-DOCCONTEXT), `PedagogicalScenarioLoader` (scénario
  pédagogique, CAP-SPD), `GlossaryLoader` (glossaire terminologique,
  CAP-GLOSSARY). Tous object pur, mirror pattern, `ContextChannel.Docs.
  truncateToTokens` pour la troncation budget.
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`
  (`capsule.audio`), `SubtitleBurnInCommand` (`capsule`, CAP-27),
  `FormatConversion` (`capsule`, CAP-MP4). `buildArgv(...)` sans I/O,
  fully testable.
* `capsule.feed.SlideSegment` (`CapsuleFeedModels.kt:22-33`) — `index`,
  `title`, `speakerNote`, `type: SlideType`, `manimScene: String?`.
  Invariants title/speakerNote non-blank. Source de vérité contenu
  pédagogique. `TranscriptPlan` consomme `List<SlideSegment>` (pas
  nouveau modèle).
* 14 `CucumberTaskSpec` (`build.gradle.kts:29-166`). 15ème
  `cucumberTestTranscript` (US-5) après `cucumberTestGlossaryContext` (L153)
  avant `cucumberTestIntegration` (L159).
* Domaine `capsule.transcript` (11ème) : 7 fichiers prod (TranscriptConfig
  + TranscriptStrategy enum S-115, TranscriptPlan + TranscriptBuilder +
  TranscriptPromptBuilder S-116, TranscriptLlmEnhancer +
  ChatModelTranscriptEnhancer S-117). 3 objets purs (Builder +
  PromptBuilder + Plan data class), 1 port + 1 adapter, 0 I/O dans le
  domaine, fully testable.