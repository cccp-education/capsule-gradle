= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 075 — CR-10 CI optimization cucumberTest onlyIf
_Prochaine session_ : 076 — Backlog capsule restant

== Mission session 075 (TERMINÉE)

. CR-10 — `cucumberTest` est coûteux (Playwright + FFmpeg, ~15 min).
. Skip sauf demande explicite (`-PrunCucumber` ou `CI=true`).
. Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes).
. 7 unit tests `CucumberTestGuardTest` (414 → 421 unit VERT).
. BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps`.
. Câblage `onlyIf` dans `build.gradle.kts` (logique inlinée — script ne peut
  pas importer le classpath `main` du projet).
. `./gradlew check` VERT (cucumberTest SKIPPED sans flag).
. Zéro code production modifié hors build script + nouveau domaine.

== Contexte reprise session 076

- EPIC CR TERMINÉ (CR-1→CR-10, 10/10 EPICs).
- 421 unit tests VERT (+7 vs S-074 414).
- `./gradlew check` VERT — cucumberTest skippé par défaut (CR-10).
- Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature`,
  pas les autres features (à investiguer).
- Backlog capsule restant :
  - CAP-CR3-1 (CI `full` vrais binaires + probe durée) — 🔴 P0
  - CAP-CR3-2 (Mode strict anti-NoOp) — 🔴 P0
  - CAP-CR3-3 (Décision ScreenshotCapture vs Playwright) — 🟠 P1
  - CAP-CR3-4 (Convention Gradle resolveScriptDir/resolveDeckDir) — 🟡 P2
  - CAP-27 (VTT burn-in) — P2
  - CAP-28 (Coverage gaps) — P2
  - CAP-29 (Pipeline traduction vidéo multi-langue, 6 US ~20 pts) — P0
  - CAP-ARCH (cross-borough slider+codebase i18n, 6 US ~19 pts) — après CAP-29
- Version capsule : 0.0.1-SNAPSHOT (non publié Central).
- Capsule consomme slider 0.0.16 (compileOnly + runtime apply).
- Domaine `capsule.ci` créé (CR-10).
- Baseline : `main` avec modifications non committées (S-075).
- Règle 1 — AUCUN commit sans permission.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 421 unit tests VERT (S-075 baseline)
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* English only : code, Gherkin, commentaires
* Domaines DDD capsule : `capsule.feed` + `capsule.ci` (post-CR-10)