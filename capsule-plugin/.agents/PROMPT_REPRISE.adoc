= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 136
:borough: Détroit
:session_precedente: 135 — RENAME-4 fixtures FPA

== Contexte Hérité

Session 215 (globale) — Cadrage EPIC RENAME (transverse 7 boroughs) :
neutralisation nommage métier privé FPA→content / CDA→coding. Doc
`.agents/CADRAGE_EPIC_RENAME.adoc`. Baby-steps : RENAME-1 codebase ✅
S-212 (bump 0.0.12), RENAME-4 capsule ✅ S-135.

Session 135 — RENAME-4 (clôturée) : neutralisation vocabulaire métier
16 fichiers (5 prod + 11 test), gate grep 5 termes = 0, check VERT 13m54s,
baseline 1166 unit + 75 functional 0 FAIL.

## Mission Session 136 (clôturée) — TODO REVERT ✅

Dépin `codebase-gradle-plugin` 0.0.10 → **0.0.14** (aligné lib codebase
N1 0.0.14). Correctif amont `fb19b7c` (propression Provider<OcrEngine>
supprimée) vérifié via git log + strings jar. Commentaire workaround
périmé retiré du catalogue. Gates : compileKotlin VERT, check VERT 15m49s,
baseline exacte 1166 unit + 75 functional + 21 cucumber 0 FAIL.
Rapport : `.agents/sessions/136-todo-revert-codebase-gradle-plugin.adoc`.

== Mission Session 137 — DORMANT (maintenance / nouveau cadrage au besoin)

Borough Détroit = tous EPICs terminés + RENAME-4 + TODO REVERT clôturés.
Aucun EPIC actif. Candidats si demande pilote :

* RENAME-5 plantuml (cross-borough, registre non-traductible — 1 pt)
* Maintenance opportuniste uniquement sur demande explicite

== Méthodologie

Baby-step DDD/TDD/BDD : 1 fichier à la fois, compilation entre chaque
étape. Pas de subagent, pas de parallélisation.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* English only : code, Gherkin, commentaires
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps
  préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `LanguageCatalog` N0 (i18n-contracts 0.0.2) = 10 langues
  (en/zh/hi/es/fr/ar/bn/pt/ru/ur) — pas de "de"
* DSL 4 sources pattern : ENV < props < YAML < CLI
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`,
  `SubtitleBurnInCommand`, `FormatConversion`, `PodcastConcatCommand`
* `capsule.feed.SlideSegment` — source de vérité contenu pédagogique
* `compileOnly(libs.slider)` conservé (runtime). `testImplementation(libs.slider)`
  RETIRÉ S-120 (Groovy 3.x conflit ProjectBuilder).
* Catalogue : `slider-plugin`, slider 0.0.18, codebase 0.0.14 (lib + plugin
  Gradle 0.0.14 alignés — TODO REVERT S-136), BOM 0.0.27.
* 14 domaines DDD capsule (feed, capture, tts, subtitles, manim, config,
  multilang, ai, context, pipeline, i18n, validation, audio, transcript,
  remotion, podcast, support, preview...).
* Baseline : **1166 unit VERT (valeur exacte S-135), 75 functional VERT,
  21 cucumber dédiés GREEN**.
* Java 25 requis (sdkman `25.0.4-tem`) — conventions-plugin 0.0.4 exige JVM 25.