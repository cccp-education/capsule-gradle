= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 138
:borough: Détroit
:session_precedente: 137 — BKY-LANG-22-9/10 (dogfooding 22 langues + fix P0 + publication 0.0.1)

== Contexte (S-137)

Session cross-borough EPIC transverse BKY-LANG-22, pilotée bakery S-226.

- *Bug P0 réel découvert par le dogfooding réel* : la chaîne réelle
  `extractSpeakerNotes` produit `<slug>_<lang>-deck-script.txt` (suffixe `-deck`)
  mais `CapsuleVideoPlanner` exigeait `<deckName>_<lang>-script.txt` →
  fail-fast `no matching deck + script pair`. Les tests NoOp hand-nommés
  masquaient la divergence (S-082 contournait par `mv` manuel).
- *Fix* : `CapsuleVideoPlanner.resolveScript` tolère le suffixe `-deck` (miroir
  S-082 côté sortie WebM). Baby-step DDD/TDD/BDD, RED prouvé, BDD 14/14.
- *Dogfooding réel 22 langues* : consumer composite, `generateCapsuleVideoAllLanguages`
  → **22/22 vidéos** espeak+Playwright (dégradation propre th/ja/ar).
- *Publication Central capsule-plugin 0.0.1* : infra nmcp créée (settings +
  publishing mavenCentral + version release) + fix POM marker (name/description
  sur toutes les publications). Bundle `54a58526`.
- Rapport : `.agents/sessions/137-bky-lang-22-9-10-dogfooding-publication.adoc`.

== Prochaine session (138) — DORMANT (maintenance)

Tous les EPICs capsule sont terminés. À la demande du pilote :
- Maintenance / correctifs.
- Dette transverse : rendu `asciidoctorRevealJs` Gradle cassé (gems JRuby
  `jar-dependencies 0.5.4 vs 0.4.1`, préexistant S-023/S-034) — bloque le
  dogfooding HTML automatisé.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`.
* Baseline S-137 : `test` 1168/0, `cucumberTestMultiLang` 14/0, `functionalTest`
  75 (1 échec infra timeout Playwright sous charge).
* Versions : capsule-plugin 0.0.1 (Central), slider 0.0.19, BOM 0.0.48,
  i18n-contracts 0.0.4, codebase 0.0.14.
* Baby-step DDD/TDD/BDD : unit → functional → cucumber ; tout feature cucumber
  nécessite un runner dédié (bug S-082) + steps préfixés (bug S-088).
* English only : code, Gherkin, commentaires.
* Git/commit/push autorisés (décision pilote 2026-09-17) — zéro secret.
* Rendu HTML : conflit gems JRuby → Ruby système si dogfooding requis.
