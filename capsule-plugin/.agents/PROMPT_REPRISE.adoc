= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 131
:borough: Détroit
:session_precedente: 130 — EPIC CAP-CR3-4 TERMINÉ 3/3 US (coverage gaps)

== Contexte Hérité

Session 124 — **EPIC CAP-28 TERMINÉ 4/4 US** — coverage gaps sur 3 fichiers
low-coverage (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl
26%). US-0 cadrage BACKLOG détaillé (7 décisions). US-1 PiperTtsEngine 4 unit
(`PiperTtsEngineTest.kt` nouveau — `language()` 2 branches + `resolvedModel`
2 branches). US-2 ManimEngineImpl 2 unit (`probeDuration` delegation).
US-3 ScreenshotCaptureImpl 3 unit (capture empty durations throws, isAvailable
ffmpeg absent, close resets probe). Baby-step TDD strict RED → GREEN.
**986 unit VERT 0 FAIL** (+9 net baseline 977 → 986). Zéro prod modifié — pure
couverture. Découverte : `LanguageCatalog` N0 ne contient pas "de" (10 langues :
en/zh/hi/es/fr/ar/bn/pt/ru/ur). EPIC CAP-28 clos.

== Mission Session 131 (clôturée) — Prochaine : S-132

S-131 = **Merge PR #2 (lion92)** — maintenance pipeline + enrichissement
animation Remotion. EPIC CAP-MAINT-PIPELINE TERMINÉ 6/6 US, 1133 unit VERT
(+62 net). Gouvernance synchronisée (BACKLOG/INDEX/SESSIONS_HISTORY/
TEST_COVERAGE/PROMPT_REPRISE).

== Mission Session 132 — Couverture tests PR #2 (CAP-ANIM-TEST + CAP-PR2-COV)

Couverture de la **totalité** du code non testé livré par la PR #2
(S-131). Baby-step DDD/TDD/BDD (RED → GREEN → REFACTOR).

Partie JS — EPIC CAP-ANIM-TEST (`Capsule.jsx`) :

. **US-0 Cadrage** — audit couverture `Capsule.jsx`, décisions extraction
  logique pure (`RemotionTiming` Kotlin OU JS testé node).
. **US-1 Extraction beats** — `beatSeconds` + `applyCues` purs, +unit.
. **US-2 playbackRate borné** — `clipPlaybackRate`, +unit bornes.
. **US-3 Crossfade recouvrement** — `contentAppear`, +unit transition.
. **US-4 Intégration rendu** — test node/jsdom 2 diapos (pixels transition).
. **US-5 BDD Cucumber** — `capsule_remotion_anim.feature` + runner dédié
  20e + steps préfixés "remotion anim".

Partie Kotlin — EPIC CAP-PR2-COV (14 appelants migrés PR #2) :

. **US-0 Cadrage** — audit 14 appelants + stratégie (guard + argv + délégation).
. **US-1 Guard d'intégrité** — `ProcessMigrationGuardTest` (scan
  `ProcessBuilder`/`renameTo`/`waitFor` interdits hors `ProcessRunner`/`FileReplace`).
. **US-2 Appelants ffmpeg** — tests argv binaire factice (8 appelants).
. **US-3 Appelants fichier** — tests délégation `FileReplace.moveOver` (6 appelants).
. **US-4 BDD Cucumber** — `capsule_pr2_coverage.feature` 2 scénarios + runner dédié 20e.

== Méthodologie

Baby-step DDD/TDD/BDD : 1 fichier à la fois, compilation entre chaque étape.
Pas de subagent, pas de parallélisation.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* English only : code, Gherkin, commentaires
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps
  préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `LanguageCatalog` N0 (i18n-contracts 0.0.2) = 10 langues
  (en/zh/hi/es/fr/ar/bn/pt/ru/ur) — pas de "de"
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11 `Messages*.properties`
  10 langues, 24 keys (23 + transcript S-118)
* DSL 4 sources pattern : ENV < props < YAML < CLI
* 3 loaders domaine `capsule.context` : `DocContextLoader`,
  `PedagogicalScenarioLoader`, `GlossaryLoader`
* Pattern object pur pour commande ffmpeg : `AudioPostCommand`
  (`capsule.audio`), `SubtitleBurnInCommand` (`capsule`, CAP-27),
  `FormatConversion` (`capsule`, CAP-MP4)
* `capsule.feed.SlideSegment` — `index`, `title`, `speakerNote`, `type:
  SlideType`, `manimScene: String?`. Source de vérité contenu pédagogique.
* `compileOnly(libs.slider)` conservé (runtime). `testImplementation(libs.slider)`
  RETIRÉ S-120 (Groovy 3.x conflit ProjectBuilder).
* Catalogue : `slider-plugin` (artifactId réel S-067), slider 0.0.18, codebase
  0.0.10, BOM 0.0.27.
* 12 domaines DDD capsule : `capsule.feed`, `capsule.capture`, `capsule.tts`,
  `capsule.subtitles`, `capsule.manim`, `capsule.config`, `capsule.multilang`,
  `capsule.ai`, `capsule.context`, `capsule.pipeline`, `capsule.i18n`,
  `capsule.validation`, `capsule.audio`, `capsule.transcript`, `capsule.remotion`.
* Baseline : 1133 unit VERT, 75 functional VERT, cucumber dédiés GREEN.