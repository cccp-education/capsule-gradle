= PROMPT_REPRISE — Capsule Gradle (Détroit)
:session: 120
:borough: Détroit
:session_precedente: 119 — CAP-TRANSCRIPT US-5 Cucumber BDD

== Contexte Hérité

Session 119 — CAP-TRANSCRIPT US-5 Cucumber BDD (TERMINÉE) : 
- Écriture du fichier de fonctionnalité capsule_transcript.feature avec 4 scénarios
- Création du runner Cucumber dédié CapsuleTranscriptCucumberRunner
- Création des définitions d'étapes CapsuleTranscriptSteps
- Mise à jour de build.gradle.kts pour ajouter la tâche Cucumber dédiée
- Tous les scénarios passent : BUILD SUCCESSFUL
- EPIC CAP-TRANSCRIPT maintenant à 6/6 US terminées (US-0 à US-5)

== Mission Session 120

1. CAP-TRANSCRIPT US-6 TODO (selon l'INDEX.adoc) :
   - Selon l'INDEX.adoc, la prochaine US est TODO (à définir)
   - Vérifier les issues ouvertes dans le domaine capsule.transcript pour voir ce qui reste
   - Si aucune US définie, clôturer l'EPIC CAP-TRANSCRIPT et passer au prochain EPIC (CAP-CHAPITRE)

== Méthodologie

Baby-step DDD/TDD/BDD : 1 fichier à la fois, compilation entre chaque étape.
Pas de subagent, pas de parallélisation.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* English only : code, Gherkin, commentaires
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) + steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11 `Messages*.properties` 10 langues, 24 keys (23 + transcript S-118)
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` + `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat`/`mergeTranscriptStrategy` (enums)
* 3 loaders domaine `capsule.context` : `DocContextLoader` (corpus documentaire, CAP-DOCCONTEXT), `PedagogicalScenarioLoader` (scénario pédagogique, CAP-SPD), `GlossaryLoader` (glossaire terminologique, CAP-GLOSSARY). Tous object pur, mirror pattern, `ContextChannel.Docs.truncateToTokens` pour la troncation budget.
* Pattern object pur pour commande ffmpeg : `AudioPostCommand` (`capsule.audio`), `SubtitleBurnInCommand` (`capsule`, CAP-27), `FormatConversion` (`capsule`, CAP-MP4). `buildArgv(...)` sans I/O, fully testable.
* `capsule.feed.SlideSegment` (`CapsuleFeedModels.kt:22-33`) — `index`, `title`, `speakerNote`, `type: SlideType`, `manimScene: String?`. Invariants title/speakerNote non-blank. Source de vérité contenu pédagogique. `TranscriptPlan` consomme `List<SlideSegment>` (pas nouveau modèle).