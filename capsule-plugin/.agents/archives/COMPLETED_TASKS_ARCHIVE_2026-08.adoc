= COMPLETED_TASKS_ARCHIVE — Août 2026
:date: 2026-08-09

== Session 086 — CAP-ARCH-3 Koog pipeline génération contenu (domaine `capsule.pipeline`) ✅

US-3 de l'EPIC CAP-ARCH. Domaine `capsule.pipeline` : `CapsulePipelineGraph`
(koog 1.0.0, pattern `DeckPipelineGraph` slider) — propose-context →
validate-context → generate-speaker-notes + edges conditionnels. Tâche
`generateCapsuleContent`. 540 unit VERT (+49) + 34 functional VERT (+3).
`check` VERT (3m39s). README master EN mis à jour.

Tâches terminées :
. `src/main/kotlin/capsule/pipeline/CapsuleState.kt` — stage machine `CapsuleStage` + invariants
. `src/main/kotlin/capsule/pipeline/CapsulePromptBuilder.kt` + `DefaultCapsulePromptBuilder.kt` — ports pédagogiques
. `src/main/kotlin/capsule/pipeline/CapsuleLlm.kt` + `ChatModelCapsuleLlm.kt` — port + adapter langchain4j
. `src/main/kotlin/capsule/pipeline/ContentPlanValidator.kt` + `ContentPlanValidationResult.kt` — 8 checks
. `src/main/kotlin/capsule/pipeline/TtsScriptDeriver.kt` — dérivation `*-script.txt`
. `src/main/kotlin/capsule/pipeline/CapsulePipelineGraph.kt` — graphe koog (execute séquentiel + mermaid)
. `src/main/kotlin/capsule/pipeline/GenerateCapsuleContentTask.kt` — tâche (deck + contexte → `.adoc`/`.txt`, UP-TO-DATE, mock routé, durcissement API)
. `src/main/kotlin/capsule/CapsuleManager.kt` — wiring registerGenerateCapsuleContentTask
. `src/test/kotlin/capsule/pipeline/` — 49 unit (CapsuleStateTest, ContentPlanValidatorTest, TtsScriptDeriverTest, CapsulePipelineGraphTest)
. `src/functionalTest/kotlin/capsule/pipeline/GenerateCapsuleContentFunctionalTest.kt` — 3 functional (registration, pipeline E2E, no-deck)
. `README.md` — layout ai/context/pipeline + section "LLM content pipeline (CAP-ARCH-3)" + libs réelles + totaux
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 086
. Vérifications — `check` BUILD SUCCESSFUL (3m39s), zéro secret,
  marqueurs backlog légitimes, pas de commit (règle 1)

Prochaine session : CAP-ARCH-4 — Wiring exécution existante : sortie pipeline
LLM → `generateCapsuleVideoAllLanguages` sans modifier le backend. Décision
`compileOnly slider` confirmée/remplacée. Baby-step TDD : functional (chaîne
complète NoOp) → cucumber (`@i18n @cross-borough`).

== Session 085 — CAP-ARCH-2 Contexte augmenté (RAG + Graphify, domaine `capsule.context`) ✅

US-2 de l'EPIC CAP-ARCH. Domaine `capsule.context` : `CapsuleContextBuilder`
(object pur) assemble le `CompositeContext` (codebase-contracts 0.1.0) —
canaux EAGER/RAG/Graphify/Docs + `ChannelBudget`. 491 unit VERT (+14) +
31 functional VERT (+3) + 4 cucumber `@context @rag` GREEN. `check` VERT.

Tâches terminées :
. `gradle/libs.versions.toml` + `build.gradle.kts` — codebase-contracts 0.0.2 → 0.1.0 +
  `implementation(libs.codebase.contracts)` + `CucumberTaskSpec("cucumberTestContext")`
. `src/main/kotlin/capsule/context/CapsuleContext.kt` — data class, invariant
  `rendered.isBlank() == channels.isEmpty()`, isEmpty/nonEmptyCount/tokenEstimate
. `src/main/kotlin/capsule/context/CapsuleContextBuilder.kt` — object pur
  (defaultBudget/toChannels/channelsWithBudget/merge/build)
. `src/main/kotlin/capsule/context/CollectCapsuleAugmentedContextTask.kt` — tâche
  `collectCapsuleAugmentedContext` (groupe `collect`, eagerFiles
  `.agents/INDEX.adoc` + `PROMPT_REPRISE.adoc` + `AGENT.adoc`
  `@PathSensitive(RELATIVE)`, `-Pcontext.*`, output `build/capsule/augmented-context.txt`)
. `src/main/kotlin/capsule/CapsuleManager.kt` — wiring registerCollectAugmentedContextTask
. `src/test/kotlin/capsule/context/CapsuleContextBuilderTest.kt` — 14 unit tests
. `src/functionalTest/kotlin/capsule/context/CapsuleAugmentedContextFunctionalTest.kt` — 3 functional (registration, artefact, UP-TO-DATE)
. `src/test/features/capsule_context.feature` + `CapsuleContextSteps.kt` +
  `CapsuleContextCucumberRunner.kt` — 4 scénarios `@context @rag` (runner dédié contourne bug S-082)
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 085
. Vérifications — `check` BUILD SUCCESSFUL (3m33s), zéro secret,
  marqueurs backlog légitimes, pas de commit (règle 1)

Prochaine session : CAP-ARCH-3 — Koog pipeline génération contenu (domaine
`capsule.pipeline`, `CapsulePipelineGraph` pattern `DeckPipelineGraph` slider,
`generateCapsuleContent`).

== Session 084 — CAP-ARCH-1 Socle LLM bridge (pattern SLD-8, domaine `capsule.ai`) ✅

US-1 de l'EPIC CAP-ARCH. Capsule devient consommatrice du hub codebase (N1)
pour son futur contenu LLM-driven : plugin codebase `implementation` 0.0.5
(`LlmBuildService` + `LlmProvider`), `koog-agents` bump 0.8.0 (dépendance
morte) → 1.0.0 (exclude annotations + buildscript force annotations:26.0.2-1),
langchain4j / langchain4j-ollama (BOM 0.0.16). Domaine `capsule.ai` :
`CapsuleLlmService` (object pur — `registerLlmBuildService` +
`resolveModel(provider, serviceProvider): ChatModel` via
`LlmProviderChatModelAdapter` + `-Pai.provider` default ollama). Tâche
`capsuleAiSmokeTest` (groupe `generate`, `usesService`, mock `-Pollama.baseUrl`,
log `CAPSULE AI → …`) câblée dans `CapsuleManager`. Baby-step TDD/BDD 3 niveaux :
477 unit VERT (+10), 28 functional VERT (+3), cucumber `capsule_ai.feature` `@ai`
3/3 GREEN (mock HTTP, zéro LLM réel). `./gradlew check` VERT. 1 FAIL
`@integration` burn-in préexistant (hors CAP-ARCH, connu S-069/S-072).

Tâches terminées :
. `build.gradle.kts` + `gradle/libs.versions.toml` — codebase 0.0.5 + koog 1.0.0 +
  langchain4j/langchain4j-ollama (BOM) + buildscript force annotations
. `src/main/kotlin/capsule/ai/CapsuleLlmService.kt` — register + resolve + aiProvider
. `src/main/kotlin/capsule/ai/LlmProviderChatModelAdapter.kt` — pont ChatModel
. `src/main/kotlin/capsule/ai/CapsuleAiSmokeTestTask.kt` — tâche smoke test
. `src/main/kotlin/capsule/CapsuleManager.kt` — câblage registerAiSmokeTestTask
. `src/test/kotlin/capsule/ai/` — 12 unit tests (CapsuleLlmService + Adapter)
. `src/functionalTest/kotlin/capsule/ai/` — 3 functional tests GradleTestKit
. `src/test/features/capsule_ai.feature` + `CapsuleAiSteps.kt` — 3 scénarios `@ai`
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/TODO/SESSIONS_HISTORY/
  TEST_COVERAGE/rapport 084
. Vérifications — `check` VERT, zéro secret, marqueurs backlog légitimes,
  pas de commit (règle 1)

Prochaine session : CAP-ARCH-2 — Contexte augmenté (RAG + Graphify, domaine
`capsule.context`, `CapsuleContextBuilder` + `CompositeContext` + `ChannelBudget`).

== Session 083 — CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu ✅

Cadrage EPIC CAP-ARCH refondu fondé sur le code réel. L'audit corrige le
cadrage S-073 (périmé, fait avant CAP-29) : zéro appel LLM dans capsule (rien
à purger), `koog-agents` 0.8.0 dépendance morte (0 usage), `compileOnly slider`
0 import de classe, `i18n-contracts` déjà consommé (CAP-29.1). Vision refondue
sur la demande product owner : capsule LLM-driven piloté par codebase
(koog + langchain4j + RAG + graphify), backend d'exécution existant recyclé
intact. EPIC redécoupé en 7 US (CAP-ARCH-0 ✅, CAP-ARCH-1→6 TODO, ~21 pts).
Zéro code modifié.

Tâches terminées :
. `BACKLOG.adoc` — section EPIC CAP-ARCH refondue (audit S-083, vision,
  décisions de cadrage, 7 US détaillées), en-tête `last_update` 2026-08-09
. `.agents/INDEX.adoc` — session 083 ajoutée, section EPIC CAP-ARCH refondue,
  roadmap (CAP-ARCH 🟡 EN COURS), métadonnées (prochaine 084)
. `PROMPT_REPRISE.adoc` — réécrit pour session 084 (CAP-ARCH-1, baby-steps 1a-d)
. `TODO.adoc` — CAP-ARCH-0 coché, US 1-6 ajoutées, en-tête mis à jour
. `.agents/SESSIONS_HISTORY.adoc` — entrée 083 ajoutée
. `.agents/sessions/083-caparch0-cadrage-llm-driven.adoc` — rapport de session
. Vérifications — `compileKotlin` VERT, zéro secret, marqueurs backlog légitimes,
  pas de commit (règle 1)

Prochaine session : CAP-ARCH-1 — Socle LLM bridge (pattern SLD-8, domaine
`capsule.ai`, `CapsuleLlmService`, bump koog 1.0.0, purge dépendance morte).

== Session 082 — CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 ✅

Documentation du domaine `capsule.multilang` (README master EN + README plugin),
dogfooding réel du pipeline multi-langue (3 WebM fr/en/ar dont 1 RTL), découverte
et fix d'un défaut de nommage (`CapsuleVideoOutputRenamer`), clôture EPIC CAP-29
7/7 US. 464 unit VERT (+4) + 25 functional VERT + 6 cucumber multilang GREEN.

Tâches terminées :
. `README.md` + `capsule-plugin/README.md` — domaine `capsule.multilang`,
  table des tâches, convention nommage `<deckName>_<lang>-deck.html|script.txt|webm`
. Dogfooding réel consommateur composite `includeBuild` — `extractSpeakerNotes`
  réel → `generateCapsuleVideoAllLanguages` réel (Piper fr/en + espeak, chromium
  Playwright) → 3 WebM générés en 3m07
. Défaut nommage découvert — rendu `<deckName>_<lang>-deck.webm` (header script)
  au lieu du plan `<deckName>_<lang>.webm` (`entry.outputVideo`), brisant
  l'économie d'encre (skip jamais déclenché)
. `capsule/multilang/CapsuleVideoOutputRenamer.kt` (nouveau) — object pur
  `rename(rendered, target): File` aligne le WebM rendu sur le contrat plan
. Wiring post-capture dans `GenerateCapsuleVideoAllLanguagesTask.renderEntry`
. Re-dogfooding — alignement `-deck.webm` → `_<lang>.webm` (3 langues) +
  économie d'encre prouvée (`--rerun-tasks` → `0/3 rendered, 3 skipped`)
. Tests — 4 unit `CapsuleVideoOutputRenamerTest` (+4 net, 464 total) +
  `GenerateCapsuleVideoAllLanguagesFunctionalTest` GREEN (25 total) + 6 cucumber
  multilang GREEN (S-081 baseline, non régressés)
. Vérifications — 1 FAIL `@integration` burn-in préexistant (subtitle path
  `build/capsule/` vs `outputDir=capsules`) hors CAP-29, connu S-069/S-072,
  zéro secret

EPIC CAP-29 TERMINÉ 7/7 US. Domaine `capsule.multilang` clos (9 fichiers prod).

Prochaine session : CAP-ARCH (cross-borough slider+codebase, débloqué par
CAP-29 ✅) ou CAP-CR3 (solidité chaîne vidéo — burn-in @integration FAIL).

== Session 081 — CAP-29.5 Cucumber BDD E2E 10 langues ✅

Cucumber BDD E2E multi-langue (CAP-29.5). `multi_language_capsule.feature`
(6 scénarios `@multilang @i18n`) + `MultiLanguageCapsuleSteps` (step defs).
`cucumberTest -PrunCucumber` : 6/6 multilang GREEN. 460 unit VERT (inchangé)
+ 25 functional VERT + kover VERT.

Tâches terminées :
. `src/test/features/multi_language_capsule.feature` (nouveau) — 6 scénarios :
  plan 10 langues (LanguageCatalog), nommage `<deckName>_<lang>.webm`,
  skip langue sans paire deck+script, fail-fast plan vide, E2E GradleRunner NoOp
  → 10 WebM + log `CAPSULE MULTILANG → n/n languages rendered`, E2E RTL ar/ur
. `src/test/kotlin/capsule/scenarios/MultiLanguageCapsuleSteps.kt` (nouveau) —
  helpers plan pur `CapsuleVideoPlanner.plan()` + 2 scénarios GradleRunner NoOp
  (ttsEngine=noop, captureEnabled=false, deck source + 10 scripts par langue,
  assert WebM `build/capsules/*.webm`)
. Vérifications — `compileTestKotlin` VERT, `cucumberTest -PrunCucumber` 6/6
  multilang GREEN (suite 154 tests, 1 FAIL préexistant `@integration` burn-in
  subtitle path — hors périmètre, connu S-069/S-072), `check` BUILD SUCCESSFUL,
  zéro secret

Prochaine session : CAP-29.6 Documentation + dogfooding (1 deck × 3 langues
fr/en/ar) + clôture EPIC CAP-29 (7/7 US).

== Session 080 — CAP-29.4 Câblage translateAndGenerateCapsuleVideos ✅

Tâche composite `translateAndGenerateCapsuleVideos` : câble le pipeline complet
multi-langue (deck source → translateDeck 10 langues → extractSpeakerNotes 10
scripts → generateCapsuleVideoAllLanguages 10 WebM). 460 unit VERT (inchangé)
+ 25 functional (+3) PASS.

Tâches terminées :
. `CapsuleFeedTaskDomain.kt` — +2 constantes `TRANSLATE_AND_GENERATE_CAPSULE_VIDEOS`
  + `TRANSLATE_AND_GENERATE_VIDEOS_DESCRIPTION` (stable task identifiers)
. `CapsuleFeedTaskRegistrar.kt` — +`registerTranslateAndGenerateCapsuleVideos` :
  `tasks.register` + group `capsule` + description + dependsOn
  (`TRANSLATE_AND_EXTRACT_SPEAKER_NOTES` + `generateCapsuleVideoAllLanguages`)
. `CapsuleManager.kt` — câblage 3ème appel registrar dans
  `registerExtractSpeakerNotesTask()`
. Tests — 3 functional `TranslateAndGenerateCapsuleVideosFunctionalTest`
  (RED→GREEN : registration groupe, description mentionne les 2 sous-tâches,
  dependsOn chain via `--dry-run` avec stub `translateDeck` dans projet isolé)
. Vérifications — `check` BUILD SUCCESSFUL (unit 460 + functional + kover,
  cucumber SKIPPED — CR-10), zéro secret

Prochaine session : CAP-29.5 Cucumber BDD E2E 10 langues
(`multi_language_capsule.feature`).

== Session 079 — CAP-29.3 GenerateCapsuleVideoAllLanguagesTask ✅

Tâche `generateCapsuleVideoAllLanguages` : itère le `CapsuleVideoPlan` (S-078),
TTS+capture par langue avec `ttsLanguage` adapté, économie d'encre. Domaine
`capsule.multilang`. 460 unit tests VERT (+6 net) + 1 functional PASS.

Tâches terminées :
. `CapsuleVideoAllLanguagesRunner.kt` (nouveau) — object pur `run(plan,
  probeDuration, render): List<CapsuleVideoEntry>` (callback render injecté —
  frontière domaine/adaptateur nette) + `shouldRender(entry, probeDuration)` :
  skip si WebM existe ET probe > 0, re-render si probe ≤ 0 (artefact invalide)
. `GenerateCapsuleVideoAllLanguagesTask.kt` (nouveau) — tâche Gradle
  `: CapsuleVideoTask`, `@TaskAction override execute()` : build plan via
  `CapsuleVideoPlanner`, itère via runner, réutilise `synthesizeTtsForScript` +
  `captureDeckSequential/Parallel` + `injectAudio` + `computeSlideDurations`
  (CR-1). TTS par langue via `MultiLanguageResolver` : set `ttsLanguage`/
  `ttsVoice`/`espeakVoice`, reset `ttsEngine=null` (engine résolue par champ).
  Log `CAPSULE MULTILANG → x/y languages rendered`
. Refactor base `CapsuleVideoTask.kt` (zéro changement de comportement) —
  `execute()` → `open` (permet override @TaskAction du sous-type),
  `resolveTtsEngine()`/`injectAudio()` → `internal` (réutilisables module)
. `CapsuleManager.kt` — enregistrement `generateCapsuleVideoAllLanguages`
  (groupe generate)
. Tests — 6 unit `CapsuleVideoAllLanguagesRunnerTest` (RED→GREEN : 3 langues
  toutes rendues, skip WebM valide, re-render WebM invalide, plan mixte ordre
  préservé, shouldRender true/false). _Correction RED_ : `LanguageCatalog` n'a
  pas "de" (10 langues en/zh/hi/es/fr/ar/bn/pt/ru/ur) — langues corrigées
  fr/en/ar/ru. 1 functional `GenerateCapsuleVideoAllLanguagesFunctionalTest` :
  3 langues fr/en/ar NoOp → 3 WebM + log `CAPSULE MULTILANG`
. Vérifications — `test` 460 VERT (0 FAIL), functionalTest PASS, `check`
  BUILD SUCCESSFUL (cucumber SKIPPED — CR-10), zéro secret

Note : le skip ink-economy n'est pas observable en functional (ffprobe ne parse
pas le WebM minimal NoOp, duration vide) → couvert de façon déterministe en unit
via `probeDuration` injecté.

Prochaine session : CAP-29.4 câblage `translateAndGenerateCapsuleVideos`
(dependsOn `translateAndExtractSpeakerNotes` + `generateCapsuleVideoAllLanguages`),
functional (NoOp, vérifie dependsOn chain).

== Session 078 — CAP-29.2 CapsuleVideoPlan + CapsuleVideoPlanner ✅

Domaine `capsule.multilang` enrichi. `CapsuleVideoEntry` (data class immutable
4 champs) + `CapsuleVideoPlan` (invariant non-empty) + `CapsuleVideoPlanner`
(object pur). 454 unit tests VERT (+18 net).

Tâches terminées :
. `CapsuleVideoPlan.kt` (nouveau) — `CapsuleVideoEntry` (deckFile, scriptFile,
  language: SupportedLanguage, outputVideo) avec invariants fail-fast (deck
  exists, script exists, language in LanguageCatalog) + `CapsuleVideoPlan`
  (invariant non-empty, accès `size()`/`isEmpty()`/`languages()`)
. `CapsuleVideoPlanner.kt` (nouveau) — object pur `plan(deckDir, scriptDir,
  outputDir, targetLanguages)` : skip silencieux des langues sans deck+script,
  fail-fast si plan vide. Nommage `<deckName>_<lang>-deck.html` +
  `<deckName>_<lang>-script.txt` → `<deckName>_<lang>.webm` (aligné slider)
. Tests — 10 unit `CapsuleVideoPlanTest` (entries, invariants, languages) +
  8 unit `CapsuleVideoPlannerTest` (découverte 3 langues, skip, throw, naming,
  RTL Arabic) avec `@TempDir`
. Vérifications — `test` 454 VERT (0 FAIL), `check` BUILD SUCCESSFUL, zéro secret

Prochaine session : CAP-29.3 `GenerateCapsuleVideoAllLanguagesTask`, baby-step
TDD unit + functional (NoOp engines, 3 langues).

== Session 077 — CAP-29.1 MultiLanguageResolver + VoiceMapping 10 langues ✅

Domaine `capsule.multilang` créé (7ème domaine DDD). `Language` enum supprimé
(4 entrées FR/EN/ES/DE), remplacé par consommation directe de `LanguageCatalog`
(BOM `i18n-contracts` 0.0.2). `VoiceMapping` étendu à 10 langues (fr/en/es/de/
zh/hi/ar/bn/pt/ru/ur). 436 unit tests VERT (+15 net).

Tâches terminées :
. Pré-requis build — BOM `workspace-bom:0.0.16` + `i18n-contracts:0.0.2` ajoutés
  au `libs.versions.toml` + `build.gradle.kts` (`implementation(platform(...))` +
  `implementation(libs.i18n.contracts)`)
. Domaine `capsule.multilang` créé — `VoiceMapping.kt` (62 lignes, 11 Piper +
  11 espeak) + `MultiLanguageResolver.kt` (51 lignes, `ResolvedLanguage` data
  class + resolver object)
. `Language` enum supprimé — `LanguageVoiceMapping.kt` détruit (40 lignes)
. Migration consommateurs — `TtsManager.kt` (`TtsEngine.language(): SupportedLanguage?`,
  `PiperTtsEngine`/`EspeakTtsEngine` prennent `SupportedLanguage?`),
  `CapsuleVideoTask.kt:197` + `CapsuleBuildTask.kt:40` (`Language.fromCode` →
  `MultiLanguageResolver.resolve`)
. Tests — 22 unit `MultiLanguageResolverTest` (resolve 10 codes, unknown→null,
  RTL ar/ur, piperModel/espeakVoice 11 codes, resolveByPiperModel/EspeakVoice,
  supportedCodes 10) + 2 tests `LanguageVoiceMappingTest` adaptés (délèguent
  à `MultiLanguageResolver`) + 4 tests `PiperTtsEngine`/`EspeakTtsEngine` migrés
  (`Language.EN/DE/ES` → `LanguageCatalog.findByCode`)
. Vérifications — `compileKotlin` VERT, `test` 436 VERT (0 FAIL), `check`
  BUILD SUCCESSFUL (53s), zéro secret, zéro référence `Language` résiduelle

Prochaine session : CAP-29.2 `CapsuleVideoPlan` + `CapsuleVideoPlanner`,
baby-step TDD unit.

== Session 076 — CAP-29.0 Cadrage EPIC pipeline multi-langue ✅

Cadrage formel EPIC CAP-29 (Pipeline Traduction Vidéo Multi-Langue). Zéro code
modifié — session de vision et découpage baby-step.

Tâches terminées :
. Audit code existant — `Language` enum 4 entrées (FR/EN/ES/DE), `VoiceMapping`
  4 Piper+4 espeak, `generateCapsuleVideo` mono-langue, `translateAndExtractSpeakerNotes`
  SLD-11.4 déjà câblé, `slider.translateDeck` 10 langues via `LanguageCatalog`
. Décision d'architecture — consommer `LanguageCatalog` (10 langues, contrats N0
  `i18n-contracts`) au lieu d'étendre enum local. Évite dette technique détruite
  dans CAP-ARCH-1. Pattern document-gradre (`DocumentI18nBridge`), plantuml-gradle
  (`PlantumlTranslationServiceAdapter`)
. Domaine cible — `capsule.multilang` (7ème domaine DDD)
. Découpage US — 7 US (CAP-29.0 cadrage + CAP-29.1→6 exécution), ~20 pts
. Gouvernance — BACKLOG/INDEX/PROMPT_REPRISE/AGENTS racine mis à jour
. Vérifications — `compileKotlin` VERT, zéro secret, backlog synchronisé

Prochaine session : CAP-29.1 `MultiLanguageResolver` + extension `VoiceMapping`
10 langues, baby-step TDD unit.

== Session 075 — CR-10 CI optimization cucumberTest onlyIf ✅

CI optimization (CR-10, 🟡 MOYEN). `cucumberTest` skippé sauf demande explicite
(`-PrunCucumber` ou `CI=true`) pour garder `./gradlew check` rapide en local.

Tâches terminées :
. Domaine `capsule.ci.CucumberTestGuard` (data class pure, 4 méthodes :
  shouldRun/shouldSkip/skipReason) — nouveau domaine DDD
. 7 unit tests `CucumberTestGuardTest` (runCucumber active, CI active, both,
  neither, shouldSkip negation, skipReason mention, skipReason blank)
. Câblage `onlyIf { shouldRun }` dans `build.gradle.kts` (logique inlinée —
  script ne peut pas importer classpath `main`)
. BDD `cucumber_skip.feature` (4 scénarios `@ci`) + `CucumberSkipSteps`
  (5 step defs)
. `./gradlew check` VERT — cucumberTest SKIPPED sans flag (garde active)
. `./gradlew cucumberTest -PrunCucumber` s'exécute (garde désactivée)
. Bug préexistant découvert : `CucumberTestRunner` ne lance que `capsule.feature`
  (à investiguer session future)

Métriques : +7 unit tests (421 total), EPIC CR TERMINÉ (10/10), `check` VERT
sans cucumber.

Rapport : `.agents/sessions/075-cr10-ci-optimization-cucumber-onlyif.adoc`.

== Session 074 — CR-9 Robust HTML parsing ✅

Refactor robust HTML parsing (CR-9, 🟡 MOYEN). Regex raw `Regex("""<section\b[^>]*>""")`
dans `injectAudioSequentialFallback` remplacée par `HtmlSectionParser.findTopLevelSectionOpenTags`.

Tâches terminées :
. `HtmlSectionParser.findTopLevelSectionOpenTags(html): List<MatchResult>` (38 lignes)
  — depth-tracking existant réutilisé (skip nested + self-closing)
. `injectAudioSequentialFallback` : 2 lignes regex raw supprimées → 1 ligne
  `HtmlSectionParser.findTopLevelSectionOpenTags`
. DRY — réutilise `HtmlSectionParser` (DRY-2 S-031)
. Bug fix : vertical stacks (sections imbriquées) n'étaient pas skippées par la
  regex raw → `data-audio` injecté sur sous-sections
. +8 unit tests `HtmlSectionParserTest.findTopLevelSectionOpenTags` (flat, nested,
  self-closing, attributes, empty, malformed, positions, multiline)
. +1 unit test `CapsuleVideoTaskTest.sequential fallback skips nested sections in vertical stack`
  (data-audio count = 2, pas 4)
. +1 scénario cucumber `@audio @sequential` + 2 step defs (Given nested, Then exactly N)
. `./gradlew test` VERT — 414 unit tests, 0 FAIL, 0 SKIP

Métriques : +9 unit tests (414 total), regex raw supprimée, HtmlSectionParser réutilisé.

Rapport : `.agents/sessions/074-cr9-robust-html-parsing.adoc`.

== Session 073 — CR-8 Logging structuré + EPIC CAP-ARCH cadré ✅

CR-8 (🟡 MOYEN) — `CapsulePlugin.kt:40-48` 1 ligne monolithic 13 placeholders →
4 lignes `logger.lifecycle` par section (TTS/Capture/Subtitles/Manim).
`CapsuleConfigLogger` object pur extrait (domaine `capsule.config`),
`formatConfigLog(config): List<String>`. +9 unit tests + 1 scénario cucumber
`@config @logging`. EPIC CAP-ARCH cadré au BACKLOG (6 US ~19 pts).

Rapport : `.agents/sessions/073-cr8-logging-structure-cap-arch.adoc`.

== Session 072 — CR-5 DRY CapsuleConfigMerger ✅

Refactor DRY du `CapsuleConfigMerger.kt` (CR-5, 🟠 MAJEUR). 5 paires de méthodes
`mergeXxxConfig`/`mergeXxxConfigNoYaml` (~76 lignes dupliquées) unifiées en
5 méthodes + 4 helpers génériques.

Tâches terminées :
. `merge()` unifié — `yaml: CapsuleConfig? = if (yamlLoaded) yamlConfig else null`
. 10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 méthodes unifiées (yaml nullable)
. 4 helpers génériques extraits : `mergeStr/mergeInt/mergeDouble/mergeBoolean`
. Sémantique préservée : `toBoolean()` lenient, `isNotBlank()` heuristic strings
. +13 unit tests NoYaml (48 total ConfigMergerTest, +12 vs S-071)
. +1 scénario cucumber `@config @no-yaml` + 1 step Given
. `./gradlew test` VERT — 396 unit tests
. `./gradlew cucumberTest` : scénario @no-yaml PASS, 1 échec @integration préexistant

Métriques : 322→302 lignes (-19 net), 396 unit VERT (+12), 48/48 ConfigMergerTest PASS.

Rapport : `.agents/sessions/072-cr5-dry-config-merger.adoc`.

== Session 071 — CR-2 + Fix Cucumber + CR-3/4/6/7 ✅

CR-2 (thread safety + timeout `captureSlideParallel`) + Fix 5 échecs cucumber
consolidé (DuplicateStepDefinitionException + feature quotes + temp file) +
CR-3 (error handling TTS retry 1× + accumulation `failedSlides` + logger.error) +
CR-4 (HTML escape XSS `injectSubtitleTrack`, HtmlEscape object) + CR-6 (robust
CLI parsing cliInt/cliDouble/cliBoolean) + CR-7 (validation YAML déjà implémenté
+ test). 384 unit tests VERT (+23), 64 cucumber 0 FAIL, 3 functional VERT.

Rapport : `.agents/sessions/071-cr2-fix-cucumber-cr3-4-6-7.adoc`.

== Session 070 — Sync slider 0.0.15 → 0.0.16 ✅

Synchronisation consommateur capsule-gradle suite à la publication slider
0.0.16 (session 066 slider-gradle) + BOM 0.0.17 (session 019 workspace-bom).
Bump catalogue `capsule-plugin/gradle/libs.versions.toml` `slider = "0.0.15"`
→ `"0.0.16"`. `./gradlew compileKotlin` VERT avec slider 0.0.16 (mavenLocal).
Zéro code production modifié. Session pure sync consommateur (cross-borough).

Tâches terminées :
. Bump `capsule-plugin/gradle/libs.versions.toml` slider 0.0.15 → 0.0.16
. `./gradlew :slider:publishToMavenLocal` côté slider-gradine (sync mavenLocal)
. `./gradlew compileKotlin` capsule-plugin VERT avec slider 0.0.16
. Mise à jour INDEX + SESSIONS_HISTORY + création archive août

1 fichier build bumpé — commit + push cette session (accord pilote donné).

Rapport : `.agents/sessions/070-sync-slider-0.0.16.adoc`.

== Session 069 — EPIC CAP-MODEL TERMINÉ ✅

Unification modèle `capsule.feed` ↔ `capsule.CapsuleScript` (7/7 US :
cadrage + 6 exécution). `SlideType` enum migré vers `capsule.feed`,
`SlideSegment` étendu (type+manimScene), `CapsuleScriptReader` remplace
`CapsuleManager.parseScript` (64 lignes supprimées), 6 consommateurs migrés,
`capsule.CapsuleModels` purgé (SlideSegment/CapsuleScript/SlideType supprimés,
reste CapsuleExtension/CapsuleConventions). 361 unit tests VERT (+124).
Cucumber boundary `consolidated_capsule_model.feature` (5 scénarios). Zéro
doublon modèle.

Rapport : `.agents/sessions/069-cap-model-unification.adoc`.

== Session 073 — CR-8 Logging structuré ✅

Refactor logging structuré `CapsulePlugin.kt:40-48` (CR-8, 🟡 MOYEN). 1 ligne
monolithic avec 13 placeholders `{}` → 4 lignes `logger.lifecycle` par section
(TTS, Capture, Subtitles, Manim).

Tâches terminées :
. `CapsuleConfigLogger` object pur extrait (domaine `capsule.config`, 23 lignes)
. `formatConfigLog(config): List<String>` → 4 lignes structurées
. `formatTtsLine` / `formatCaptureLine` / `formatSubtitleLine` / `formatManimLine`
. Tokens préservés verbatim (engine=, quality=, subtitle=, subtitleFormat=, burnIn=, scriptsDir=)
. `CapsulePlugin.kt` câblage (l.40-48 monolithic → 3 lignes forEach)
. +9 unit tests `CapsuleConfigLoggerTest` (4 sections, tokens, defaults, non-blank)
. +1 scénario cucumber `@config @logging` (5 steps) + 5 step defs
. `./gradlew test` VERT — 405 unit tests (+9), 0 failures, 0 errors

EPIC CAP-ARCH cadré au BACKLOG : collaboration cross-borough slider+codebase pour
contenu et i18n (6 US ~19 pts, inspiration document/bakery/plantuml). ⬜ TODO.

Métriques : 405 unit VERT (+9), 1 scénario cucumber ajouté.

Rapport : `.agents/sessions/073-cr8-logging-structure-cap-arch.adoc`.
== Session 087 — Code review + Bug burn-in fix + CAP-ARCH-7 Video Destination Versioning (2026-08-10)

*Code review post-S-083→086* : 4 points relevés.

*Bug burn-in fix* (CAP-ARCH-7 US-0 implicite) : `CapsuleOutputDirResolver`
(object pur, package `capsule.support`) résout l'`outputDir` depuis le
`build.gradle` du projet Cucumber. 9 unit tests TDD. 3 steps Cucumber migrés.
Runner `cucumberTestIntegration` câblé. **BUILD SUCCESSFUL 3m39s** — scénario
E2E burn-in VERT (était FAIL depuis S-069).

*CAP-ARCH-7 Video Destination Versioning* (nouvelle feature issue review) :
- US-1 : `OutputConfig` + `VersioningStrategy` (TIMESTAMP|INCREMENTAL) étend
  `CapsuleConfig` section `output`. `CapsuleConfigMerger` 4 sources. 10 unit.
- US-2 : `VideoDestinationResolver` (object pur). 10 unit.
- US-3 : `DistributeCapsuleVideoTask` (tâche `distributeCapsuleVideo`). 3 functional.

Métriques : 568 unit VERT (+28 net) + 37 functional VERT (+3). Cucumber
`@burnin @integration` E2E VERT.

Rapport : `.agents/sessions/087-code-review-burn-in-fix-caparch7.adoc`.
