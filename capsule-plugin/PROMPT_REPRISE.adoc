= PROMPT_REPRISE — Session 029 : Refactoring DRY top priorité

== Contexte

Session 028 — Code review complète de capsule-gradle. Score 8/10. 19 fichiers source, 7 648 LOC, 173 unit + 8 functional + 27 Cucumber. Pipeline complet TTS→Capture→Manim→Mix→Distrib fonctionnel.

== Problèmes identifiés en top priorité (code review session 028)

1. **DRY ffprobeDuration ×3** — CapsuleVideoTask, ManimEngineImpl, ManimVideoMixerImpl
2. **DRY extractTopLevelSections ×2** — CapsuleVideoTask, ManimSlideReplacer
3. **DRY wavToMp3 ×2** — PiperTtsEngine, EspeakTtsEngine
4. **println → logger** dans PlaywrightCaptureImpl (l.75)
5. **Resource leak Playwright** — close() pas dans finally

== Prochaine étape

Attaquer les 3 DRY P0 : factoriser ffprobeDuration, extractTopLevelSections, wavToMp3 dans des utilitaires partagés.

== État git

main → modifications non commitées (session 027 + session 028)
BACKLOG mis à jour avec 12 tickets (3 P0, 2 P1, 4 P2, 3 P3)