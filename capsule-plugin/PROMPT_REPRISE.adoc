= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 110 — CAP-GLOSSARY US-1 `GlossaryEntry` +
`GlossaryContext` + `GlossaryLoader` (object pur) + `CapsuleContext.
glossarySection` (4ème champ, invariant ajusté) + `CapsuleContextBuilder.
build()` étendu (4ème paramètre `glossarySection`, `buildRendered`
refactor 3 sections conditionnelles, `tracker.retainOnly` étendu `+=
GLOSSARY_CHANNEL`) + `DefaultCapsulePromptBuilder` étendu (instruction
"Use the official terminology" conditionnelle `contains("Official
Glossary")`) + `ContextProvenance.GLOSSARY_CHANNEL` const. Domaine
`capsule.context` étendu (3 nouveaux fichiers prod : `GlossaryEntry.kt`,
`GlossaryContext.kt`, `GlossaryLoader.kt` — 3ème loader du domaine après
`DocContextLoader` et `PedagogicalScenarioLoader`). Baby-step TDD strict
RED (23 tests → `Unresolved reference`) → GREEN (857 unit VERT +23 net :
15 `GlossaryLoaderTest` + 7 `CapsuleContextBuilderTest` glossarySection +
2 `DefaultCapsulePromptBuilderTest` glossary) → REFACTOR (`check` BUILD
SUCCESSFUL in 18m48s, 62 functional VERT inchangé, Kover VERT,
validatePlugins VERT, zéro régression, zéro secret). Backward compat
strict (`glossarySection = ""` défaut → tests existants inchangés). EPIC
CAP-GLOSSARY 2/4 US.
_Prochaine session_ : 111 — CAP-GLOSSARY US-2 Wiring
`CollectCapsuleAugmentedContextTask` + `ContextConfig` 3ème champ
`glossaryFile` + merger 4 sources + `CapsuleExtension.glossaryFile` +
`pushConfigIntoExtension` + `CapsuleManager` provider + provenance
tracking `GLOSSARY` channel. Baby-step TDD 2 functional
(GlossaryContextFunctionalTest : glossary fourni → contexte enrichi
section `==== Official Glossary (glossary)` ~M tokens, absent → fallback
no-op `glossarySection = ""`).

== Contexte reprise session 111 (post CAP-GLOSSARY US-1 S-110)

- EPIC CAP-GLOSSARY (P1) — Injection terminologique, 4 US ~6 pts. US-0
  cadrage ✅ S-109. US-1 data classes + loader + builder + prompt
  builder étendus ✅ S-110 (857 unit VERT +23). Reste US-2 wiring + 2
  functional → US-3 cucumber 3 scénarios.
- Domaine `capsule.context` étendu (pas de nouveau domaine —
  `GlossaryEntry` + `GlossaryContext` + `GlossaryLoader` rejoignent
  `DocContextLoader`/`PedagogicalScenarioLoader`/`ProvenanceTracker`/
  `CapsuleContextBuilder`/`CapsuleContext`/`ContextProvenance` dans le
  domaine existant 13 fichiers prod après US-1).
- US-1 livré (S-110) :
  * `GlossaryEntry(term: String, definition: String)` data class
    immutable (invariant fail-fast si `term` ou `definition` blank).
  * `GlossaryContext(terms: List<GlossaryEntry>)` data class immutable
    (liste ordonnée — l'ordre a une valeur pédagogique, pas une map).
  * `GlossaryLoader` object pur mirror `PedagogicalScenarioLoader` —
    `load(glossaryFile: File, maxTokens: Int): String`. Parse AsciiDoc
    `== Glossary` section + bullets `- term: definition` (split sur
    premier `:`), rend `==== Official Glossary (glossary)\n<term>:
    <definition>\n...`, tronque via `ContextChannel.Docs.truncateToTokens
    (maxTokens)` (variant Docs sémantiquement proche). Missing file →
    empty string. Malformed bullet (pas `- term: definition`) skipped.
    Non-Glossary section ignored. Zero budget → empty string.
  * `CapsuleContext` 4ème champ `glossarySection: String = ""`,
    invariant ajusté `rendered.isBlank() == (channels.isEmpty() &&
    scenarioSection.isBlank() && glossarySection.isBlank())`.
  * `CapsuleContextBuilder.build(composite, budget, scenarioSection="",
    glossarySection="", tracker=null)` étendu — `buildRendered` private
    helper refactor `when` 4 cas → `listOf(channelBlock, trimmedScenario,
    trimmedGlossary).filter { isNotBlank }.joinToString("\n\n")` 3
    sections conditionnelles. `tracker.retainOnly` étendu `+=
    GLOSSARY_CHANNEL` si `trimmedGlossary.isNotBlank()` (pattern
    `SCENARIO_CHANNEL`). `CapsuleContext` retour étendu `glossarySection
    = trimmedGlossary`.
  * `DefaultCapsulePromptBuilder.buildGeneratePrompt` étendu —
    instruction line conditionnelle `if (state.augmentedContext.contains
    ("Official Glossary"))` → "Use the official terminology defined in
    the Official Glossary section. Prefer glossary terms over generic
    synonyms." (pattern `Pedagogical Scenario` L50-55).
  * `ContextProvenance.GLOSSARY_CHANNEL = "GLOSSARY"` const (pattern
    `SCENARIO_CHANNEL` L41).
  * `CollectCapsuleAugmentedContextTask` L132 appel `build()` migré en
    arguments nommés (`composite = `, `budget = `, `scenarioSection = `,
    `tracker = `) pour robustesse — le wiring US-2 étendra avec
    `glossarySection = resolveGlossarySection(budget)`.
- 857 unit VERT (S-110 baseline, +23 net : 15 `GlossaryLoaderTest` + 7
  `CapsuleContextBuilderTest` glossarySection + 2
  `DefaultCapsulePromptBuilderTest` glossary). 62 functional VERT
  (inchangé). `check` BUILD SUCCESSFUL in 18m48s (S-110).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4), codebase-plugin
  0.0.5 (implementation), koog-agents 1.0.0, BOM 0.0.16, codebase-contracts
  0.1.0, i18n-contracts 0.0.2.
- Patterns de référence : CAP-SPD S-094/095 (extension capsule-local
  `scenarioSection`, `PedagogicalScenarioLoader` mirror — `GlossaryLoader`
  est le 3ème loader du domaine `capsule.context`), CAP-DOCCONTEXT S-093
  (`DocContextLoader` 1er loader, `ContextConfig` 4 sources),
  CAP-PROVENANCE S-107/108 (`ProvenanceTracker` étendu, `GLOSSARY_CHANNEL`
  const pattern `SCENARIO_CHANNEL`).

== Cible US-2 (détails BACKLOG)

- `ContextConfig` 3ème champ `glossaryFile: String? = null` (pattern
  `scenarioFile` CAP-SPD-3).
- `CapsuleConfigMerger.mergeContextConfig` étendu `mergeStr` pour
  `glossaryFile` (pattern `scenarioFile` L329). `loadFromEnvironment`
  (`CAPSULE_CONTEXT_GLOSSARY_FILE`) + `buildConfigFromProperties`
  (`capsule.context.glossaryFile`) étendus.
- `CapsuleExtension.glossaryFile: Property<String>` convention `""`
  (pattern `scenarioFile`).
- `pushConfigIntoExtension` push `glossaryFile` (pattern `scenarioFile`
  L124-126).
- `CollectCapsuleAugmentedContextTask.glossaryFile:
  ConfigurableFileCollection @InputFiles @PathSensitive(RELATIVE)
  @Optional` + `resolveGlossarySection(budget)` (5% budget = 400
  tokens/8000, pattern `resolveScenarioSection` L276-291) + `trackSources`
  étendu tracking GLOSSARY channel (1 source si résolu, pattern SCENARIO
  L225-244).
- `CapsuleManager.registerCollectAugmentedContextTask` wiring lazy
  provider `glossaryFile.from(project.provider {...})` (pattern
  `scenarioFile` L229-237).
- `CapsuleContextBuilder.build()` appel étendu `glossarySection =
  resolveGlossarySection(budget)`.
- Baby-step TDD : 2 functional (GlossaryContextFunctionalTest : glossary
  fourni → contexte enrichi section `==== Official Glossary (glossary)`
  ~M tokens, absent → fallback no-op `glossarySection = ""`).

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 857 unit VERT (S-110 baseline), 62 functional VERT, 12 cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) +
  steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues
* Runners cucumber dédiés (12) — `cucumberTestGlossaryContext` à créer US-3
  (14ème `CucumberTaskSpec`, 13ème cucumber dédié)
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers
  `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` +
  `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat` (enums)
* CAP-GLOSSARY = extension capsule-local `glossarySection` (pas de LLM/réseau/
  FFmpeg — load fichier glossaire AsciiDoc + troncation budget + prompt
  instruction). Pattern CAP-SPD mirror (`scenarioSection` 3ème extension
  capsule-local après `scenarioSection`).
* 3 loaders domaine `capsule.context` après US-1 : `DocContextLoader` (corpus
  documentaire, CAP-DOCCONTEXT), `PedagogicalScenarioLoader` (scénario
  pédagogique, CAP-SPD), `GlossaryLoader` (glossaire terminologique,
  CAP-GLOSSARY). Tous object pur, mirror pattern, `ContextChannel.Docs.
  truncateToTokens` pour la troncation budget.