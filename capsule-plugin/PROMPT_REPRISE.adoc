= PROMPT_REPRISE — Session 039 : CAP-26 Style burn-in configurable (font size, couleur, position)

== Contexte

Session 038 — CAP-25 Pipeline E2E avec burn-in terminé.
1 scénario Cucumber @subtitles @burnin @integration ajouté (deck → TTS → capture → mix → burn-in → WebM final).
2 step defs : Given real ffmpeg config + Then burn-in completed successfully.
Compile OK, unit tests PASS. cucumberTest hang pré-existant non résolu.

== État git

2 fichiers modifiés (capsule.feature + CapsuleStepDefinitions.kt). Aucun commit.
Compile: BUILD SUCCESSFUL.
Unit tests: BUILD SUCCESSFUL.
Cucumber: cucumberTest task hang (pré-existant).
Secrets: 0 trouvé.

== Prochaines étapes

. CAP-26 Style burn-in configurable — DSL subtitleBurnInStyle (fontSize, fontColor, outlineColor, position)
. Passer les paramètres de style dans la commande FFmpeg `force_style='FontSize=XX,PrimaryColour=&H...'`
. Cucumber @subtitles @burnin @style scenarios
. Option: CAP-27 VTT burn-in (FFmpeg subtitles filter accepte aussi VTT)
