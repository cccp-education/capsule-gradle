= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 088 — CAP-ARCH-4 Wiring backend + CAP-ARCH-7 US-4 Cucumber distrib
_Prochaine session_ : 089 — CAP-ARCH-5 (i18n aligné socle) ou CAP-ARCH-6 (cucumber cross-borough E2E)

== Mission session 088 (TERMINÉE)

. **CAP-ARCH-4 Wiring exécution existante** (2 pts) — tâche composite
  `generateCapsuleContentAndVideos` (groupe `capsule`) chaîne
  `generateCapsuleContent` → `translateAndGenerateCapsuleVideos` (qui chaîne
  déjà translateDeck → extractSpeakerNotes → generateCapsuleVideoAllLanguages).
  Backend existant intact. Pattern `DefaultTask` + `dependsOn` par nom string
  (S-080). 3 functional + 3 cucumber `@architecture @wiring` GREEN.
. **CAP-ARCH-7 US-4 Cucumber BDD `@distrib @versioning`** — feature
  `capsule_distrib.feature` (5 scénarios) + `CapsuleDistribSteps`
  (self-contained GradleRunner) + `CapsuleDistribCucumberRunner`. Scénarios :
  registration, copy timestamp, copy incremental v1, second run v2, skip
  idempotent (économie d'encre). 5 cucumber GREEN.
. 568 unit VERT (inchangé) + 40 functional VERT (+3) + 8 cucumber dédiés
  GREEN (3 `@architecture @wiring` + 5 `@distrib @versioning`). Zéro régression.

== Contexte reprise session 089

- EPIC CAP-ARCH : 0 ✅ S-083, 1 ✅ S-084, 2 ✅ S-085, 3 ✅ S-086,
  **4 ✅ S-088**, 7 ✅ S-087+S-088 — reste **CAP-ARCH-5 (3 pts) + CAP-ARCH-6
  (3 pts)** = 6 pts.
- 568 unit VERT + 40 functional VERT + cucumber `@burnin @integration` E2E VERT
  + 3 cucumber `@architecture @wiring` + 5 cucumber `@distrib @versioning`.
- **CAP-ARCH-5** — i18n aligné sur le socle : i18n reste via
  `slider.translateDeck` (déjà LLM codebase SLD-8). Vérification pipeline
  complet × 10 langues. `CapsuleI18nBridge` si nécessaire. Non-régression :
  464 unit + 25 functional + 6 cucumber multilang GREEN.
- **CAP-ARCH-6** — Cucumber BDD boundary cross-borough : E2E deck source →
  translateDeck (10 langues) → translateAndExtractSpeakerNotes →
  generateCapsuleContent (mock LLM) → generateCapsuleVideoAllLanguages. Tags
  `@i18n @cross-borough @architecture @ai`. Mock LLM HTTP (`-Pollama.baseUrl`).
- Backend existant intact : extractSpeakerNotes, TTS, capture, multi-lang CAP-29.
- `CapsuleOutputDirResolver` (package `capsule.support`) disponible pour
  migrer les steps Cucumber restants qui hardcodent `build/capsule/`.
- Modifications S-083→088 non committées (Règle 1 — demander permission de commit).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider 0.0.16
  (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Domaines DDD capsule : `capsule.feed` + `capsule.ci` + `capsule.multilang` +
  `capsule.ai` + `capsule.context` + `capsule.pipeline` + `capsule.support` (test).
- Runners cucumber dédiés : `CapsuleContextCucumberRunner`, `CapsuleContentAndVideosCucumberRunner`,
  `CapsuleDistribCucumberRunner`, `CucumberIntegrationTestRunner` (pattern S-082).

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 568 unit tests VERT (S-088 baseline) — vérifier avant de coder
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082 :
  `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner écrase
  la system property cucumber.features) — pattern `CapsuleContextCucumberRunner`
* English only : code, Gherkin, commentaires
* Économie d'encre = skip WebM existant + probe > 0 (ink economy)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `cucumberTestIntegration` câblé — exécute les scénarios `@integration`.
  Lancer avec : `./gradlew cucumberTestIntegration -PrunCucumber`
* `cucumberTestContentAndVideos` câblé — `./gradlew cucumberTestContentAndVideos -PrunCucumber`
* `cucumberTestDistrib` câblé — `./gradlew cucumberTestDistrib -PrunCucumber`