= PROMPT_REPRISE — Session 042 : CR-2 Thread safety + timeout captureSlideParallel

== Contexte

Session 041 — README i18n TERMINE.
20 fichiers README créés (10 langues × consumer + plugin) dans
`README.consommateurs/` et `README.plugin/` à la racine `capsule-gradle/`.
Badges shields.io, marqueurs 0.0.1-SNAPSHOT (non publié),
195 unit + 22 Cucumber PASS, switcher i18n, AR/UR RTL.

== État git

Fichiers créés : 20 README + `.agents/sessions/041-readme-i18n.adoc`.
Fichiers modifiés : INDEX.adoc, SESSIONS_HISTORY.adoc, PROMPT_REPRISE.adoc.
Aucun commit.

== Prochaines étapes

. CR-2 Thread safety + timeout captureSlideParallel
. executor.shutdown() → executor.shutdownNow() dans finally
. futures.mapNotNull { it.get() } → it.get(5, TimeUnit.MINUTES) avec try/catch
. Tests unitaires : simuler task bloquante → timeout
. Cucumber @parallel @timeout

== Backlog priorisé

. 🔴 CR-2 — Thread safety + timeout captureSlideParallel
. 🔴 CR-3 — Error handling TTS — dégradation signalée + retry
. 🟠 CR-4 — Sécurité HTML escape injectSubtitleTrack
. 🟠 CR-5 — DRY CapsuleConfigMerger (~76 lignes dupliquées)
. 🟠 CR-6 — Robust CLI parsing (`as? Int` → `toIntOrNull()`)
. 🟠 CR-7 — Validation YAML avec try/catch gracieux
. 🟡 CR-8 — Logging structuré CapsulePlugin
. 🟡 CR-9 — Robust HTML parsing (HtmlSectionParser au lieu de regex raw)
. 🟡 CR-10 — CI optimization cucumberTest onlyIf
. 🟢 CAP-27 — VTT burn-in
. 🟢 CAP-28 — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%)