= PROMPT_REPRISE — Session 022 : Tag @integration Cucumber + Manim Cucumber + ManimEngine wiring

== Contexte

Session 021 — Diagnostic : les 3 Cucumber FAILURES sont les tests environnementaux
(espeak/ffmpeg/chromium absents), PAS les 3 scénarios @config. Les @config sont GREEN.

== Diagnostic session 021

Les 3 scénarios Cucumber qui échouent sont :
1. "Audio constraint — real TTS must produce binary MP3 not text" → espeak absent
2. "Output constraint — capsule video must be written to build/capsules/" → chromium absent
3. "WebM validity constraint — generated video must have EBML header" → chromium absent

Les 3 scénarios @config (scaffold, overwrite, YAML override) PASSÉS.

== Prochaines étapes (priorité décroissante)

. **Taguer les 3 scénarios environnementaux `@integration`** dans capsule.feature pour les exclure du run Cucumber normal (CucumberTestRunner filtre déjà `not @integration`).
. **Cucumber BDD Manim** — Scénario Gherkin `[manim:SceneName]` avec mock/skip conditionnel.
. **Intégration ManimEngine** — Quand `SlideType.MANIM`, appeler `ManimEngine.render()`.
. **CapsuleManager resolveManimEngine()** — Factory résolvant ManimEngine depuis CapsuleConfig.

== État git

main → modifications non commitées (session 019+020+021 contenu non commité — attendre permission)
Unit tests: BUILD SUCCESSFUL (110 unit + 8 func = GREEN)
Cucumber: 9/12 (3 env @integration à taguer, 3 @config GREEN)