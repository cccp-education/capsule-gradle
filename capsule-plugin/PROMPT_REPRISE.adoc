= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 096 — CAP-CR3-3 Branchement ScreenshotCapture (EPIC TERMINÉ 5/5 US). `CaptureStrategy` enum (PLAYWRIGHT/SCREENSHOT, défaut PLAYWRIGHT backward compat) + `CaptureConfig.strategy` 15ème champ + `mergeCaptureConfig` 4 sources (ENV `CAPSULE_CAPTURE_STRATEGY` < props `capsule.capture.strategy` < YAML < CLI). `CaptureResolver` object pur (factory dispatch `when(strategy)` avec `PlaywrightFactory`/`ScreenshotFactory` fun interfaces, `StrictModeGuard` réutilisé). `ScreenshotPlanner` object pur (`ScreenshotCapturePlan` + `ScreenshotSlideEntry` + `renderConcatList` + `ffmpegPngToWebmArgs` + `ffmpegConcatArgs`). `ScreenshotCaptureImpl.capture()` étendu multi-slide (boucle screenshot PNG par slide + navigate Reveal.next() + ffmpeg png→webm par slide durée exacte + concat webm concat demuxer). `CapsuleExtension.captureStrategy: Property<CaptureStrategy>` + helper Groovy `captureStrategy(String)`. `CapsuleVideoTask.resolvePlaywrightCapture` refactorisé → `CaptureResolver.resolve` (log "Capture: X strategy"). Cucumber `capsule_capture_strategy.feature` 3 scénarios `@capture @strategy` + runner dédié `CapsuleCaptureStrategyCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestCaptureStrategy")`. Baby-step TDD/BDD 3 niveaux : 29 unit (7 CaptureStrategy + 8 merger + 6 CaptureResolver + 8 ScreenshotPlanner) → 3 functional → 3 cucumber. 742 unit + 52 functional + 3 cucumber dédiés GREEN, 0 FAIL. Zéro régression.
_Prochaine session_ : 097 — Prochain EPIC séquence MVP0 : CAP-MP4 (Export MP4/H.264, 4 US ~4 pts, P1 — distribution talaria.school). Alternative : CAP-AUDIO (P1 — qualité perçue). Vérifier séquence backlog : CAP-MP4 (P1) > CAP-AUDIO (P1) > CAP-GLOSSARY (P1) > CAP-PROVENANCE (P1).

== Contexte reprise session 097 (post CAP-CR3-3 TERMINÉ)

- EPIC CAP-CR3-3 TERMINÉ 5/5 US (S-096) — `ScreenshotCaptureImpl` désormais câblé
  comme alternative configurable via `capture.strategy: playwright|screenshot`.
- `CaptureStrategy` enum (`CapsuleConfig.kt`) — 2 valeurs `PLAYWRIGHT`/`SCREENSHOT`,
  défaut `PLAYWRIGHT` (backward compat). Companion `fromString` case-insensitive.
- `CaptureResolver` object pur (`CaptureResolver.kt`) — factory dispatch
  `when(strategy)` avec `PlaywrightFactory`/`ScreenshotFactory` fun interfaces.
- `ScreenshotPlanner` object pur (`ScreenshotPlanner.kt`) — produit
  `ScreenshotCapturePlan` (entries + concatListFile + finalWebm) + argv builders.
- `ScreenshotCaptureImpl` multi-slide (`PlaywrightManager.kt`) — boucle screenshot
  PNG par slide + ffmpeg png→webm + concat webm.
- `CapsuleExtension.captureStrategy: Property<CaptureStrategy>` + helper Groovy
  `captureStrategy(String)` (case-insensitive).
- `CaptureConfig.strategy` 4 sources (ENV<props<YAML<CLI, ENV
  `CAPSULE_CAPTURE_STRATEGY`, props `capsule.capture.strategy`).
- Cucumber `capsule_capture_strategy.feature` 3 scénarios `@capture @strategy`,
  runner dédié `CapsuleCaptureStrategyCucumberRunner` (pattern S-082),
  `CucumberTaskSpec("cucumberTestCaptureStrategy")` câblé.
- 742 unit + 52 functional + 3 cucumber dédiés VERT (baseline S-096).
- Domaines DDD capsule (8) : `capsule.feed` + `capsule.ci` +
  `capsule.multilang` + `capsule.ai` + `capsule.context` +
  `capsule.pipeline` + `capsule.support` + `capsule.i18n`.
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Modifications S-096 non committées (code + gouvernance).

== EPICs restants (séquence MVP0)

* CAP-CR3-3 (P0) — branchement ScreenshotCapture, 5 US ~13 pts
* CAP-CR3-1 (P0, maintenance) — CI `full` avec vrais binaires + probe durée
* CAP-MP4 (P1) — export MP4/H.264, 4 US ~4 pts
* CAP-AUDIO (P1) — post-production audio, 5 US ~10 pts
* CAP-GLOSSARY (P1) — injection terminologique, 4 US ~6 pts
* CAP-PROVENANCE (P1) — audit context, 3 US ~5 pts
* CAP-CHAPITRE (P2) — intro/outro + chapter markers, 5 US ~10 pts
* CAP-TRANSCRIPT (P2) — reverse pipeline article AsciiDoc, 5 US ~10 pts
* CAP-PODCAST (P2) — extraction audio-only, 3 US ~4 pts
* CAP-PREVIEW (P2) — mode dry-run, 3 US ~4 pts
* CAP-CR3-4 (P2, maintenance) — convention Gradle explicite resolveScriptDir/resolveDeckDir
* CAP-27 (P2, maintenance) — VTT burn-in
* CAP-28 (P2, maintenance) — coverage gaps

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 713 unit + 49 functional VERT (S-095 baseline)
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082 :
  `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner
  écrase la system property cucumber.features) — pattern
  `CapsuleContextCucumberRunner` / `CapsuleDocContextCucumberRunner` /
  `CapsuleScenarioContextCucumberRunner` (NEW S-095)
* Glue `capsule.scenarios` partagé : tous les steps doivent avoir des
  signatures uniques (bug S-088 `DuplicateStepDefinitionException`) —
  préfixer par feature (ex. "scenario context")
* English only : code, Gherkin, commentaires
* Économie d'encre = skip WebM existant + probe > 0 (ffprobe durée > 0)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues, `resolveLanguage(project)` cascade
  `-Pcapsule.language` > DSL > `"en"`
* Runners cucumber dédiés : `CapsuleContextCucumberRunner`,
  `CapsuleContentAndVideosCucumberRunner`, `CapsuleDistribCucumberRunner`,
  `CapsuleArchBoundaryCucumberRunner`, `CapsuleStrictModeCucumberRunner`,
  `CapsuleDocContextCucumberRunner`, `CapsuleScenarioContextCucumberRunner`,
  `CucumberIntegrationTestRunner` (pattern S-082)
* `StrictModeGuard` (domaine `capsule.config`) — 6 resolve* branchés,
  `noop` explicite non concerné
* DSL 4 sources pattern : ENV (`CAPSULE_*`) < gradle.properties
  (`capsule.*`) < YAML (`capsule-context.yml`) < CLI (`-P*`). Merger
  `CapsuleConfigMerger` + helpers `mergeStr/mergeInt/mergeDouble/mergeBoolean`
  + `mergeStrList` (List<String> comma-separated).
* `CompositeContext` (codebase-contracts 0.1.0) — canaux EAGER/RAG/Graphify/
  Docs + `ChannelBudget`. `CapsuleContextBuilder` (domaine `capsule.context`).
  `DocContextLoader` (S-093) — résout fichiers pré-matchés, concatène trié par
  nom, tronque `budget.docsTokens` (10%). `PedagogicalScenarioLoader` (S-094/095,
  mirror `DocContextLoader`) — valide metadata.json + extrait AsciiDoc scenario.
* `CapsulePipelineGraph` (koog 1.0.0, domaine `capsule.pipeline`) — 3 nœuds
  propose/validate/generate, `CapsulePromptBuilder` port pédagogique,
  `DefaultCapsulePromptBuilder` (étendu S-095 instruction line scenario
  conditionnelle `contains("Pedagogical Scenario")`).
* `ContextChannel` sealed N0 (5 variants) — PAS de canal `spd` N0.
  Extension capsule-local `CapsuleContext.scenarioSection` (pas de bump N0).
* `CapsuleContext` (S-095) — 3 champs `channels`/`rendered`/`scenarioSection`,
  invariant `rendered.isBlank() == (channels.isEmpty() && scenarioSection.isBlank())`.
  `CapsuleContextBuilder.build(composite, budget, scenarioSection="")` — append
  scenario section après channels, trim sur scenarioSection.