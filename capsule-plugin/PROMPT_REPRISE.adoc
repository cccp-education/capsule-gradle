= PROMPT_REPRISE — Session 037 : CAP-24 Burn-in sous-titres dans le WebM final

== Contexte

Session 036 — CAP-23 Sous-titres SRT/VTT terminé.
SubtitleFormat enum + SubtitleEntry + SubtitleGenerator interface.
SrtGenerator + VttGenerator + SubtitleTimingCalculator.
DSL subtitleEnabled/subtitleFormat dans 4 sources.
Wiring CapsuleVideoTask: generateSubtitles + injectSubtitleTrack.
6 Cucumber @subtitles PASSED. 43/43 Cucumber PASSED, 0 failures.
38 nouveaux tests unitaires (SubtitleModels/Srt/Vtt/TimingCalculator).

== Livrables session 036

1. **SubtitleModels.kt** — SubtitleFormat (SRT/VTT) + SubtitleEntry + SubtitleGenerator
2. **SrtGenerator.kt** — Format SubRip (index + HH:MM:SS,mmm --> HH:MM:SS,mmm + texte)
3. **VttGenerator.kt** — Format WebVTT (WEBVTT header + HH:MM:SS.mmm --> HH:MM:SS.mmm)
4. **SubtitleTimingCalculator.kt** — Timestamps cumulatifs depuis durées audio
5. **DSL** — subtitleEnabled + subtitleFormat dans Extension/Config/Merger/Plugin/Scaffold
6. **Wiring** — CapsuleVideoTask.generateSubtitles + injectSubtitleTrack + resolveSubtitleGenerator
7. **Cucumber @subtitles** — 6 scénarios (config, SRT, VTT, track injection)

== État git

8 fichiers modifiés, 8 fichiers nouveaux. Aucun commit.
Compile: BUILD SUCCESSFUL.
Cucumber: 43 PASSED, 0 FAILED, 17 SKIPPED.
Secrets: 0 trouvé.

== Prochaines étapes

. CAP-24 Burn-in sous-titres — Incrustation SRT/VTT dans le WebM via FFmpeg subtitles filter
. FFmpeg `subtitles` filter: `-vf "subtitles=sub.srt"` pour burn-in hard subs
. Option `burnInSubtitles` dans le DSL (Boolean, default false)
. Intégration dans CapsuleVideoTask.mixAudioWithVideo ou nouvelle étape post-capture
. Cucumber @subtitles @integration scenarios avec vérification visuelle (placeholder)
