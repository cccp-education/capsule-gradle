= PROMPT_REPRISE — Session 025 : Cucumber @integration Manim E2E complet

== Contexte

Session 024 — ManimSlideReplacer : interface + Impl + NoOp + factory + wiring dans CapsuleVideoTask.execute().
computeSlideDurationsWithManim() enrichi (priorité Manim video > TTS audio > default).
154 tests unitaires GREEN (21 nouveaux). 1 scénario Cucumber @manim (slide replacement).

== Livrables session 024

1. **ManimSlideReplacer.kt** — Interface + Impl (remplacement HTML section par video embed) + NoOp + factory
2. **computeSlideDurationsWithManim()** — Priorité Manim duration > TTS audio > default
3. **ManimSlideReplacerTest** — 14 tests TDD GREEN
4. **ComputeSlideDurationsWithManimTest** — 4 tests TDD GREEN
5. **ManimSlideReplacementWiringTest** — 3 tests wiring GREEN
6. **CapsuleVideoTask.execute()** — Wiring ManimSlideReplacer + manimDurations + deck replacement
7. **capsule.feature** — Nouveau scénario @manim slide replacement
8. **CapsuleStepDefinitions** — Step ManimSlideReplacer validation

== Prochaines étapes (priorité décroissante)

. **Cucumber @integration Manim** — Scénario Gherkin end-to-end complet (ManimEngine noop + ManimVideoMixer noop + ManimSlideReplacer noop + output validé)
. **ManimEngine configurable** — Scripts Manim en dehors de `src/manim/`, paramètres qualité
. **Parralélisation Manim** — ManimEngine.render() en parallèle pour les slides MANIM multiples
. **EPIC CAP-18** — Manim slide path replacement + probe duration (COMPLETE)

== État git

main → modifications non commitées (session 024 contenu)
Unit tests: BUILD SUCCESSFUL (154 unit = GREEN)