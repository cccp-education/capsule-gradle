= PROMPT_REPRISE — Capsule — Session 073
:session: 073
:date: 2026-08-07

== Session 072 terminée — CR-5 DRY CapsuleConfigMerger

CR-5 ✅ TERMINÉ — refactor DRY du `CapsuleConfigMerger.kt` (322→302 lignes, -19 net).
10 méthodes `mergeXxxConfig`/`mergeXxxConfigNoYaml` → 5 méthodes unifiées + 4 helpers
génériques (`mergeStr/mergeInt/mergeDouble/mergeBoolean`). `merge()` dispatch sur
`yaml: T?` nullable (`if (yamlLoaded) yamlConfig else null`). Sémantique préservée :
`toBoolean()` lenient, heuristic `isNotBlank()` pour strings, Bool/Int/Double YAML>Props>ENV.

Méthode baby-step TDD/BDD :
- **Unit** : +13 tests NoYaml dans `CapsuleConfigMergerTest` (48 total, couvre les 5
  sections en yamlLoaded=false + edge case blank YAML fallback).
- **Cucumber** : +1 scénario `@config @no-yaml` "gradle.properties are used when no
  capsule-context.yml is present" + 1 step Given.

Métriques : 396 unit tests VERT (+12 vs S-071 384), 48/48 ConfigMerger PASS, 1 cucumber
@no-yaml PASS. 1 échec cucumber `Burn-in E2E @integration` préexistant (ffmpeg exit 183,
environnement, non lié CR-5).

Rapport : `.agents/sessions/072-cr5-dry-config-merger.adoc`.

== Prochaine session — 073

Backlog capsule restant (par priorité) :

. 🟡 **CR-8** — Logging structuré CapsulePlugin l.36-44 (1 ligne monolithic 13
  placeholders → 4-5 `logger.lifecycle` par section TTS/Capture/Subtitles/Manim).
  _Fix_ : extraire `logResolvedConfig(mergedConfig)` méthode privée, 4-5 appels
  `logger.lifecycle` thématiques. _Tests_ : unitaire sur log capture.

. 🟡 **CR-9** — Robust HTML parsing (HtmlSectionParser au lieu de regex raw dans
  `injectAudioSequentialFallback`). _Fix_ : réutiliser `HtmlSectionParser` existant.
  _Tests_ : unitaires HTML malformé.

. 🟡 **CR-10** — CI optimization cucumberTest `onlyIf { hasProperty("runCucumber") || CI }`.
  _Fix_ : `cucumberTest.onlyIf { project.hasProperty("runCucumber") || System.getenv("CI") == "true" }`.
  _Impact_ : skip cucumber en local rapide, run en CI. Dettes : isoler `Burn-in E2E @integration`
  (ffmpeg exit 183 préexistant).

. 🟢 **CAP-27** — VTT burn-in (FFmpeg subtitles filter avec VTT, actuellement SRT seulement).

. 🟢 **CAP-28** — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%).

. 🟢 **CAP-29** — Pipeline Traduction Vidéo Multi-Langue (cadré S-097, 6 US ~20 pts,
  dépend CR-2 ✅ + CR-3 ✅ → débloqué). _Domaine cible_ : `capsule.multilang`.

. 🟠 **CAP-CR3-1** — CI `full` avec vrais binaires + probe de durée (CR-GLOBALE-003).
. 🟠 **CAP-CR3-2** — Mode strict anti-NoOp (`capsule.strictMode` DSL).
. 🟠 **CAP-CR3-3** — Décision capture ScreenshotCaptureImpl vs PlaywrightCaptureImpl.
. 🟡 **CAP-CR3-4** — Convention Gradle explicite resolveScriptDir/resolveDeckDir.

== À corriger à la clôture (dettes techniques gouvernance)

* BACKLOG.adoc : CR-5 marqué `✅ TERMINÉ (S-072)`.
* INDEX.adoc : ajouter session 072 dans la table + roadmap EPICs CR.
* TABLE AGENTS.adoc racine : mettre à jour entrée capsule (S-071→S-072).

== Contexte technique

- Build migré vers conventions plugins (education.cccp.build.* 0.0.2)
- Version capsule : 0.0.1-SNAPSHOT (non publié Maven Central)
- Version slider : 0.0.16 (mavenLocal, attente publication Central)
- BOM workspace-bom : Central 0.0.16, mavenLocal 0.0.17
- 396 unit tests VERT (capsule) + 895 unit (slider, post-SLD-11)
- Domaines DDD capsule : `capsule.feed`, `capsule.capture`, `capsule.tts`, `capsule.subtitles`, `capsule.manim`, `capsule.config`, `capsule.plugin`
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only : code, Gherkin, comments — no French
- `./gradlew test` VERT (0 FAIL, 0 SKIP)
- `./gradlew cucumberTest -PrunCucumber` : 1 échec `Burn-in E2E @integration` préexistant (ffmpeg)
- EPICs clos capsule : CAP-0→25 + CAP-MODEL + K-1 + DRY-1→3 + CR-1 + CR-2 + CR-3 + CR-4 + CR-5 + CR-6 + CR-7
- Loi de l'Économie d'Encre (AGENT.adoc) : vérifier artefact résultat avant opération coûteuse