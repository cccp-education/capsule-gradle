= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 094 (partiel) — CAP-SPD US-0 cadrage (audit CapsulePipelineGraph/CapsuleContextBuilder, contrat SPD format-agnostic, 7 décisions architecturales BACKLOG). Capsule public OSS : valide `type=="SPD"` uniquement, PAS `source` (pas de couplage borough producteur). US-1 `SpdContext`/`SpdContextLoader` 10/10 unit GREEN (object pur mirror `DocContextLoader`, extrait AsciiDoc SPD + valide metadata.json K-2 enveloppe).
_Prochaine session_ : 094 (suite) — CAP-SPD US-2 `CapsuleContext.spdSection` + `CapsuleContextBuilder` étendu + `DefaultCapsulePromptBuilder` étendu, baby-step TDD RED → GREEN → REFACTOR ~6 unit.

== Mission session 094 (EN COURS — US-0 ✅, US-1 10/10 GREEN)

. **CAP-SPD US-0 Cadrage TERMINÉ** (1 pt) — audit fondé sur le code, 7 décisions
  architecturales documentées BACKLOG. Correction majeure : capsule public OSS
  format-agnostic (valide `type=="SPD"`, pas `source` — pas de couplage borough
  producteur). `metadata.json` K-2 = enveloppe provenance (7 champs), payload
  pédagogique dans l'AsciiDoc compagnon. N0 `ContextChannel` sealed → extension
  capsule-local `CapsuleContext.spdSection` (pas de bump N0). SPD absent →
  vidéo non bloquée (fallback no-op). Wiring sur
  `collectCapsuleAugmentedContext -Pspd.file` (cohérence CAP-DOCCONTEXT).

. **CAP-SPD US-1 10/10 GREEN** (3 pts) — `SpdContext` data class (immutable :
  `objectives: List<String>`, `duration: String`, `prerequisites:
  List<String>`, `modalities: String`, `sessionTitle: String`, `module:
  String`, invariant fail-fast si sessionTitle blank) + `SpdContextLoader`
  (object pur, mirror `DocContextLoader`) — `load(metadataFile: File?,
  adocFile: File, maxTokens: Int): String`. Valide metadata.json (`type=="SPD"`
  uniquement, pas `source`) si présent, extrait sections AsciiDoc (`== Objectifs`,
  `== Prérequis`, `== Modalités d'évaluation`, attributs `:module:`/`:title:`),
  rend `==== SPD Pedagogical Context (spd)\nSession: ...\nModule: ...\n
  Objectives: ...\nDuration: ...\nPrerequisites: ...\nModalities: ...`, tronque
  via `ContextChannel.Docs.truncateToTokens(maxTokens)` (réutilise le variant
  Docs, pas de nouveau variant sealed N0). Missing file gracefully skipped.
  SPD absent → vidéo non bloquée. Baby-step TDD : RED → GREEN (10/10 PASS).

. **CAP-SPD US-2** (2 pts) — `CapsuleContext` 3ème champ `spdSection: String
  = ""` (invariant ajusté), `CapsuleContextBuilder.build(composite, budget,
  spdSection="")` étendu, `DefaultCapsulePromptBuilder.buildGeneratePrompt`
  instruction line SPD. ~6 unit.

. **CAP-SPD US-3** (2 pts) — Wiring `collectCapsuleAugmentedContext -Pspd.file`
  (`spdFile: RegularFileProperty @Optional`, `ContextConfig.spdFile`,
  `mergeContextConfig` étendu `mergeStr`, `CapsuleExtension.spdFile`,
  4 sources). 2 functional.

. **CAP-SPD US-4** (2 pts) — Cucumber `capsule_spd_context.feature`
  `@context @spd` (3 scénarios), runner dédié `CapsuleSpdContextCucumberRunner`
  (pattern S-082), steps préfixés "spd context" (bug S-088),
  `CucumberTaskSpec("cucumberTestSpdContext")` câblé.

== Contexte reprise session 094 (CAP-SPD)

- EPIC CAP-SPD deuxième de la séquence MVP0 (P0, axe contexte augmenté).
  5 US ~10 pts : US-0 cadrage ✅, US-1 `SpdContext`/`SpdContextLoader`,
  US-2 `CapsuleContext`/`CapsuleContextBuilder`/`DefaultCapsulePromptBuilder`
  étendus, US-3 Wiring `collectCapsuleAugmentedContext -Pspd.file`,
  US-4 Cucumber BDD.
- 694 unit VERT + 47 functional VERT + 4 cucumber dédiés GREEN (baseline S-093).
- Domaines DDD capsule (8) : `capsule.feed` + `capsule.ci` +
  `capsule.multilang` + `capsule.ai` + `capsule.context` +
  `capsule.pipeline` + `capsule.support` + `capsule.i18n`.
  (`capsule.config` = package transverse, pas un domaine DDD dédié.)
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Modifications S-083→093 committées (git propre). S-094 = cadrage, pas de code.
- Borough Détroit actif (CAP-DOCCONTEXT terminé S-093, CAP-SPD en cours S-094).

== EPICs restants (séquence MVP0)

* CAP-SPD (P0, EN COURS) — consommationmétadonnées SPD, 5 US ~10 pts
* CAP-CR3-3 (P0) — branchement ScreenshotCapture, 5 US ~13 pts
* CAP-CR3-1 (P0, maintenance) — CI `full` avec vrais binaires + probe durée
* CAP-MP4 (P1) — export MP4/H.264, 4 US ~4 pts
* CAP-AUDIO (P1) — post-production audio, 5 US ~10 pts
* CAP-GLOSSARY (P1) — injection terminologique, 4 US ~6 pts
* CAP-PROVENANCE (P1) — audit context, 3 US ~5 pts
* CAP-CHAPITRE (P2) — intro/outro + chapter markers, 5 US ~10 pts
* CAP-TRANSCRIPT (P2) — reverse pipeline article AsciiDoc, 5 US ~10 pts
* CAP-PODCAST (P2) — extraction audio-only, 3 US ~4 pts
* CAP-PREVIEW (P2) — mode dry-run, 3 US ~4 pts
* CAP-CR3-4 (P2, maintenance) — convention Gradle explicite resolveScriptDir/resolveDeckDir
* CAP-27 (P2, maintenance) — VTT burn-in
* CAP-28 (P2, maintenance) — coverage gaps

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 694 unit tests VERT (S-093 baseline) — vérifier avant de coder
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082 :
  `@ConfigurationParameter(FEATURES_PROPERTY_NAME)` du CucumberTestRunner
  écrase la system property cucumber.features) — pattern
  `CapsuleContextCucumberRunner` / `CapsuleDocContextCucumberRunner` /
  `CapsuleSpdContextCucumberRunner` (NEW S-094)
* Glue `capsule.scenarios` partagé : tous les steps doivent avoir des
  signatures uniques (bug S-088 `DuplicateStepDefinitionException`) —
  préfixer par feature (ex. "spd context")
* English only : code, Gherkin, commentaires
* Économie d'encre = skip WebM existant + probe > 0 (ffprobe durée > 0)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues, `resolveLanguage(project)` cascade
  `-Pcapsule.language` > DSL > `"en"`
* Runners cucumber dédiés : `CapsuleContextCucumberRunner`,
  `CapsuleContentAndVideosCucumberRunner`, `CapsuleDistribCucumberRunner`,
  `CapsuleArchBoundaryCucumberRunner`, `CapsuleStrictModeCucumberRunner`,
  `CapsuleDocContextCucumberRunner`,
  `CucumberIntegrationTestRunner` (pattern S-082)
* `StrictModeGuard` (domaine `capsule.config`) — 6 resolve* branchés,
  `noop` explicite non concerné
* DSL 4 sources pattern : ENV (`CAPSULE_*`) < gradle.properties
  (`capsule.*`) < YAML (`capsule-context.yml`) < CLI (`-P*`). Merger
  `CapsuleConfigMerger` + helpers `mergeStr/mergeInt/mergeDouble/mergeBoolean`
  + `mergeStrList` (List<String> comma-separated).
* `CompositeContext` (codebase-contracts 0.1.0) — canaux EAGER/RAG/Graphify/
  Docs + `ChannelBudget`. `CapsuleContextBuilder` (domaine `capsule.context`).
  `DocContextLoader` (S-093) — résout fichiers pré-matchés, concatène trié par
  nom, tronque `budget.docsTokens` (10%). `SpdContextLoader` (NEW S-094 US-1,
  mirror `DocContextLoader`) — valide metadata.json + extrait AsciiDoc SPD.
* `CapsulePipelineGraph` (koog 1.0.0, domaine `capsule.pipeline`) — 3 nœuds
  propose/validate/generate, `CapsulePromptBuilder` port pédagogique,
  `DefaultCapsulePromptBuilder` (étendu US-2 instruction line SPD).
* `ContextChannel` sealed N0 (5 variants) — PAS de canal `spd` N0.
  Extension capsule-local `CapsuleContext.spdSection` (pas de bump N0).