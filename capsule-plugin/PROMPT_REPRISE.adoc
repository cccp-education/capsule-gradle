= PROMPT_REPRISE — capsule-gradle Session 007

_Mission_ : Publish mavenLocal + cross-borough verification (engine N3 integration)
_Statut_ : A FAIRE
_Date_ : à définir

== Contexte

Session 006 TERMINE (36/36 PASS — 25 unit + 6 functional + 5 Cucumber) :
- CAP-6 : capsuleCompositeContext task — export JSON (capsule-capsule-context.json)
  compatible engine N3 avec metadonnees completes (deckName, slideCount, videos, viewport, distrib, slides, TTS)
- CAP-7 : capsuleDistrib task — recadrage FFmpeg 9:16 (1080x1920) avec fallback copy si ffmpeg absent
- CapsuleExtension enrichie : ffmpegExecutablePath, distribOutputWidth/Height, compositeContextOutputFile
- 6 nouveaux tests unitaires (CapsuleDistribTaskTest + CapsuleCompositeContextTaskTest)
- 2 tests fonctionnels supplementaires

== Sujets Session 007

- PublishToMavenLocal : verifier que le plugin est importable par d'autres projets
- Integration engine N3 : verifier que le JSON capsule-context.json est parsable
  par assembleCompositeContext (si engine deja integre)
- Cross-borough verification : le contrat slider→capsule est stable
- Cleanup eventuel : warnings, deprecations Gradle 10
- Verification git status et absence de secrets

== Pipeline complet (rappel)

  AsciiDoc → slider-gradle → deck.html + *-script.txt
  → capsulescript → capsulebuild (TTS)
  → capsulevideo (Playwright capture → .webm)
  → capsuledistrib (FFmpeg crop 9:16)
  → capsulecompositecontext (JSON → engine N3)

== Taches Gradle (resume)

  capsulescript / capsulebuild / capsulevideo / capsuledistrib / capsulecompositecontext
