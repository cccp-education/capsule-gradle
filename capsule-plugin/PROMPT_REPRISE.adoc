= PROMPT_REPRISE — Session 034 : Vérifier Cucumber WebM + Kover + décision EPIC

== Contexte

Session 033 — 3 bugs corrigés + CI template créé. pushConfigIntoExtension respecte DSL > merge,
NoOpPlaywrightCapture produit WebM EBML valide, CI `.github/workflows/test.yml` aligné quizz-benchmark.

== Livrables session 033

1. **CI Template** — `.github/workflows/test.yml` (JDK 24, `./gradlew check`, `working-directory: capsule-plugin`)
2. **Bug-fix DSL > merge** — `CapsuleConventions` snapshot + `pushConfigIntoExtension` guard convention.
   `CapsuleModels.kt` + `CapsulePlugin.kt` + `CapsulePluginTest.kt` modifiés.
3. **Bug-fix NoOp WebM** — `MINIMAL_WEBM` ByteArray EBML header. `PlaywrightManager.kt` + `CapsulePluginTest.kt` modifiés.

== État git

Modifications non commitées (session 033)
Unit tests: 211 PASS, 0 failures
Cucumber: 2/3 @integration PASS (Audio constraint + Output constraint), WebM en attente rebuild propre
Compile: BUILD SUCCESSFUL

== Prochaines étapes

. Vérifier Cucumber WebM avec `./gradlew :clean :jar :cucumberTest` (rebuild propre)
. Générer rapport Kover (`./gradlew :koverHtmlReport`) pour identifier gaps de couverture
. Décider prochain EPIC: CAP-22 TTS multi-langue ou CAP-23 sous-titres selon gaps Kover
