= PROMPT_REPRISE — Session 029 : ManimEngine Real Integration

== Contexte

Session 028 — Manim Parallel Render (CAP-20) terminé. 195 unit tests GREEN. ManimParallelRenderer interface + NoOp + Impl (thread pool) + factory + CapsuleVideoTask wiring refactor (renderAll parallèle → mux séquentiel). Config DSL parallelRender propagé dans 4 sources (ENV < props < YAML < CLI) + scaffold. 22 scénarios Cucumber.

== Livrables session 028

1. **ManimParallelRenderer.kt** — Interface + NoOpManimParallelRenderer + ManimParallelRendererImpl (Callable + thread pool, parallelism configurable)
2. **14 unit tests** ManimParallelRendererTest (NoOp, Impl, factory, edge cases)
3. **CapsuleVideoTask refactor** — renderAll() parallèle + mux séquentiel + slide replacement via renderedFiles map
4. **Config DSL parallelRender** — ManimConfig.parallelRender + CapsuleExtension.manimParallelRender + 4 sources merger + scaffold + 8 tests
5. **Scenario Cucumber @manim @parallel** — 2 step definitions

== Prochaines étapes (priorité décroissante)

. **ManimEngine real integration** — Scénario @manim-real avec vrai binaire manim
. **EPIC CAP-18** — Manim slide path replacement + probe duration (COMPLETE)
. **EPIC CAP-21** — Parallel capture for HTML slides (reuse ManimParallelRenderer pattern?)

== État git

Modifications non commitées (session 028 : ManimParallelRenderer + parallelRender config + 22 tests)
Unit tests: 195 GREEN
Cucumber: 22 scénarios (5 @config @manim + 3 @manim + 1 @parallel + 13 autres)