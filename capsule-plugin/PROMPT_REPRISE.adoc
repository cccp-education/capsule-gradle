= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 108 — CAP-PROVENANCE US-1 + US-2 + Clôture EPIC
CAP-PROVENANCE TERMINÉ 3/3 US. US-1 : `ProvenanceSource`/`ProvenanceChannel`/
`ContextProvenance`/`ProvenanceTracker` (4 fichiers prod, domaine
`capsule.context`) + `CapsuleContextBuilder.build()` étendu `tracker:
ProvenanceTracker? = null` (prune aux canaux survivants, backward compat
null). Jackson `totalChars`/`totalTokens` exposés `@get:JsonProperty`.
15 unit GREEN. US-2 : `CollectCapsuleAugmentedContextTask` `@OutputFile
provenanceFile` + `trackSources` 5 canaux + log `CAPSULE PROVENANCE →` +
2 functional + 3 cucumber `capsule_provenance.feature` + runner dédié +
`CucumberTaskSpec("cucumberTestProvenance")` 13ème. 2 fix TDD : missing
files skipped réel + scenarioFile répertoire. `check` BUILD SUCCESSFUL in
15m52s : 834 unit + 62 functional + 3 cucumber GREEN. Zéro régression,
zéro secret.
_Prochaine session_ : 109 — CAP-GLOSSARY US-0 Cadrage EPIC Injection
terminologique (P1, 4 US ~6 pts).

== Contexte reprise session 109 (post CAP-PROVENANCE 3/3)

- EPIC CAP-GLOSSARY (P1) — Injection terminologique, 4 US ~6 pts. US-0
  cadrage à faire (vision déjà dans BACKLOG : vocabulaire AFNOR/REAC que le
  LLM généricise, glossaire terme→définition injecté dans le contexte +
  instruction prompt "Use official terminology"). Reste US-0 cadrage →
  US-1 `GlossaryContext`+`GlossaryLoader` (6 unit) → US-2
  `CapsulePromptBuilder` étendu (4 unit) → US-3 wiring+functional+cucumber.
- Domaine `capsule.context` étendu (canal `glossary`). Dépendances :
  CAP-ARCH-2 ✅ (`CapsuleContextBuilder`/`CollectCapsuleAugmentedContextTask`),
  CAP-ARCH-3 ✅ (`DefaultCapsulePromptBuilder`), CAP-PROVENANCE ✅ (le
  tracking des sources provenance doit être étendu au canal glossary —
  pattern `trackSources` S-108).
- 834 unit VERT (S-108 baseline, inchangé depuis). 62 functional VERT.
  12 cucumber dédiés GREEN (`cucumberTestProvenance` 13ème `CucumberTaskSpec`
  câblé). `check` BUILD SUCCESSFUL in 15m52s (S-108).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4).
- Patterns de référence : CAP-PROVENANCE S-107/108 (extension capsule-local
  pas N0, `tracker` optional backward compat, runner dédié S-082, steps
  préfixés S-088), CAP-DOCCONTEXT S-093 (`ContextConfig` 4 sources +
  `mergeStrList` + `DocContextLoader` mirror), CAP-SPD S-094/095
  (`PedagogicalScenarioLoader` mirror, `scenarioFile` 4 sources).

== Cible US-0 cadrage (détails BACKLOG)

- Cadrage EPIC CAP-GLOSSARY : vision, format glossaire (AsciiDoc/CSV/JSON),
  découpage US. MàJ INDEX/BACKLOG/PROMPT_REPRISE. Zéro code modifié —
  session de gouvernance pure (pattern S-057/S-063/S-076/S-083/S-092/S-094/
  S-097/S-102/S-107).
- Points à auditer (extrapolés du pattern CAP-SPD/CAP-DOCCONTEXT) :
  `CompositeContext` N0 (5 canaux EAGER/RAG/Graphify/Docs/Resource — le
  glossaire est-il un 6ème canal N0 ou une extension capsule-local comme
  `scenarioSection` CAP-SPD-2 ?), `CapsuleContextBuilder.merge` (rend les
  canaux N0 seulement — le glossaire suit-il le pattern `scenarioSection`
  append ou un canal réel ?), `DefaultCapsulePromptBuilder.buildGeneratePrompt`
  (instruction "Use official terminology" conditionnelle), `ContextConfig`
  (2 champs `docsGlobs`/`scenarioFile` — ajout `glossaryFile` ?),
  `CapsuleConfigMerger` (`mergeStr` pour `glossaryFile`, 4 sources
  ENV < props < YAML < CLI), `CollectCapsuleAugmentedContextTask`
  (`glossaryFile` `@InputFiles @Optional` + `resolveGlossarySection` +
  provenance tracking `trackSources`), 13 `CucumberTaskSpec` existants
  (`cucumberTestGlossary` 14ème à câbler US-3).

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 834 unit VERT (S-108 baseline), 62 functional VERT, 12 cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) +
  steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues
* Runners cucumber dédiés (12) — `cucumberTestGlossary` à créer US-3
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers
  `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` +
  `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat` (enums)
* CAP-GLOSSARY = canal contexte supplémentaire (pas de LLM/réseau/FFmpeg
  — load fichier glossaire + troncation budget + prompt instruction)
