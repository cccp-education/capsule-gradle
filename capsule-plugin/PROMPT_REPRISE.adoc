= PROMPT_REPRISE — Capsule — Session 075
:session: 075
:date: 2026-08-07

== Session 074 terminée — CR-9 Robust HTML parsing

CR-9 ✅ TERMINÉ — `HtmlSectionParser.findTopLevelSectionOpenTags` (38 lignes,
domaine `capsule`). Nouvelle méthode réutilisant le depth-tracking existant
(skip nested + self-closing), retourne les `MatchResult` des tags d'ouverture
top-level avec positions. `injectAudioSequentialFallback` (`CapsuleVideoTask.kt`)
: 2 lignes regex raw `Regex("""<section\b[^>]*>""")` supprimées → 1 ligne
`HtmlSectionParser.findTopLevelSectionOpenTags`. DRY — réutilise `HtmlSectionParser`
(DRY-2 S-031). Bug fix : vertical stacks (sections imbriquées) n'étaient pas
skippées par la regex raw → `data-audio` injecté sur sous-sections.

Méthode baby-step TDD/BDD :
- **Unit** : +8 tests `HtmlSectionParserTest.findTopLevelSectionOpenTags`
  (flat, nested, self-closing, attributes, empty, malformed, positions,
  multiline). +1 test `CapsuleVideoTaskTest.sequential fallback skips nested
  sections in vertical stack` (data-audio count = 2, pas 4). 414 unit VERT
  (+9 vs S-073 405).
- **Cucumber** : +1 scénario `@audio @sequential` (Sequential fallback skips
  nested sections in vertical stack) + 2 step defs (Given nested vertical
  stack sections, Then exactly {int} data-audio attributes).

Rapport : `.agents/sessions/074-cr9-robust-html-parsing.adoc`.

== Prochaine session — 075

Backlog capsule restant (par priorité) :

. 🟡 **CR-10** — CI optimization cucumberTest `onlyIf { hasProperty("runCucumber") || CI }`.
  _Fix_ : `cucumberTest.onlyIf { project.hasProperty("runCucumber") || System.getenv("CI") == "true" }`.
  _Impact_ : skip cucumber en local rapide, run en CI. Dettes : isoler `Burn-in E2E @integration`
  (ffmpeg exit 183 préexistant).

. 🟢 **CAP-29** — Pipeline Traduction Vidéo Multi-Langue (cadré S-097, 6 US ~20 pts,
  dépend CR-2 ✅ + CR-3 ✅ → débloqué). _Domaine cible_ : `capsule.multilang`.

. 🟠 **CAP-ARCH** — Collaboration cross-borough slider+codebase (content + i18n).
  6 US ~19 pts (cadré S-073). _À planifier après CAP-29_.

. 🟢 **CAP-27** — VTT burn-in (FFmpeg subtitles filter avec VTT, actuellement SRT seulement).
. 🟢 **CAP-28** — Coverage gaps (PiperTtsEngine 31%, ManimEngineImpl 25%, ScreenshotCaptureImpl 26%).

== À corriger à la clôture (dettes techniques gouvernance)

* INDEX.adoc : ajouter session 074 dans la table + roadmap EPICs CR (CR-9 ✅).
* TABLE AGENTS.adoc racine : mettre à jour entrée capsule (S-073→S-074).

== Contexte technique

- Build migré vers conventions plugins (education.cccp.build.* 0.0.2)
- Version capsule : 0.0.1-SNAPSHOT (non publié Maven Central)
- Version slider : 0.0.16 (mavenLocal, attente publication Central)
- BOM workspace-bom : Central 0.0.16, mavenLocal 0.0.17
- 414 unit tests VERT (capsule) + 895 unit (slider, post-SLD-11)
- Domaines DDD capsule : `capsule.feed`, `capsule.capture`, `capsule.tts`, `capsule.subtitles`, `capsule.manim`, `capsule.config`, `capsule.plugin`
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only : code, Gherkin, comments — no French
- `./gradlew test` VERT (0 FAIL, 0 SKIP)
- `./gradlew cucumberTest -PrunCucumber` : bloque (scénarios @integration ffmpeg, préexistant)
- EPICs clos capsule : CAP-0→25 + CAP-MODEL + K-1 + DRY-1→3 + CR-1→CR-9 + CR-2→CR-7
- Loi de l'Économie d'Encre (AGENT.adoc) : vérifier artefact résultat avant opération coûteuse