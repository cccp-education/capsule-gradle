= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 112 — CAP-GLOSSARY US-3 Cucumber BDD + clôture EPIC
CAP-GLOSSARY TERMINÉ 4/4 US. Feature `capsule_glossary_context.feature`
`@context @glossary` (3 scénarios : glossary injecté → section
`==== Official Glossary (glossary)` + termes `competence transversale`/
`evaluation formative` présents + bullet `unrelated` ignoré ; absent →
fallback no-op ; malformed sans `== Glossary` → gracefully skipped).
Runner dédié `CapsuleGlossaryContextCucumberRunner` (pattern S-082
`@SelectClasspathResource` + filter `@context and @glossary and not @wip
and not @integration`). `CapsuleGlossaryContextSteps` (10 step defs
préfixés "glossary context", pattern `CapsuleScenarioContextSteps` mirror
— bug S-088 glue `capsule.scenarios` partagée). `CucumberTaskSpec
("cucumberTestGlossaryContext")` 14ème câblé `build.gradle.kts` (13ème
cucumber dédié). Baby-step BDD RED → GREEN (3/3 scénarios PASSED en 55s)
→ REFACTOR (`check` BUILD SUCCESSFUL in 28m52s, 857 unit VERT baseline
S-111 préservée — pure BDD zéro production modifié, 64 functional VERT
baseline préservée, cucumberTest global SKIPPED par CR-10, Kover VERT,
zéro régression, zéro secret). Domaine `capsule.context` clos (13 fichiers
prod, 3 loaders). EPIC CAP-GLOSSARY TERMINÉ 4/4 US.
_Prochaine session_ : 113 — Borough Détroit en maintenance. EPICs
restants : CAP-CHAPITRE (P2), CAP-TRANSCRIPT (P2), CAP-PODCAST (P2),
CAP-PREVIEW (P2), CAP-CR3-4 (P2), CAP-27 (P2 VTT burn-in), CAP-28 (P2
coverage gaps).

== Contexte reprise session 113 (post CAP-GLOSSARY US-3 S-112)

- EPIC CAP-GLOSSARY (P1) — Injection terminologique, 4 US ~6 pts.
  TERMINÉ 4/4 US : US-0 cadrage ✅ S-109, US-1 data classes + loader +
  builder + prompt builder étendus ✅ S-110 (857 unit VERT +23), US-2
  wiring + functional ✅ S-111 (64 functional VERT +2), US-3 cucumber
  BDD ✅ S-112 (3 scénarios `capsule_glossary_context.feature`).
- Domaine `capsule.context` clos (13 fichiers prod : 3 loaders
  `DocContextLoader`/`PedagogicalScenarioLoader`/`GlossaryLoader` +
  `CapsuleContextBuilder`/`CapsuleContext`/`CollectCapsuleAugmented
  ContextTask`/`PedagogicalScenario`/`ProvenanceSource`/`ProvenanceChannel`/
  `ContextProvenance`/`ProvenanceTracker`/`GlossaryEntry`/`GlossaryContext`).
- 857 unit VERT (S-112 baseline, inchangé — US-3 = pure BDD). 64
  functional VERT (inchangé). `check` BUILD SUCCESSFUL in 28m52s (S-112).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly), codebase-plugin 0.0.5 (implementation),
  koog-agents 1.0.0, BOM 0.0.16, codebase-contracts 0.1.0,
  i18n-contracts 0.0.2.
- 13 cucumber dédiés GREEN (12 runners + `cucumberTestGlossaryContext`
  13ème cucumber dédié, 14ème `CucumberTaskSpec` total avec integration).
  21 features, 18 runners, 18 steps.
- Borough Détroit en maintenance. Tous les EPICs P0/P1 terminés
  (CAP-29, CAP-ARCH, CAP-CR3-1/2/3, CAP-DOCCONTEXT, CAP-SPD,
  CAP-PROVENANCE, CAP-MP4, CAP-AUDIO, CAP-GLOSSARY). Reste P2 :
  CAP-CHAPITRE, CAP-TRANSCRIPT, CAP-PODCAST, CAP-PREVIEW, CAP-CR3-4,
  CAP-27, CAP-28.

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 857 unit VERT (S-112 baseline), 64 functional VERT, 13 cucumber dédiés GREEN
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) +
  steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si artefact existe et valide (AGENT.adoc Loi)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers
  `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` +
  `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat` (enums)
* 3 loaders domaine `capsule.context` : `DocContextLoader` (corpus
  documentaire, CAP-DOCCONTEXT), `PedagogicalScenarioLoader` (scénario
  pédagogique, CAP-SPD), `GlossaryLoader` (glossaire terminologique,
  CAP-GLOSSARY). Tous object pur, mirror pattern, `ContextChannel.Docs.
  truncateToTokens` pour la troncation budget.