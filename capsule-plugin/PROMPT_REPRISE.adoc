= PROMPT_REPRISE — Session 038 : CAP-25 Pipeline E2E avec burn-in (intégration complète)

== Contexte

Session 037 — CAP-24 Burn-in sous-titres terminé.
SubtitleBurnInService interface + SubtitleBurnInServiceImpl (FFmpeg subtitles filter) + NoOpSubtitleBurnInService.
DSL subtitleBurnIn dans 4 sources (Extension/Config/Conventions/Merger/Scaffold/Plugin).
Wiring CapsuleVideoTask: resolveSubtitleBurnInService + burnInSubtitlesIfEnabled (post-mixAudioWithVideo, séquentiel + parallèle).
10 tests unitaires SubtitleBurnInServiceTest PASS.
3 Cucumber @subtitles @burnin scénarios (config disabled default, config enabled DSL, service invocation).

== État git

9 fichiers modifiés, 2 fichiers nouveaux. Aucun commit.
Compile: BUILD SUCCESSFUL.
Unit tests: BUILD SUCCESSFUL.
Cucumber: cucumberTest task hang (pré-existant, non lié à CAP-24).
Secrets: 0 trouvé.

== Prochaines étapes

. CAP-25 Pipeline E2E avec burn-in — Test d'intégration complet du pipeline capsule avec burn-in activé
. Vérifier que le WebM final contient bien les sous-titres incrustés (vérification visuelle ou binaire)
. Cucumber @subtitles @burnin @integration scenario complet (deck → TTS → capture → mix → burn-in → WebM final)
. Option: support VTT burn-in (FFmpeg subtitles filter accepte aussi VTT)
. Option: style configurable (font size, couleur, position) via DSL
