= PROMPT_REPRISE — Session 024 : Manim slide path replacement + ManimEngine probeDuration

== Contexte

Session 023 — ManimVideoMixer : interface + Impl + NoOp + factory + wiring dans CapsuleVideoTask.execute().
Après ManimEngine.render(), le MP4 est muxé avec l'audio TTS du slide via ManimVideoMixer.mix().
133 tests unitaires GREEN (16 nouveaux). 2 Cucumber @manim.

== Livrables session 023

1. **ManimVideoMixer.kt** — Interface + ManimVideoMixerImpl (ffmpeg mux) + NoOpManimVideoMixer + MixerException
2. **CapsuleManager.resolveManimVideoMixer()** — Factory ffmpeg-path based
3. **CapsuleVideoTask.resolveManimVideoMixerInternal()** — Injection via propriété interne
4. **CapsuleVideoTask.execute()** — Wiring : ManimEngine.render() → ManimVideoMixer.mix(mp4, mp3, muxed.mp4)
5. **ManimVideoMixerTest** — 11 tests (NoOp, Impl unavailable, probeDuration, factory)
6. **ManimVideoMixerWiringTest** — 5 tests (MANIM+TTS, mixed slides, no audio, factory)
7. **CapsuleVideoTask.manimVideoMixer** — Champ interne pour tests
8. **Cucumber @manim** — 2e scénario : mux MP4+TTS validé

== Prochaines étapes (priorité décroissante)

. **Manim slide path replacement** — Remplacer le slide HTML Manim dans le deck final par la vidéo MP4 muxée (embed en data-uri ou remplacement dans le flux vidéo)
. **Manim slide duration** — Utiliser ManimEngine.probeDuration() pour la durée du slide Manim au lieu du slideDurationSeconds par défaut
. **Cucumber @integration Manim** — Scénario Gherkin end-to-end complet avec ManimEngine noop + ManimVideoMixer noop + validation output
. **ManimEngine configurable** — Scripts Manim en dehors de `src/manim/`, paramètres qualité

== État git

main → modifications non commitées (session 023 contenu — attendre permission)
Unit tests: BUILD SUCCESSFUL (133 unit = GREEN)
Cucumber: 12/12 (2 @manim GREEN)
Functional: 8/10 (2 environnementaux espeak/chromium absents)