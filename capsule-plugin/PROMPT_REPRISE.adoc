= PROMPT_REPRISE — Session 017 : Intégration sélective fork lion92 (suite)

== Contexte

Session 016 — 2/5 refactos intégrés (CapsuleModels + DRY). 3 restants.
Méthodologie : baby step TDD, Unit → Functional → Cucumber, un commit par refacto.

== Refactos restants

. **Parallélisme TTS** — CapsuleBuildTask.execute() : séquentiel → Executors.newFixedThreadPool(cores) + AtomicInteger + Future<File?>. Imports : java.util.concurrent.Executors, Future, AtomicInteger.
. **ScreenshotCapture** — ScreenshotCaptureImpl dans PlaywrightManager.kt : PNG screenshot + FFmpeg → WebM. Plus fiable et rapide que Playwright video. Signature duration: Double.
. **ManimEngine** — Nouveau fichier. Manim Python → MP4 → WebM via ProcessBuilder. isAvailable() pre-flight. Chemin standard videos/{sceneName}/{quality}/.

== Refactos complétés (session 016)

| Refacto | Commit | Tests |
| CapsuleModels (SlideType, espeak, manim config) | 4218a70 | 4 unit |
| DRY (companion, AUDIO_INJECT_SCRIPT, parseScript [manim]) | 72366b1 | 2 unit |

== Améliorations post-intégration

. createSingleSlideHtml() — lecture RAM + batch writing
. Split CapsuleManager.kt (~1000 lignes)
. Jackson mapper dans CapsuleCompositeContextTask (remplacer buildJsonString manuel)
. Cucumber [manim:SceneName] avec mock ou skip conditionnel

== État git

main → 72366b1 (3 commits non pushés, + les docs de session)
audit/lion92-fork → 6860a60 (local + remote)
Pré-existant : 7 tests UnknownPluginException (pluginUnderTestMetadata)