= PROMPT_REPRISE — Session 031 : P0 DRY + Resource Leak + Magic Number

== Contexte

Session 031 — 5 P0 items du BACKLOG traités. 3 DRY violations résolues (wavToMp3, ffprobeDuration, extractTopLevelSections), resource leak Playwright fixé (close() dans finally), magic number 4 rendu configurable via DSL.

== Livrables session 031

1. **AudioConversionUtil.kt** — Objet partagé wavToMp3 + isAvailable. PiperTtsEngine et EspeakTtsEngine déléguent.
2. **HtmlSectionParser.kt** — Objet partagé extractTopLevelSections. CapsuleVideoTask et ManimSlideReplacer déléguent. Regex corrigé pour ignorer self-closing tags.
3. **MediaProbeUtil.probeDuration** — ManimEngineImpl et ManimVideoMixerImpl déléguent à MediaProbeUtil. CapsuleVideoTask.ffprobeDuration() supprimée.
4. **Resource leak** — CapsuleVideoTask branche séquentielle : deckCapture.close() déplacé dans finally.
5. **Magic number 4** — CaptureConfig.parallelCaptureThreads + ManimConfig.parallelRenderThreads. Propagé dans CapsuleExtension, CapsulePlugin, CapsuleConfigMerger (4 sources), CapsuleScaffoldTask, CapsuleVideoTask.

== État git

Modifications non commitées (session 031 : 5 P0 items)
Unit tests: 195+ GREEN
Cucumber: 22 scénarios

== Prochaines étapes (priorité décroissante)

. **P1 println → logger** — PlaywrightCaptureImpl l.75, CapsuleManager.parseScript l.177-218
. **P2 Code mort isPlaceholder()** — Supprimer ou réutiliser
. **P3 lateinit var → lazy delegation**
. **P3 Fichier temporaire concat cleanup try/finally**
. **P3 ManimConfig.validate() → when explicite**
