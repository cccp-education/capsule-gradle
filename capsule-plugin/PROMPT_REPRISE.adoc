= PROMPT_REPRISE — Capsule — Session 083
:session: 083
:date: 2026-08-08

== Session 082 terminée — CAP-29.6 Documentation + dogfooding + clôture EPIC CAP-29 ✅

EPIC CAP-29 TERMINÉ 7/7 US. README master EN + README plugin mis à jour
(domaine `capsule.multilang`, table des tâches, convention nommage
`<deckName>_<lang>-deck.html|script.txt|webm`). Dogfooding réel consommateur
composite `includeBuild` (`/tmp/opencode/dogfood-multilang`) : 3 WebM générés
(fr/en/ar dont 1 RTL) en 3m07 (Piper fr/en + espeak, chromium Playwright).

Défaut de nommage découvert par le dogfooding : le rendu produisait
`<deckName>_<lang>-deck.webm` (dérivé du header script) au lieu du plan
`<deckName>_<lang>.webm` (`entry.outputVideo`), brisant l'économie d'encre.
Fix TDD : `CapsuleVideoOutputRenamer` (domaine `capsule.multilang`, object pur,
4 unit RED→GREEN) + wiring post-capture dans `GenerateCapsuleVideoAllLanguagesTask.renderEntry`.
Re-dogfooding : alignement `-deck.webm` → `_<lang>.webm` (3 langues) + économie
d'encre prouvée (`--rerun-tasks` → `0/3 rendered, 3 skipped`).

Livrables :
- `capsule/multilang/CapsuleVideoOutputRenamer.kt` (nouveau)
- `capsule/multilang/GenerateCapsuleVideoAllLanguagesTask.kt` (modifié)
- `capsule/multilang/CapsuleVideoOutputRenamerTest.kt` (nouveau, 4 unit)
- `README.md` + `capsule-plugin/README.md` (modifiés)

Tests : 464 unit VERT (+4) + 25 functional VERT + 6 cucumber multilang GREEN.
1 FAIL `@integration` burn-in préexistant (subtitle path `build/capsule/` vs
`outputDir=capsules`) — hors CAP-29, connu S-069/S-072.

== Prochaine session — 083 (au choix, non décidé)

. 🟠 **CAP-ARCH** — Cross-borough slider+codebase (6 US ~19 pts, cadré S-073,
  débloqué par CAP-29 ✅). Vision : création de contenu IA + i18n via les hubs
  (`slider` decks, `codebase` RAG/vibecoding) plutôt qu'appels LLM dispersés.
  Inspiration : document-gradle (`DocumentI18nBridge`), bakery-gradle
  (`ContentTranslationService`), plantuml-gradle (`PlantumlTranslationServiceAdapter`).
. 🔴 **CAP-CR3** — Solidité chaîne vidéo (empêcher « faux positif » exit 0 +
  livrable illisible). CAP-CR3-1 CI full vrais binaires + probe durée WebM ;
  CAP-CR3-2 mode strict anti-NoOp (`capsule.strictMode` DSL) ;
  CAP-CR3-3 décision capture ; CAP-CR3-4 convention `resolveScriptDir/resolveDeckDir`.
  Inclut le fix du burn-in `@integration` FAIL.

== Backlog restant

. 🟢 **CAP-27** — VTT burn-in. **CAP-28** — Coverage gaps (PiperTtsEngine 31%,
  ManimEngineImpl 25%, ScreenshotCaptureImpl 26%).
. 🐛 **Bug** — `CucumberTestRunner` ne lance que `capsule.feature` (S-075, à
  investiguer). 🐛 **Tooling** — filtre cucumber `@multilang` via system property
  non transmis au worker Gradle (S-082, passthrough `-PcucumberTags` à outiller).

== Contexte technique

- Build migré vers conventions plugins (education.cccp.build.* 0.0.2)
- BOM workspace-bom 0.0.16 (Central) + i18n-contracts 0.0.2 (Central)
- Version capsule : 0.0.1-SNAPSHOT (non publié Maven Central)
- Version slider : 0.0.16 (mavenLocal)
- 464 unit tests VERT (capsule) + 0 FAIL, 0 SKIP
- 25 functional tests VERT + 6 cucumber multilang GREEN
- `./gradlew check` VERT (cucumberTest SKIPPED sans -PrunCucumber)
- Domaines DDD capsule : `capsule.feed`, `capsule.capture`, `capsule.tts`,
  `capsule.subtitles`, `capsule.manim`, `capsule.config`, `capsule.plugin`,
  `capsule.ci`, `capsule.multilang` (clos 7/7 US)
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only : code, Gherkin, comments — no French
- EPICs clos capsule : CAP-0→25 + CAP-MODEL + K-1 + DRY-1→3 + CR-1→CR-10 + CAP-29
- Loi de l'Économie d'Encre (AGENT.adoc) : vérifier artefact résultat avant opération coûteuse
- `i18n-contracts` 0.0.2 consommé : `LanguageCatalog` (10 langues),
  `SupportedLanguage` (code/name/nativeName/rtl/localeTag)
