= PROMPT_REPRISE — Session 026 : ManimEngine configurable

== Contexte

Session 025 — Cucumber @integration @manim E2E complet. Scénario Gherkin end-to-end validé avec NoOp ManimEngine + NoOp ManimVideoMixer + ManimSlideReplacer + deck final <video>. CucumberIntegrationTestRunner créé (filtre `not @wip and (@integration or @manim)`). 154 tests unitaires GREEN. 4 new steps Manim E2E.

== Livrables session 025

1. **capsule.feature** — Nouveau scénario `@integration @manim` E2E pipeline (render → mux → replace → deck validé)
2. **CucumberIntegrationTestRunner.kt** — Runner Cucumber dédié @integration + @manim
3. **CapsuleStepDefinitions.kt** — 4 nouvelles steps Manim E2E (noop Manim config, Manim script .py, placeholder MP4, muxed MP4, replaced deck)
4. **CucumberIntegrationTestRunner** — Scénario `Manim E2E pipeline` PASSED (4/4 steps)

== Prochaines étapes (priorité décroissante)

. **ManimEngine configurable** — Scripts Manim en dehors de `src/manim/`, paramètres qualité, configuration via capsule-context.yml (ManimConfig déjà existant dans CapsuleConfig)
. **Parallélisation Manim** — ManimEngine.render() en parallèle pour les slides MANIM multiples
. **ManimEngine real integration** — Scénario @manim-real avec vrai manim binaire (+ tag @wip en CI)
. **EPIC CAP-18** — Manim slide path replacement + probe duration (COMPLETE)

== État git

main → modifications non commitées (session 025 contenu : capsule.feature + CucumberIntegrationTestRunner + CapsuleStepDefinitions)
Unit tests: BUILD SUCCESSFUL (154 unit = GREEN)
Cucumber @integration @manim: PASSED