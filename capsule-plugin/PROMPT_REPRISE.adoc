= PROMPT_REPRISE — Capsule Gradle (Détroit)

_Session terminee_ : 103 — CAP-AUDIO US-1 `AudioPostConfig` + merger 4
sources. Nouveau domaine `capsule.audio` créé (10ème) avec
`AudioPostConfig` data class 5 champs (`bgmEnabled: Boolean = false`,
`bgmFile: String = ""`, `bgmLevel: Double = -18.0`, `loudnessTarget:
Double = -16.0`, `duckingEnabled: Boolean = false`). Section `audioPost`
10ème dans `CapsuleConfig`. `mergeAudioPostConfig` (pattern
`mergeValidationConfig` — `mergeBoolean` bgmEnabled/duckingEnabled,
`mergeStr` bgmFile, `mergeDouble` bgmLevel/loudnessTarget) + 5 ENV vars
`CAPSULE_AUDIO_POST_*` + 5 props `capsule.audioPost.*`. `CapsuleExtension`
+5 properties + `CapsuleConventions` +5 champs + `pushConfigIntoExtension`
section AudioPost (5 guards convention). Baby-step TDD RED → GREEN →
REFACTOR : +20 unit (4 `AudioPostConfigTest` + 12 merger `audioPost` + 4
defaults/equals/copy). 802 unit VERT (782 baseline + 20),
`CapsulePluginFunctionalTest` VERT. Zéro régression, zéro secret.
_Prochaine session_ : 104 — CAP-AUDIO US-2 `AudioPostProcessor` interface +
Impl + NoOp + factory + `AudioPostCommand` object pur, ~4 pts, ~14 unit.

== Contexte reprise session 104 (post CAP-AUDIO US-1 config+merger)

- EPIC CAP-AUDIO (P1) — post-production audio, 5 US ~12 pts.
  US-0 ✅ S-102 (cadrage), US-1 ✅ S-103 (config+merger).
- Domaine `capsule.audio` (10ème) ouvert : `AudioPostConfig` créé (1
  fichier), `AudioPostProcessor` + Impl + NoOp + factory + Exception
  + `AudioPostCommand` à créer US-2.
- `AudioPostConfig` data class 5 champs, defaults backward compat
  (bgmEnabled=false, bgmFile="", bgmLevel=-18.0 dB, loudnessTarget=-16.0
  LUFS EBU R128, duckingEnabled=false).
- 4 sources câblées : ENV `CAPSULE_AUDIO_POST_*` < props
  `capsule.audioPost.*` < YAML `audioPost.*` < CLI
  `-Pcapsule.audioPost.*`. `mergeAudioPostConfig` utilise
  `mergeBoolean`/`mergeStr`/`mergeDouble` helpers existants.
- `CapsuleExtension` : 5 properties (`audioPostBgmEnabled/File/Level`,
  `audioPostLoudnessTarget`, `audioPostDuckingEnabled`) avec
  conventions. `pushConfigIntoExtension` pousse les 5 (guard
  `isPresent` + convention, pattern `durationValidation`).
- `CapsuleConventions` étendu 5 champs (même defaults).
- 802 unit VERT (S-103 baseline). 11 cucumber dédiés GREEN (inchangés).
- 4 factories `resolve*` dans `CapsuleManager` :
  `resolveManimVideoMixer`, `resolveManimEngine`,
  `resolveSubtitleBurnInService`, `resolveFormatConverter`. 5ème cible
  US-2 : `resolveAudioPostProcessor`.
- `StrictModeGuard.requireAvailable` — 7 resolve* branchés. 8ème =
  `resolveAudioPostProcessor` (cible US-2).
- `SubtitleBurnInService` (interface + Impl + NoOp + factory +
  `BurnInException`) — pattern de référence US-2.
- `VideoFormatConverter` (interface + Impl + NoOp + factory, retourne
  Boolean degraded) — 2ème pattern US-2.
- `FormatConversion.convertIfNeeded` (object pur dispatch) — pattern
  pour `AudioPostCommand` (object pur, argv builder).
- `CapsuleMessages` — 31 keys, pas de clé audioPost (CAP-AUDIO est une
  étape, pas une tâche).
- Version capsule : 0.0.1-SNAPSHOT (non publié Central). Consomme slider
  0.0.16 (compileOnly, 0 import réel — décision CAP-ARCH-4).

== EPICs restants (séquence MVP0)

* CAP-AUDIO (P1) — post-production audio, 5 US ~12 pts. EN COURS (US-0/1 ✅,
  reste US-2 processor+factory, US-3 wiring, US-4 cucumber).
* CAP-GLOSSARY (P1) — injection terminologique, 4 US ~6 pts
* CAP-PROVENANCE (P1) — audit context, 3 US ~5 pts
* CAP-CHAPITRE (P2) — intro/outro + chapter markers, 5 US ~10 pts
* CAP-TRANSCRIPT (P2) — reverse pipeline article AsciiDoc, 5 US ~10 pts
* CAP-PODCAST (P2) — extraction audio-only, 3 US ~4 pts
* CAP-PREVIEW (P2) — mode dry-run, 3 US ~4 pts
* CAP-CR3-4 (P2, maintenance) — convention Gradle explicite
  resolveScriptDir/resolveDeckDir
* CAP-27 (P2, maintenance) — VTT burn-in
* CAP-28 (P2, maintenance) — coverage gaps

== Rappels

* GroupId : `education.cccp`, Plugin ID : `education.cccp.capsule`
* 802 unit VERT (S-103 baseline), 57 functional VERT (S-101 baseline)
* Baby-step DDD/TDD/BDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
* Tout nouveau feature cucumber nécessite un runner dédié (bug S-082) +
  steps préfixés par feature (bug S-088 glue `capsule.scenarios` partagée)
* English only : code, Gherkin, commentaires
* Économie d'encre = skip si `!bgmEnabled && !duckingEnabled` (défaut
  false → no-op backward compat), skip si BGM enabled mais bgmFile
  blank/missing → degraded (garde original)
* Ports Ollama : 11437-11465 rotation (11434-11436 INTERDITS)
* `CapsuleMessages` object (domaine `capsule.i18n`) — 11
  `Messages*.properties` 10 langues
* Runners cucumber dédiés (11) — pas de cucumber audioPost encore
* DSL 4 sources pattern : ENV < props < YAML < CLI. Merger helpers
  `mergeStr/mergeInt/mergeDouble/mergeBoolean/mergeStrList` +
  `mergeVersioning`/`mergeCaptureStrategy`/`mergeOutputFormat` (enums)
* `AudioPostProcessor.process()` retourne Boolean (degraded mode garde
  original, pattern `VideoFormatConverter`) + `AudioPostException`
  (erreurs critiques, pattern `BurnInException`)
* `AudioPostProcessorImpl` FFmpeg : `loudnorm=I=-16:TP=-1.5:LRA=11` +
  `amix` si bgmEnabled + `sidechaincompress` si duckingEnabled
* Factory `resolveAudioPostProcessor(ffmpegPath, strict)` 5ème — pattern
  `resolveFormatConverter` : noop→NoOp, unavailable+non-strict→NoOp,
  unavailable+strict→throw, available→Impl