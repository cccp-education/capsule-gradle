= PROMPT_REPRISE — Session 019 : createSingleSlideHtml + Cucumber Manim + Branchement DSL parallelCapture

== Contexte

Session 018 — 3/3 livrés. Full check GREEN (56 unit + 8 functional + 9 cucumber = 73 tests).
Méthodologie : baby step TDD, Unit → Functional → Cucumber.

== Livrés session 018

| Livrable | Tests |
| ManimEngine.kt (interface+impl+NoOp) | 6 unit TDD |
| captureSlideParallel() + concatWebmFiles() | 4-6 unit TDD |
| Split CapsuleManager.kt (957→195 lignes, 8 fichiers) | 0 régression |

== Prochaines étapes (priorité décroissante)

. **createSingleSlideHtml()** — Injection JS pour isoler une slide du deck reveal.js. Source : fork lion92 commit `e845db4`. Fragile (dépend du DOM). Évaluer si ScreenshotCaptureImpl + parallel capture rend cette approche obsolète.
. **Amélioration Cucumber** — [manim:SceneName] avec mock ou skip conditionnel. Ajouter scénario Gherkin pour pipeline manim complet.
. **Branchement DSL** — `capsuleExtension.parallelCaptureEnabled` pour basculer entre capture séquentielle et captureSlideParallel().
. **Intégration ManimEngine dans CapsuleVideoTask** — Quand SlideType.MANIM, appeler ManimEngine.render() au lieu de PlaywrightCapture. Résoudre le MP4→WebM.
. **CapsuleManager.kt résolution Manim** — resolveManimEngine() dans CapsuleManager ou CapsuleVideoTask.

== Source files (session 018)

| Fichier | Lignes | Rôle |
| CapsuleManager.kt | 195 | Orchestrateur + companion (parseScript, resolve*) |
| CapsuleVideoTask.kt | 423 | generateCapsuleVideo + captureSlideParallel + concatWebmFiles |
| CapsuleBuildTask.kt | 130 | generateCapsule (TTS parallèle) |
| CapsuleDistribTask.kt | 123 | deployCapsule (crop 9:16) |
| CapsuleScriptTask.kt | 46 | generateCapsuleScript |
| CapsuleCompositeContextTask.kt | 93 | collectCapsuleContext (JSON N3) |
| CapsuleParseContextTask.kt | 57 | transformCapsuleContext |
| ManimEngine.kt | 102 | interface + impl + NoOp + exception |
| CapsuleModels.kt | 85 | CapsuleExtension DSL + data classes |
| PlaywrightManager.kt | 205 | PlaywrightCapture + ScreenshotCapture |
| TtsManager.kt | 165 | TtsEngine + Piper + Espeak + NoOp |

== État git

main → à pousser (session 018 contenu commité)
Full check : BUILD SUCCESSFUL (73 tests)

== Remote fork lion92

Toujours en remote. 2 commits clés restants : `136079e` + `e845db4`.
Ne PAS merger — intégration sélective pièce par pièce uniquement.