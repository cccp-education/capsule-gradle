= PROMPT_REPRISE — Capsule — Session 084
:session: 084
:date: 2026-08-09

== Session 083 terminée — CAP-ARCH-0 Cadrage EPIC CAP-ARCH refondu ✅

Audit fondé sur le code réel (pas l'hypothèse S-073) :
- Zéro `Ollama`/`ChatModel`/`ChatLanguageModel`/`langchain4j`/`LLM` dans
  `src/main` → capsule n'a AUCUN appel LLM à purger.
- `koog-agents` `0.8.0` en `implementation` mais **0 usage** dans le code →
  dépendance morte, à réactiver en `1.0.0` pour CAP-ARCH-3.
- `compileOnly slider` mais **0 import** de classe slider (seul
  `dependsOn("translateDeck")` string-based) → à re-confirmer en CAP-ARCH-4.
- `i18n-contracts` 0.0.2 déjà consommé (CAP-29.1) → l'US « migration i18n »
  du cadrage S-073 est largement faite.
- Pattern de référence : slider `AssistantManager` (SLD-8) —
  `registerLlmBuildService()` + `resolveModel()` → `LlmProvider` codebase →
  `LlmProviderChatModelAdapter` → `ChatModel` langchain4j.
- `codebase-contracts` 0.1.0 expose `CompositeContext` (EAGER + RAG +
  Graphify + Docs) = la « réalité augmentée complète » consommable.

Vision refondue : capsule LLM-driven piloté par codebase (koog + langchain4j
+ RAG + graphify), backend d'exécution existant recyclé intact
(extractSpeakerNotes, TTS, capture, subtitles, multi-lang CAP-29).
EPIC redécoupé : 7 US (CAP-ARCH-0 ✅, 1→6 TODO, ~21 pts).
Zéro code modifié. MàJ BACKLOG/INDEX/PROMPT_REPRISE/TODO.

== Prochaine session — 084 : CAP-ARCH-1 Socle LLM bridge (pattern SLD-8)

Découpage baby-step (unit → functional → cucumber) :
. **CAP-ARCH-1a** — Dépendances : `implementation` codebase plugin (comme
  slider) + koog-agents `1.0.0` (bump 0.8.0, exclusion koog submodules,
  forcer `org.jetbrains:annotations:26.0.2-1` — pattern slider). Purge de la
  dépendance morte koog 0.8.0. `compileKotlin` VERT.
. **CAP-ARCH-1b** — Domaine `capsule.ai` : `CapsuleLlmService` (object pur).
  `registerLlmBuildService(project): Provider<LlmBuildService>` +
  `resolveModel(provider, serviceProvider): ChatModel` (via
  `LlmProviderChatModelAdapter`) + `-Pai.provider` default `ollama`.
  TDD RED → GREEN : unit purs (register/resolve avec LlmProvider mocké).
. **CAP-ARCH-1c** — Wiring : tâche Gradle `capsuleAiSmokeTest` (ou intégration
  dans une tâche existante) qui appelle le ChatModel résolu avec un prompt de
  smoke test. functional test (GradleTestKit, mock HTTP `-Pollama.baseUrl`).
. **CAP-ARCH-1d** — Cucumber : `capsule_ai.feature` tags `@ai`, mock LLM HTTP,
  vérifier registration + résolution + smoke call (NoOp/mock).
  MàJ INDEX/BACKLOG/PROMPT_REPRISE.

== Backlog restant (ordre)

. 🔵 **CAP-ARCH-1** (socle LLM bridge) → **CAP-ARCH-2** (contexte augmenté
  CompositeContext) → **CAP-ARCH-3** (koog `CapsulePipelineGraph` +
  `generateCapsuleContent`) → **CAP-ARCH-4** (wiring backend existant) →
  **CAP-ARCH-5** (i18n aligné socle) → **CAP-ARCH-6** (cucumber cross-borough).
. 🔴 **CAP-CR3** — Solidité chaîne vidéo (CAP-CR3-1 CI full vrais binaires +
  probe durée WebM ; CAP-CR3-2 mode strict anti-NoOp `capsule.strictMode` ;
  CAP-CR3-3 décision `ScreenshotCaptureImpl` vs `PlaywrightCaptureImpl` ;
  CAP-CR3-4 convention `resolveScriptDir/resolveDeckDir`). Inclut le fix du
  burn-in `@integration` FAIL (subtitle path).
. 🟢 **CAP-27** — VTT burn-in. **CAP-28** — Coverage gaps (PiperTtsEngine 31%,
  ManimEngineImpl 25%, ScreenshotCaptureImpl 26%).
. 🐛 **Bug** — `CucumberTestRunner` ne lance que `capsule.feature` (S-075, à
  investiguer). 🐛 **Tooling** — filtre cucumber `@multilang` via system property
  non transmis au worker Gradle (S-082, passthrough `-PcucumberTags` à outiller).

== Contexte technique

- Build migré vers conventions plugins (education.cccp.build.* 0.0.2)
- BOM workspace-bom 0.0.16 (Central) + i18n-contracts 0.0.2 (Central)
- codebase-contracts 0.1.0 (mavenLocal) — CompositeContext/ContextChannel/ChannelBudget
- Version capsule : 0.0.1-SNAPSHOT (non publié Maven Central)
- Version slider : 0.0.16 (mavenLocal)
- 464 unit tests VERT (capsule) + 0 FAIL, 0 SKIP
- 25 functional tests VERT + 6 cucumber multilang GREEN
- `./gradlew check` VERT (cucumberTest SKIPPED sans -PrunCucumber)
- Domaines DDD capsule : `capsule.feed`, `capsule.capture`, `capsule.tts`,
  `capsule.subtitles`, `capsule.manim`, `capsule.config`, `capsule.plugin`,
  `capsule.ci`, `capsule.multilang` (clos 7/7 US)
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only : code, Gherkin, comments — no French
- EPICs clos capsule : CAP-0→25 + CAP-MODEL + K-1 + DRY-1→3 + CR-1→CR-10 + CAP-29
- Loi de l'Économie d'Encre (AGENT.adoc) : vérifier artefact résultat avant opération coûteuse
- `i18n-contracts` 0.0.2 consommé : `LanguageCatalog` (10 langues),
  `SupportedLanguage` (code/name/nativeName/rtl/localeTag)
- Pattern slider SLD-8 : `LlmBuildService` (codebase.koog.llm.service) +
  `LlmProvider` (fun interface suspend) + `LlmProviderChatModelAdapter`
