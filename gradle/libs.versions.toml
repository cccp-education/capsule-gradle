[versions]
capsule = "0.0.1-SNAPSHOT"
slider = "0.0.16"

[libraries]

[plugins]
slider = { id = "education.cccp.slider", version.ref = "slider" }
capsule = { id = "education.cccp.capsule", version.ref = "capsule" }

[bundles]
